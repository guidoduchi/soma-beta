# SOMA Beta Normalization CP-007 — Catalogue

Status: **Accepted**  
Scope: `BETA-REQ-0153`–`BETA-REQ-0177`  
Source baseline: `docs/BETA_REQUIREMENTS.md` preserves the pre-normalization wording.

## Summary

- Reviewed: **25**
- Replace: **25**
- Retain: **0**
- Requirement identities changed: **0**
- Stable clauses: **2904**

The governing obligations below supersede the earlier wording while all accepted detail remains normative in `CP007_CLAUSES.md`.

| Beta ID | Disposition | Classification | Accepted governing obligation |
|---|---|---|---|
| `BETA-REQ-0153` | **REPLACE** | Business rule; Structural invariant; Functional capability; UI/UX obligation | RFC archival is a reversible SOMA-local presentation state, never inferred merely from absent active WFM work. A Master archive affects only the exact reviewed/revalidated eligible branch members, changes only archival state, preserves operation-scoped restoration, continues exact-identity imports/reconciliation while archived, and requires explicit restoration/review when newer active evidence conflicts. |
| `BETA-REQ-0154` | **REPLACE** | Business rule; Structural invariant; Functional capability | SR and RFC lifecycles remain independent from their relationship. Reviewed links target the applicable Master/root RFC even for retained terminal SRs, never reopen/restart/cascade lifecycle, and unlinking is a reasoned append-only correction; duplicate link or already-absent unlink is an idempotent no-op. |
| `BETA-REQ-0155` | **REPLACE** | Business rule; Structural invariant; Functional capability | Manual WFM registration requires exact canonical `TK` and owning `NC` identities plus active-work eligibility; work without those identities remains Local Task. Exact later source adopts only the exact WFM, parent correction preserves identity and becomes high-risk after history, `Complete` and `Plan Cancel` remain terminal historical provider evidence, and any current-Objective consequence is separately reviewed. |
| `BETA-REQ-0156` | **REPLACE** | Business rule; Structural invariant; Quality/platform requirement | WFM source plan is one optional provider-owned interval, distinct from SOMA plan, Objective envelope, and actual execution. Valid pairs preserve explicit offsets or interpret offsetless WFM source time in `America/Guayaquil`, persist as UTC whole seconds at any valid minute, reject invalid/incomplete pairs without fabrication, review material same-Task changes without silent regrouping, and use a new Task No. for retries. |
| `BETA-REQ-0157` | **REPLACE** | Business rule; Structural invariant; Functional capability | Automatic Objective grouping non-authoritatively computes maximal global connected components of strict positive-duration Task overlap, transitively. Customer and Master/root-RFC remain explanatory partitions rather than overlap boundaries; exact-touch intervals remain separate unless reviewed together, envelopes are min-start/max-end, unresolved/multi-customer context stays explicit, and bridging work proposes consolidation rather than overlapping Objectives. |
| `BETA-REQ-0158` | **REPLACE** | Business rule; Structural invariant; UI/UX obligation | Source acceptance and Objective regrouping acceptance are separate. Regrouping is an exact non-authoritative diff against immutable revisions; Planned may restructure, In-Progress is high-risk/history-preserving, terminal is immutable, rejection retains only a bounded equivalence fingerprint, and acceptance freshly revalidates revisions, locks, membership, and global non-overlap before atomic commit. |
| `BETA-REQ-0159` | **REPLACE** | Functional capability; Business rule; Structural invariant; Quality/platform requirement | RFC/WFM adapters use source-family versioned header registries with explicit normalized closed aliases and field classes, never fuzzy or positional inference. RFC requires recognized `Task ID` plus usable identity; WFM requires `RFC No.` plus `Task No.`; missing optional-active fields warn without clearing truth, unresolved Customer stays explicit without blocking temporal grouping, and duplicate semantic headers conflict before mutation. |
| `BETA-REQ-0160` | **REPLACE** | Business rule; Structural invariant; Quality/platform requirement | Canonical WFM `Task No.` is row identity, so repeated RFC No. across distinct Task Nos is valid. Equivalent duplicate observations may collapse with warning; conflicting same-identity rows block only affected safe scope, identityless logical rows fabricate nothing, outside-matrix content has no domain meaning but remains security-scanned, and parser/pagination limits are measured LLD controls that preserve exact totals/conflict visibility. |
| `BETA-REQ-0161` | **REPLACE** | Business rule; Structural invariant; Functional capability; UI/UX obligation | Confirmed terminal subordinate evidence affects only that RFC and applicable active owned WFMs; confirmed terminal Master evidence affects the exact current two-level branch and applicable active owned WFMs. Cascade execution is separate, deliberate, exact-scope and atomic; preserves provider status plus local provenance, never changes SR/unrelated Local Task lifecycle, preserves surviving Objective work with warning, yields `Incomplete — Awaiting Review` if an In-Progress Objective loses its last executable Task, preserves terminal history, follows Communication orphan grace after confirmation, revalidates captured scope after hold, survives restart while pending, corrects errors append-only, and requires new Task No. for later genuine attempts. |
| `BETA-REQ-0162` | **REPLACE** | Business rule; Structural invariant; UI/UX obligation | When an In-Progress Objective loses executable work through an accepted governed transition, unaffected executable Tasks continue with warning; if none remain, it becomes `Incomplete — Awaiting Review`, never auto-Completed/Cancelled. Plan/membership locks derive from accepted execution/history rather than clock passage, and any Objective/Task with accepted history is permanently excluded from hard deletion; only a genuinely unaccepted empty UI working draft may be discarded. |
| `BETA-REQ-0163` | **REPLACE** | Structural invariant; Business rule; Functional capability | WFM Master/subordinate/root context derives exclusively from the accepted hierarchy of its direct owning RFC, regardless of schedule/Objective membership; no WFM-to-WFM hierarchy, authoritative `is_master`, or `master_wfm_id` exists. Distinct Task Nos remain independent attempts, and RFC-hierarchy correction recomputes current WFM context without changing WFM/source identity or rewriting prior hierarchy, Objective, or audit history. |
| `BETA-REQ-0164` | **REPLACE** | UI/UX obligation; Business rule; Structural invariant; Quality/platform requirement | Undo is a bounded action-scoped compensating capability available only while a freshly revalidated deterministic safe inverse exists; unrelated later actions do not invalidate it merely by chronology, material dependency changes may, and stale/unsafe inverses become unavailable with explanation. Successful Undo preserves history, warning acknowledgement is presentation-only, and immutable lifecycle/source/Communication/submission/warehouse/execution/audit evidence uses its governed correction workflow instead of generic Undo. |
| `BETA-REQ-0165` | **REPLACE** | Business rule; Structural invariant; Functional capability | Each RFC has at most one current canonical Customer, with unresolved ownership explicit. Customer Account Number is the strongest supported external reconciliation key while names are descriptive only; accepted WFM/linked-SR evidence may support reviewed reconciliation, Infrastructure never establishes RFC ownership, resolved hierarchy members share one Customer, unresolved members warn, and unresolved ownership does not block temporal grouping but remains a distinct explanatory partition. |
| `BETA-REQ-0166` | **REPLACE** | Business rule; Structural invariant; Functional capability; UI/UX obligation | Local Task is a first-class Task with immutable internal identity and no fabricated provider identity; Task Name is its only universally required descriptive field and RFC/SR/Device/spare/plan/actual/Notes/Objective context are optional under owning workflows. It may remain unscheduled or belong to at most one Objective; Objective-created plans may initialize from the envelope but then become independently reviewable Task facts while the Objective envelope derives from accepted member plans. |
| `BETA-REQ-0167` | **REPLACE** | Business rule; Structural invariant; Functional capability; UI/UX obligation | Task plan and actual execution remain separate, and every Task independently supports reviewed `Completed`, `Incomplete`, `Cancelled without execution`, and `Awaiting Review` outcomes. Objective start does not start member Tasks; an unstarted Task may be cancelled during/after activity while others execute, effective cancellation time is distinct from recording time, mixed Task outcomes produce a mixed aggregate, and whole-Objective Cancelled is allowed only when no Task began execution. |
| `BETA-REQ-0168` | **REPLACE** | Structural invariant; Business rule; UI/UX obligation; Quality/platform requirement | Every Objective has immutable internal identity and stable non-reusable tracking ID. Monthly ordinal is derived presentation using actual start else planned start, omitted if unscheduled, bucketed by effective-start month, tie-broken by immutable identity and allowed to recalculate without identity change; historical labels freeze. Supported IANA timezone selection, including `Pacific/Galapagos`, applies only to Objective/Task scheduling/calendar/MW presentation and cannot alter canonical or other-domain chronology. |
| `BETA-REQ-0169` | **REPLACE** | Quality/platform requirement; Structural invariant; UI/UX obligation; Functional capability | Text/structured/list/collection bounds are semantic field-specific LLD contracts using applicable grapheme/UTF-8-byte/line/depth/cardinality measures, never Alpha's universal 400-character rule or a replacement universal constant. Manual overflow rejects with actionable feedback and no truncation; imported overflow fails/quarantines at the smallest safe scope with bounded provenance; deterministic cursor pagination/virtualization uses measured limits rather than product-wide 100/500 caps, and exact totals/full exports cover the complete authoritative result. |
| `BETA-REQ-0170` | **REPLACE** | Business rule; Structural invariant; Functional capability; UI/UX obligation | Every distinct canonical WFM Task No. is a distinct attempt. Multiple same-RFC WFMs may share one Objective for genuinely distinct activities; the one-active-WFM restriction applies only to overlapping active attempts of the same reviewed activity lineage. Suspected competition preserves every identity/source fact for review as distinct activity, retry lineage, cancellation, source error, or unresolved conflict and is never hidden by identity merging or overlapping Objectives. |
| `BETA-REQ-0171` | **REPLACE** | Business rule; Functional capability; Structural invariant | Accepted provider-`Complete` WFM history without Objective may produce a reviewed historical-Objective proposal only with a valid accepted source plan; provider completion/source planning never proves actual execution, SOMA outcome, Device work, spare use, or Inventory effect, and no usable interval means terminal unassigned history. Historical Tasks may be excluded from operational counts without rewriting history; if all members are excluded the Objective projects `Excluded from operational counts`, while identities/evidence/membership/lifecycle/audit/reports remain intact and aggregates disclose included/excluded populations. |
| `BETA-REQ-0172` | **REPLACE** | Business rule; Structural invariant; Functional capability; UI/UX obligation | Valid WFM source plan remains immutable provider evidence and the default reviewed operational-plan candidate, distinct from accepted Task plan, Objective envelope, and execution. Conflict review may adopt source, retain manual with reason, correct, or remain unresolved; auto-recalculation stops only after accepted execution, terminal evidence, or explicit plan lock—not clock passage—and Objective-timezone presentation never changes canonical/source chronology. |
| `BETA-REQ-0173` | **REPLACE** | Business rule; Structural invariant; Functional capability; UI/UX obligation | Advanced Search `Product` remains discarded. Product Line is reusable across Customers; each Contract belongs to exactly one Customer; each Contract Product Line owns that Contract's customer-specific SLA policy; an SR has at most one active compatible CPL whose Contract belongs to its resolved Customer. Manual/batch classification uses reviewed eligible/incompatible/stale/unresolved/individually-reviewable partitions with one independently auditable result per target. |
| `BETA-REQ-0174` | **REPLACE** | Business rule; Structural invariant; Functional capability; Quality/platform requirement | Effective SLA elapsed duration is exactly `max(0, endpoint − accepted Report Date − cumulative accepted source-reported suspension)`: active endpoint is current canonical instant; terminal eligible endpoint is first accepted effective Resolved/Closed; Cancelled preserves history but leaves cohorts; missing Report Date is uncalculable; suspension is never inferred and regression is reviewed. Current duration/state are reproducible projections using exact whole-second/integer-or-decimal arithmetic without premature rounding, while completed report snapshots are immutable. |
| `BETA-REQ-0175` | **REPLACE** | Business rule; Structural invariant; Functional capability; Quality/platform requirement | Canonical SLA cohorts are eligible non-Cancelled SRs whose accepted Report Date falls in the selected month in fixed `America/Guayaquil`, partitioned by Customer, Contract, CPL, severity and policy tier; Objective timezone never changes membership. Individual state and cohort percentage remain distinct; deterministic lower-bound/best-possible logic yields `Pending`, `Currently Met`, `At Risk`, `Breached`, or qualifying `Final Met` without calling an open month final, and Daily/Weekly/range reports are immutable dated progress snapshots of monthly cohorts with full as-of/timezone/scope/population/denominator/policy/result evidence. |
| `BETA-REQ-0176` | **REPLACE** | Business rule; Structural invariant; Functional capability; Security/privacy constraint; UI/UX obligation | Every RFC/WFM workbook first produces exact-source staging before mutation; valid source presence remains distinct from proposal acceptance so rejected/deferred/unresolved changes cannot fabricate disappearance. Safe target dispositions occur at the smallest explainable idempotent scope; auto-accept is limited to approved low-risk classes and excludes terminal, hierarchy, ownership, timeframe, disappearance, empty-population, and competing-attempt cases; disputed terminal RFCs remain individually reviewable, accepted terminal evidence plus pending cascade proposal commit atomically while cascade execution remains separately protected, and whole-import rejection is reserved for genuine workbook-level integrity/identity/chronology/scope/safety/inseparable-consistency failure. |
| `BETA-REQ-0177` | **REPLACE** | Structural invariant; Quality/platform requirement; Business rule; UI/UX obligation | Known application instants persist as canonical UTC whole seconds; date-only values, durations, unknown effective times, and unresolved source chronology remain distinct non-instants. `America/Guayaquil` remains ordinary operational/fixed SLA authority; user-selected IANA timezone is confined to Objective/Task schedule input/presentation/calendar/MW context, new local input converts through that zone with context preserved, timezone changes only re-render existing instants and wall-clock preservation requires reviewed rescheduling; source adapters obey explicit offsets/profile rules and preserve conversion provenance, ambiguity stays unresolved, fixed timers use UTC instants, and historical evidence/reports retain their effective timezone/offset context. |
