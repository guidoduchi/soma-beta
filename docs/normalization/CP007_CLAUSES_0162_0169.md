# SOMA Beta Normalization CP-007 — Normative Clauses Shard B

Status: **Accepted**  
Scope: clauses owned by `BETA-REQ-0162`–`BETA-REQ-0169`.  
Clause IDs are the displayed prefix plus three-digit suffix. This shard contains **894** clauses.

## `BETA-REQ-0162` — prefix `OBJ-EXECLOCK`

**Governing obligation:** In-Progress Objective executable-loss handling, evidence-derived planning locks and permanent history-retention boundary.

`001` — This executable-loss behavior shall apply when an Objective is currently In Progress.
`002` — In-Progress state shall derive from accepted Objective lifecycle evidence.
`003` — The current wall clock passing planned start shall not alone make an Objective In Progress.
`004` — A merely Planned Objective shall remain governed by Planned restructuring rules until accepted execution/lifecycle evidence says otherwise.
`005` — A terminal Objective shall remain governed by terminal-history immutability rather than this in-progress continuation rule.
`006` — SOMA shall distinguish Tasks that remain executable from Tasks no longer eligible for execution.
`007` — Executability shall derive from the owning Task lifecycle and applicable dependencies.
`008` — A Task shall not be considered executable merely because it is still listed in Objective membership.
`009` — A terminal Task shall not remain executable unless its owning lifecycle explicitly provides another executable phase.
`010` — A cancelled Task shall not be counted as executable.
`011` — A locally terminated WFM shall not be counted as executable.
`012` — Historical `Complete` WFM evidence shall not be counted as executable work.
`013` — Historical `Plan Cancel` WFM evidence shall not be counted as executable work.
`014` — Exact executable-state derivation shall remain owned by Task lifecycle rather than duplicated as Objective-owned status logic.
`015` — An accepted Task cancellation may remove that Task from the Objective's executable set.
`016` — A confirmed RFC terminal cascade may remove affected WFM Tasks from the Objective's executable set.
`017` — Another separately governed Task lifecycle transition may remove a Task from the executable set.
`018` — A mere proposal shall not remove a Task from executable authority.
`019` — A rejected cancellation shall not remove executable authority.
`020` — A pending RFC terminal cascade shall not remove executable authority before confirmation.
`021` — An import warning shall not remove executable authority by itself.
`022` — If at least one executable Task remains, the In-Progress Objective shall continue.
`023` — The Objective shall not automatically become Completed merely because one Task is lost.
`024` — The Objective shall not automatically become Cancelled merely because one Task is lost.
`025` — The Objective shall not automatically become Incomplete merely because one Task is lost while executable work remains.
`026` — Each surviving executable Task shall retain its own lifecycle authority.
`027` — One Task's cancellation shall not cancel unrelated surviving Tasks.
`028` — One WFM's RFC-driven termination shall not cancel unrelated Local Tasks.
`029` — One RFC branch's terminal cascade shall not terminate unaffected WFMs in another branch.
`030` — Surviving work shall continue unless its own separate lifecycle/dependency rules prevent it.
`031` — An In-Progress Objective that loses executable work shall expose a visible warning.
`032` — The warning shall indicate that the Objective's executable membership changed after execution had begun.
`033` — The warning shall remain distinct from an Objective terminal status.
`034` — The warning shall not itself cancel or complete the Objective.
`035` — The warning shall identify affected Task context sufficiently for operator understanding.
`036` — The warning shall preserve accessible semantic meaning.
`037` — The warning shall not rely only on color.
`038` — Warning acknowledgement shall not alter the underlying lifecycle fact.
`039` — A warning shall not prevent otherwise executable surviving Tasks from continuing merely because membership changed.
`040` — Acknowledging the warning shall not constitute Task execution.
`041` — Dismissing presentation of a warning shall not erase the underlying accepted transition.
`042` — The warning shall remain derivable from authoritative history where applicable.
`043` — SOMA shall recalculate whether any executable Task remains after the accepted transition.
`044` — The calculation shall use current accepted Task lifecycle state.
`045` — Stale UI membership state shall not determine the result.
`046` — Pending proposals shall not be treated as accepted removal.
`047` — If one or more executable Tasks remain, the zero-executable rule shall not trigger.
`048` — If zero executable Tasks remain, the Objective shall enter the governed incomplete-review condition.
`049` — An In-Progress Objective with no executable Task remaining shall become `Incomplete — Awaiting Review`.
`050` — `Incomplete — Awaiting Review` shall be an explicit Objective lifecycle/review state.
`051` — The state shall preserve that the Objective had entered execution.
`052` — The state shall preserve the accepted reason/consequence that no executable Tasks remain.
`053` — The state shall remain reviewable by the operator.
`054` — The state shall not imply successful completion.
`055` — Loss of the final executable Task shall never automatically mark an In-Progress Objective Completed.
`056` — Task cancellation shall not be interpreted as Task completion.
`057` — RFC-cascade termination shall not be interpreted as successful Task completion.
`058` — Zero executable Tasks shall not be treated as equivalent to all work Completed.
`059` — A “nothing left to execute” condition shall remain distinct from “all planned work completed successfully.”
`060` — Loss of the final executable Task shall never automatically mark an In-Progress Objective Cancelled.
`061` — Objective cancellation shall remain a separately governed lifecycle decision.
`062` — Cancellation of all remaining Tasks shall not retroactively mean the already-started Objective itself was cancelled before execution.
`063` — RFC terminal cascade shall not fabricate an Objective cancellation decision.
`064` — `Incomplete — Awaiting Review` shall preserve the distinction between interrupted/incomplete execution and deliberate Objective cancellation.
`065` — `Incomplete — Awaiting Review` shall require a governed subsequent review to determine the appropriate accepted outcome/correction.
`066` — Review shall expose the Objective's prior lifecycle history.
`067` — Review shall expose the Task transitions that removed executable work.
`068` — Review shall not rewrite prior execution evidence.
`069` — The exact post-review transition choices remain governed by the Objective lifecycle contract rather than invented by this requirement.
`070` — Objective plan mutability/lock state shall derive from accepted lifecycle/execution history.
`071` — Task plan mutability/lock state shall derive from accepted lifecycle/execution history.
`072` — Accepted Objective start may create applicable planning locks.
`073` — Accepted Task start may create applicable planning locks.
`074` — Accepted execution evidence may create applicable planning locks.
`075` — Accepted lifecycle history may create applicable planning locks.
`076` — Accepted correction/history may constrain later plan mutation where required.
`077` — Objective membership mutability/lock state shall derive from accepted execution/history.
`078` — Accepted Objective start may constrain membership restructuring.
`079` — Accepted Task start may constrain membership restructuring.
`080` — Accepted Task execution may constrain reassignment to another Objective.
`081` — Accepted historical Objective membership shall not be rewritten merely to restore mathematically ideal grouping.
`082` — In-progress regrouping shall therefore obey the lifecycle locks already established in `0158`.
`083` — The current clock passing an Objective's planned start shall not alone lock its plan.
`084` — The current clock passing an Objective's planned start shall not alone lock its membership.
`085` — The current clock passing a Task's planned start shall not alone prove the Task started.
`086` — The current clock passing a Task's planned end shall not alone prove the Task completed.
`087` — The current clock passing the Objective's planned end shall not alone make the Objective terminal.
`088` — A late unopened Planned Objective may remain Planned until accepted lifecycle evidence says otherwise.
`089` — Scheduled chronology shall remain plan evidence rather than execution evidence.
`090` — Once an Objective has accepted start evidence, that start history shall be preserved.
`091` — Once a Task has accepted start evidence, that start history shall be preserved.
`092` — Starting evidence shall not be erased merely to regain Draft-like editability.
`093` — Correction of erroneous start evidence shall use the governed correction workflow rather than hard deletion.
`094` — An Objective with accepted start evidence shall never be hard-deleted.
`095` — An Objective with accepted execution evidence shall never be hard-deleted.
`096` — An Objective with accepted lifecycle evidence shall never be hard-deleted.
`097` — An Objective with accepted history shall never be hard-deleted.
`098` — A terminal Objective shall never be hard-deleted.
`099` — An `Incomplete — Awaiting Review` Objective shall never be hard-deleted.
`100` — Archival shall not re-enable hard deletion.
`101` — A Task with accepted start evidence shall never be hard-deleted.
`102` — A Task with accepted execution evidence shall never be hard-deleted.
`103` — A Task with accepted lifecycle evidence shall never be hard-deleted.
`104` — A Task with accepted history shall never be hard-deleted.
`105` — A WFM with accepted source provenance shall remain subject to its stronger identity/history retention rules and shall not be hard-deleted as though it were an empty local draft.
`106` — A cancelled Task with accepted history shall not be hard-deleted merely because it will never execute.
`107` — A completed Task shall never be hard-deleted.
`108` — Hard deletion shall not be used to erase an erroneous Objective lifecycle event.
`109` — Hard deletion shall not be used to erase an erroneous Task lifecycle event.
`110` — Hard deletion shall not be used to move executed work into another Objective by deleting and recreating it.
`111` — Hard deletion shall not be used to reuse a prior Task identity.
`112` — Corrections, cancellation, supersession, archival, or other owning history-preserving workflows shall be used instead where applicable.
`113` — An unaccepted empty Objective UI working draft may be discarded.
`114` — An unaccepted empty Task UI working draft may be discarded.
`115` — Such a discard shall concern UI working state rather than an accepted domain entity with lifecycle history.
`116` — “Empty” shall mean no accepted domain content/history requiring preservation.
`117` — A working draft that has never committed an Objective/Task shall not require a fake deletion lifecycle event merely to close the editor.
`118` — Closing or discarding such an empty working draft shall not create authoritative Objective/Task history.
`119` — A persisted/accepted entity shall not be relabelled as an “empty UI draft” merely to bypass retention constraints.
`120` — Tests shall prove surviving executable work continues with warning, zero executable Tasks in an In-Progress Objective yield `Incomplete — Awaiting Review`, lifecycle/membership locks derive from accepted evidence rather than current time, and any Objective/Task with accepted start/execution/evidence/history is permanently excluded from hard deletion while only a genuinely unaccepted empty UI working draft may be discarded without authoritative mutation.

## `BETA-REQ-0163` — prefix `WFM-HIERCTX`

**Governing obligation:** WFM hierarchy context is a projection of the direct owning RFC hierarchy; no competing WFM hierarchy exists and corrections recompute current context without rewriting identities/history.

`001` — Every WFM shall derive its current hierarchy context from its owning RFC.
`002` — A WFM shall not maintain independent authoritative Master/Subordinate hierarchy state.
`003` — WFM hierarchy context shall therefore be a projection of RFC hierarchy authority.
`004` — The owning RFC relationship shall be resolved through immutable RFC identity.
`005` — WFM Task Name shall not determine hierarchy context.
`006` — WFM schedule shall not determine hierarchy context.
`007` — WFM Objective membership shall not determine hierarchy context.
`008` — WFM provider status shall not determine hierarchy context.
`009` — A WFM owned directly by a Master RFC shall project Master-RFC context.
`010` — A WFM owned by a standalone/root RFC shall project root/standalone RFC context according to the accepted RFC hierarchy vocabulary.
`011` — Adding subordinates to an owning root RFC may change the WFM's current contextual presentation from standalone/root to Master context.
`012` — Such contextual recalculation shall not change WFM identity.
`013` — A WFM owned by a subordinate RFC shall project subordinate-RFC context.
`014` — The WFM shall retain its exact owning subordinate RFC identity.
`015` — The governing Master RFC may be derived through that subordinate's accepted parent relationship.
`016` — The derived Master context shall not replace the WFM's direct owning RFC.
`017` — A subordinate-owned WFM remains owned by the subordinate RFC, not directly by the Master.
`018` — Master context is therefore explanatory/derived context rather than a second Task-parent relationship.
`019` — A WFM need not belong to an Objective to possess RFC hierarchy context.
`020` — An unscheduled WFM shall still derive Master/Subordinate context from its owning RFC.
`021` — Missing Task plan shall not remove hierarchy context.
`022` — Missing Objective membership shall not remove hierarchy context.
`023` — Temporal grouping shall not be required before hierarchy context can be shown.
`024` — Objective membership shall not assign Master/Subordinate context to a WFM.
`025` — Moving a WFM between Objectives shall not change RFC hierarchy context.
`026` — Removing a WFM from an Objective shall not change RFC hierarchy context.
`027` — Two WFMs in the same Objective may legitimately have different RFC hierarchy contexts.
`028` — Objective work-package partitions may display RFC hierarchy context without owning it.
`029` — SOMA shall not create an authoritative WFM-to-WFM parent relationship.
`030` — A WFM shall not own another WFM as a subordinate.
`031` — A WFM shall not become another WFM's Master.
`032` — A WFM shall not become another WFM's child.
`033` — Task Name similarity shall not create WFM hierarchy.
`034` — Matching schedules shall not create WFM hierarchy.
`035` — Shared Objective membership shall not create WFM hierarchy.
`036` — Shared SR relationship shall not create WFM hierarchy.
`037` — Workbook row adjacency shall not create WFM hierarchy.
`038` — Provider ordering shall not create WFM hierarchy.
`039` — SOMA shall not persist an authoritative WFM `is_master` flag.
`040` — A cached derived display value resembling `is_master` may exist only if it is strictly recomputable from RFC hierarchy.
`041` — Such a cache shall not be independently editable.
`042` — Such a cache shall not survive contradiction with accepted RFC hierarchy.
`043` — Rebuilding projections shall reproduce current context from owning RFC relationships.
`044` — SOMA shall not persist an authoritative `master_wfm_id`.
`045` — A WFM shall not require another WFM identity to derive its RFC hierarchy context.
`046` — A subordinate-owned WFM shall resolve governing Master context through `WFM → owning RFC → Master RFC`.
`047` — SOMA shall not resolve governing Master context through `WFM → another WFM`.
`048` — A WFM display/grouping helper shall not become hidden `master_wfm_id` authority.
`049` — Every canonical WFM Task No. shall identify an independent WFM attempt.
`050` — Two WFMs owned by the same RFC shall remain distinct attempts.
`051` — Two WFMs with similar Task Names shall remain distinct attempts when Task Nos. differ.
`052` — Two WFMs with identical planned intervals shall remain distinct attempts when Task Nos. differ.
`053` — Two WFMs in the same Objective shall remain distinct attempts.
`054` — One WFM shall not become the parent identity of another merely because it occurred earlier.
`055` — A later retry shall use its new canonical Task No. and remain distinct from the prior WFM.
`056` — Retry relationships, if displayed, shall not become WFM hierarchy authority unless separately approved.
`057` — One WFM's lifecycle state shall not automatically determine another WFM's lifecycle merely because both share RFC context.
`058` — Completion of one WFM shall not complete another WFM.
`059` — Cancellation of one WFM shall not cancel another WFM absent separately governed RFC cascade authority.
`060` — A retry WFM shall not reactivate the historical original WFM.
`061` — Distinct WFM identities shall preserve independent source provenance and lifecycle history.
`062` — An accepted RFC hierarchy correction shall trigger recalculation of current WFM hierarchy context for affected WFMs.
`063` — Reparenting a subordinate RFC may change the governing Master context projected for its WFMs.
`064` — Removing an RFC parent may change its WFMs from subordinate context to root/standalone context.
`065` — Adding the first subordinate to a root RFC may change direct-owned WFM presentation from standalone/root to Master context.
`066` — Context recalculation shall use the newly accepted RFC hierarchy.
`067` — Context recalculation shall not require editing each WFM manually.
`068` — RFC hierarchy correction shall not replace affected WFM internal identities.
`069` — RFC hierarchy correction shall not replace affected WFM Task Nos.
`070` — RFC hierarchy correction shall not create duplicate WFMs merely to represent new context.
`071` — RFC hierarchy correction shall not merge distinct WFMs.
`072` — RFC hierarchy correction shall not change which RFC directly owns a WFM unless the WFM parent relationship itself is separately corrected.
`073` — Reparenting an owning subordinate RFC changes its governing Master context, not its direct ownership of its WFMs.
`074` — WFM source provenance shall remain attached to the same WFM identity.
`075` — Provider RFC No./Task No. evidence shall remain historically preserved according to its owning import authority.
`076` — Current context recalculation shall not erase prior accepted RFC hierarchy evidence.
`077` — History shall remain capable of explaining the hierarchy context that existed before correction.
`078` — History shall remain capable of explaining the correction that changed current context.
`079` — Current WFM context may differ from historical context without rewriting the historical record.
`080` — RFC hierarchy correction shall remain append-only/history-preserving under `0152`.
`081` — RFC hierarchy correction shall not rewrite prior Objective membership.
`082` — A WFM historically executed under an Objective shall remain historically associated with that Objective.
`083` — Recalculating current Master/Subordinate context shall not move the WFM to another Objective automatically.
`084` — Recalculating current context shall not change a terminal Objective's historical work-package structure.
`085` — Any eligible current Objective regrouping consequence shall remain separately governed by `0158`.
`086` — RFC hierarchy correction shall preserve prior WFM audit chronology.
`087` — WFM creation/import audit times shall not be rewritten because hierarchy changed.
`088` — Prior Objective planning/execution audit shall not be rewritten because hierarchy changed.
`089` — Hierarchy correction shall append its own audit/history evidence.
`090` — Current projected context shall not alter the timestamp or semantic meaning of earlier audit events.
`091` — WFM workbenches/lists may display direct owning RFC and derived governing Master context together where useful.
`092` — UI shall distinguish direct owning RFC from derived Master context for subordinate-owned WFMs.
`093` — UI shall not present another WFM as the authoritative Master of a WFM.
`094` — Unscheduled WFMs shall expose the same RFC-derived hierarchy context as scheduled WFMs.
`095` — Current context shall update after accepted RFC hierarchy correction without hiding the historical correction trail.
`096` — Tests shall prove WFM hierarchy context derives exclusively from owning-RFC hierarchy regardless of Objective/schedule state, no WFM-to-WFM/`is_master`/`master_wfm_id` authority exists, distinct Task Nos. remain independent attempts, and RFC hierarchy correction recalculates current context while preserving WFM/source identities, previous hierarchy evidence, Objective history, and audit chronology.

## `BETA-REQ-0164` — prefix `ACT-UNDO`

**Governing obligation:** Undo is a bounded action-scoped safe inverse, independent from unrelated later chronology and prohibited from erasing immutable evidence.

`001` — Every Undo opportunity shall belong to one specific accepted action.
`002` — Undo shall not operate as an unbounded application-wide rewind.
`003` — Undo shall identify the command/action whose consequence it would safely invert.
`004` — Two independently undoable actions shall retain independent Undo eligibility.
`005` — Undo eligibility shall not be inferred solely from chronological recency.
`006` — “Last action” shall not itself constitute Undo authority.
`007` — An action that has no governed safe inverse shall expose no generic Undo merely for UI consistency.
`008` — An Undo opportunity shall identify its exact authoritative target.
`009` — Entity-targeted Undo shall use immutable internal target identity.
`010` — Relationship-targeted Undo shall identify the exact affected relationship.
`011` — Membership-targeted Undo shall identify the exact Task/Objectives involved.
`012` — Setting-targeted Undo shall identify the exact accepted setting mutation.
`013` — Descriptive labels shall not be the sole identity used to determine what Undo affects.
`014` — Target rename shall not cause Undo to affect a different entity.
`015` — Undo shall identify the exact consequence it proposes to reverse.
`016` — The inverse shall be narrower than or equal to the original action's governed effect.
`017` — Undo shall not reverse unrelated mutations bundled only by temporal proximity.
`018` — Undo shall not infer hidden collateral reversals beyond the original safe inverse contract.
`019` — Undo preview/label shall describe the actual consequence rather than merely display `Undo`.
`020` — A multi-entity inverse shall identify every entity whose state would change where such Undo is permitted.
`021` — An available Undo shall have a deterministic expected resulting state.
`022` — The UI shall communicate the resulting state sufficiently for the operator to understand the inverse.
`023` — Undo shall not mean “try to go back somehow.”
`024` — If the expected resulting state cannot be established safely, Undo shall be unavailable.
`025` — The resulting state shall still satisfy all current domain invariants.
`026` — Undo availability shall be bounded.
`027` — An Undo opportunity may expire according to its action contract.
`028` — An Undo opportunity may become invalid because authoritative state changes.
`029` — Undo shall not remain indefinitely available merely because its original action remains in history.
`030` — Exact time-based expiry, where used, shall be explicitly defined by the owning action contract.
`031` — A time expiry shall not be invented globally for actions whose safety is state-bound rather than time-bound.
`032` — A state-bound invalidation shall not be replaced with an arbitrary fixed timeout merely for implementation convenience.
`033` — Every undoable action shall define the condition under which its inverse expires or becomes invalid.
`034` — The condition may depend on target lifecycle state.
`035` — The condition may depend on dependent relationships.
`036` — The condition may depend on accepted execution/history.
`037` — The condition may depend on another action that materially consumes or supersedes the original consequence.
`038` — The condition may depend on an explicitly governed expiry period where appropriate.
`039` — The invalidation condition shall be deterministic from authoritative state.
`040` — Undo availability displayed in the UI shall not itself prove that the inverse remains valid at execution time.
`041` — SOMA shall freshly revalidate the inverse immediately before applying Undo.
`042` — Revalidation shall confirm exact target identity.
`043` — Revalidation shall confirm the original action/consequence remains applicable.
`044` — Revalidation shall confirm current lifecycle state permits the inverse.
`045` — Revalidation shall confirm current relationship state permits the inverse.
`046` — Revalidation shall confirm current execution/history locks permit the inverse.
`047` — Revalidation shall confirm current dependent state permits the inverse.
`048` — Revalidation shall verify applicable domain invariants before commit.
`049` — A stale Undo shall not partially apply.
`050` — Undo shall be available only while a safe inverse exists.
`051` — A mechanically possible inverse shall not be considered safe if it violates accepted lifecycle/history semantics.
`052` — An inverse shall not erase immutable evidence.
`053` — An inverse shall not fabricate earlier history.
`054` — An inverse shall not bypass protected confirmation/correction workflows.
`055` — An inverse shall not violate referential integrity.
`056` — An inverse shall not violate Objective membership locks.
`057` — An inverse shall not violate RFC hierarchy invariants.
`058` — An inverse shall not violate Inventory lifecycle invariants.
`059` — A later action shall not invalidate an existing Undo merely because it occurred later in time.
`060` — A later action against an unrelated entity shall not invalidate the Undo.
`061` — A later action in an unrelated workspace shall not invalidate the Undo.
`062` — A later Contact edit shall not invalidate an unrelated Objective-membership Undo solely due to chronology.
`063` — A later view/filter change shall not invalidate an unrelated domain Undo.
`064` — Navigation shall not invalidate an Undo merely because the operator opened another workbench.
`065` — Application-wide stack position shall not be the governing validity rule.
`066` — A later action may invalidate Undo when it materially changes the target or inverse prerequisites.
`067` — Starting a Task may invalidate a previously safe Task-membership Undo.
`068` — Creating dependent accepted history may invalidate a previously safe deletion/removal inverse.
`069` — A relationship correction may invalidate an Undo whose base relationship no longer exists.
`070` — A later accepted action that supersedes the original consequence may invalidate its inverse.
`071` — Invalidation shall arise from material dependency/state change rather than mere chronology.
`072` — A stale inverse shall become unavailable.
`073` — An unsafe inverse shall become unavailable.
`074` — An unavailable Undo shall not remain clickable only to fail after confirmation when the blocker is already known.
`075` — UI shall update Undo availability when relevant authoritative state changes.
`076` — If state changes between display and activation, execution-time revalidation shall still block the stale inverse.
`077` — A stale/unsafe Undo shall provide an understandable explanation where the operator can encounter its unavailable state.
`078` — The explanation shall identify the governing invalidation reason.
`079` — The explanation shall distinguish expiry from state invalidation.
`080` — The explanation shall distinguish execution/history lock from unrelated later activity.
`081` — The explanation shall identify an applicable correction workflow when one exists.
`082` — The explanation shall not imply data loss when the action remains preserved in history.
`083` — A successful Undo shall be an accepted action with its own attributable evidence where the owning command requires history/audit.
`084` — Undo shall not remove the historical fact that the original action occurred.
`085` — The original action shall remain historically visible where its history is governed.
`086` — The inverse action shall remain historically visible where required.
`087` — Current projection may reflect the inverse while preserving both actions.
`088` — Undo shall therefore behave as a safe compensating/inverse command rather than history deletion.
`089` — Warning acknowledgement may change only warning presentation/acknowledgement state.
`090` — Acknowledging a warning shall not erase the underlying authoritative fact.
`091` — Acknowledging a warning shall not alter the underlying lifecycle event.
`092` — Acknowledging a warning shall not alter source evidence.
`093` — Acknowledging a warning shall not alter execution evidence.
`094` — Acknowledging a warning shall not alter relationship authority.
`095` — Acknowledging a warning shall not alter audit evidence.
`096` — Recomputing a warning from authoritative state shall still be possible after acknowledgement.
`097` — Warning acknowledgement shall not be presented as correction of the warning's underlying condition.
`098` — Accepted lifecycle evidence shall not be reversed through generic Undo when the owning lifecycle requires correction/cancellation/reversal.
`099` — A Task start shall not be erased through generic Undo after it has become immutable lifecycle evidence.
`100` — An Objective start shall not be erased through generic Undo after it has become immutable lifecycle evidence.
`101` — A terminal RFC cascade shall not be erased through generic Undo.
`102` — Immutable lifecycle mistakes shall use their governed correction workflow.
`103` — Accepted RFC/WFM source evidence shall not be erased through generic Undo.
`104` — Accepted source chronology shall not be erased through generic Undo.
`105` — Erroneous accepted source interpretation shall use the governed source/projection correction workflow.
`106` — Accepted Communication evidence shall not be erased through generic Undo.
`107` — Accepted submission evidence shall not be erased through generic Undo.
`108` — Accepted warehouse decision/evidence shall not be erased through generic Undo.
`109` — Such facts shall use correction, cancellation, supersession, replacement, resend, restoration, or another explicitly governed owning workflow.
`110` — Accepted execution evidence shall not be removed through generic Undo.
`111` — Accepted audit events shall never be removed through Undo.
`112` — Audit mistakes shall be addressed through append-only correction rather than event deletion.
`113` — Undo availability shall therefore never be used as a bypass around the append-only evidence architecture.
`114` — Tests shall prove Undo remains action-scoped and independently valid across unrelated later actions, becomes unavailable with explanation after material expiry/state changes, freshly revalidates exact targets and safe inverse conditions, records a history-preserving inverse rather than erasing the original action, keeps warning acknowledgement presentation-only, and routes immutable lifecycle/source/Communication/submission/warehouse/execution/audit evidence through their specific governed correction workflows instead of generic Undo.

## `BETA-REQ-0165` — prefix `RFC-CUST`

**Governing obligation:** RFC Customer ownership is singular/reviewed, account-number-led, hierarchy-consistent and unresolved-safe for grouping.

`001` — Every RFC shall have at most one current Customer Organization.
`002` — An RFC may temporarily have no resolved Customer Organization.
`003` — No resolved Customer shall be represented as unresolved rather than as a fabricated default Customer.
`004` — An RFC shall not simultaneously own two current Customer Organizations.
`005` — Customer ownership cardinality shall be enforced structurally rather than through UI convention alone.
`006` — Customer ownership shall use canonical Customer Organization identity.
`007` — Customer ownership shall not use Customer display name as the relational identity.
`008` — The current RFC Customer relationship shall reference immutable Customer Organization identity.
`009` — Customer rename shall not change RFC ownership.
`010` — Customer display-name correction shall not create a new Customer relationship merely because visible text changes.
`011` — Similar Customer names shall not merge Customer identities.
`012` — Different Customer organizations may legitimately have similar or identical display names.
`013` — Customer Account Number shall be the strongest supported external Customer reconciliation key for RFC source reconciliation.
`014` — A usable recognized Customer Account Number may support reconciliation to an existing canonical Customer Organization.
`015` — Exact Customer Account Number comparison shall follow the governed normalization contract.
`016` — Customer Account Number normalization shall not invent missing digits or values.
`017` — Malformed Customer Account Number shall not silently reconcile ownership.
`018` — Customer Account Number conflict shall remain explicit rather than being overridden by a similar Customer name.
`019` — Customer Account Number shall outrank display-name similarity when the evidence conflicts.
`020` — Customer Account Name shall remain a display/descriptive source field.
`021` — Customer name alone shall not independently prove canonical Customer identity.
`022` — Exact text equality of Customer names shall not independently establish identity.
`023` — Case-insensitive equality of Customer names shall not independently establish identity.
`024` — Fuzzy Customer-name similarity shall not independently establish identity.
`025` — Customer-name aliases shall not independently establish identity unless governed by separate accepted reconciliation authority.
`026` — Customer name may support human review without becoming the reconciliation key.
`027` — Recognized RFC source Customer Account Number evidence may participate in the governed Customer reconciliation workflow.
`028` — Recognized RFC source Customer name may accompany that reconciliation as descriptive evidence.
`029` — Source-owned Customer facts shall be staged and reviewed according to the import contract before authoritative mutation where review is required.
`030` — Source omission shall not clear previously accepted RFC Customer ownership.
`031` — Missing Customer evidence shall preserve unresolved or prior accepted state according to `0159`.
`032` — Accepted WFM evidence may support a proposal to reconcile an RFC's Customer Organization.
`033` — WFM evidence shall not silently assign RFC Customer ownership.
`034` — WFM-derived reconciliation shall identify the exact owning RFC.
`035` — WFM-derived reconciliation shall preserve the source evidence supporting the proposal.
`036` — WFM-derived reconciliation shall remain subordinate to stronger accepted RFC Customer evidence where such evidence exists.
`037` — A WFM Customer-like value shall not automatically create a new Customer Organization.
`038` — WFM Customer name similarity alone shall not prove RFC Customer identity.
`039` — An accepted linked Service Request may support a proposal to reconcile the linked RFC Customer Organization.
`040` — SR-based reconciliation shall use the canonical linked SR identity.
`041` — SR-derived Customer evidence shall remain a proposal until reviewed when RFC ownership is unresolved or conflicting.
`042` — Linking an SR shall not silently overwrite RFC Customer ownership.
`043` — A terminal/historical SR may still provide accepted relationship/customer evidence for reconciliation.
`044` — SR lifecycle state shall not determine RFC Customer ownership.
`045` — Multiple linked SRs with conflicting Customer Organizations shall produce a reconciliation conflict rather than arbitrary selection.
`046` — Strong Customer reconciliation evidence shall not bypass an explicitly required review workflow.
`047` — Reconciliation review shall identify the current RFC Customer state.
`048` — Reconciliation review shall identify the proposed Customer Organization.
`049` — Reconciliation review shall identify the supporting evidence source.
`050` — Reconciliation review shall expose conflicting evidence where present.
`051` — Acceptance shall revalidate current RFC identity and Customer state before commit.
`052` — Infrastructure context shall not independently establish RFC Customer ownership.
`053` — A Network Element Customer/Site context shall not silently assign the RFC Customer.
`054` — A Device Reference resolved to Infrastructure shall not silently assign the RFC Customer.
`055` — A Site's Customer Organization shall not automatically become the RFC Customer.
`056` — A Dispatch Location shall not establish RFC Customer ownership.
`057` — A Cloud Deployment shall not establish RFC Customer ownership.
`058` — Infrastructure context may be displayed as contextual evidence without becoming ownership authority.
`059` — A Customer mismatch between Infrastructure context and RFC ownership may warn where relevant but shall not silently rewrite either authority.
`060` — Resolved RFC hierarchy members shall share one Customer Organization.
`061` — A resolved Master and its resolved subordinate RFCs shall have the same canonical Customer Organization identity.
`062` — Two resolved subordinate RFCs under one Master shall not belong to different Customer Organizations.
`063` — Known Customer mismatch shall invalidate ordinary hierarchy acceptance.
`064` — Known Customer mismatch shall remain distinct from unresolved Customer evidence.
`065` — Customer-consistency validation shall occur when hierarchy relationships are created.
`066` — Customer-consistency validation shall occur when hierarchy relationships are reparented.
`067` — Customer-consistency validation shall occur when an unresolved Customer becomes resolved.
`068` — A proposed RFC Customer reconciliation shall consider current hierarchy membership before acceptance.
`069` — Assigning a Customer that would create a known hierarchy mismatch shall not commit through the ordinary reconciliation path.
`070` — Review shall expose the affected Master/subordinate branch when Customer assignment would create or resolve a consistency conflict.
`071` — Customer reconciliation shall not silently reparent the RFC merely to make Customer consistency pass.
`072` — Hierarchy correction and Customer reconciliation shall remain distinct governed decisions.
`073` — An RFC with no resolved Customer shall remain explicitly unresolved.
`074` — Unresolved Customer shall not be represented as an empty string meaning a known Customer.
`075` — Unresolved Customer shall not be represented as “same as parent” without accepted reconciliation evidence.
`076` — A subordinate with unresolved Customer shall not automatically inherit its Master's Customer as authoritative ownership.
`077` — A Master with unresolved Customer shall not automatically inherit Customer from one subordinate.
`078` — Multiple unresolved branch members shall remain individually unresolved.
`079` — An unresolved RFC Customer shall expose an applicable warning.
`080` — A hierarchy branch containing unresolved Customer ownership shall expose that Customer consistency is not fully proven.
`081` — The warning shall remain distinct from a known Customer mismatch error/conflict.
`082` — The warning shall not fabricate a Customer value.
`083` — Warning acknowledgement shall not resolve the Customer.
`084` — Warning acknowledgement shall not alter hierarchy/customer authority.
`085` — Missing RFC Customer resolution shall not by itself prevent eligible Task temporal grouping.
`086` — An eligible Task owned by an unresolved-Customer RFC may participate in strict-overlap grouping.
`087` — Customer uncertainty shall not invalidate otherwise valid Task intervals.
`088` — Customer uncertainty shall not force the Task to remain unscheduled.
`089` — Customer uncertainty shall not create a separate overlapping Objective merely to isolate unknown ownership.
`090` — Temporal grouping shall represent unresolved Customer work through an explicit unresolved work-package partition.
`091` — An unresolved work-package partition shall remain visibly distinct from a resolved Customer partition.
`092` — Two unresolved RFC Customer contexts shall not automatically be merged into one canonical Customer partition merely because both are unresolved.
`093` — Unresolved partitions may coexist inside one Objective when temporal grouping requires it.
`094` — A multi-customer Objective may contain resolved and unresolved partitions simultaneously.
`095` — Objective membership shall not assign Customer ownership to the unresolved RFC.
`096` — Later accepted Customer reconciliation shall update current RFC Customer ownership.
`097` — Later reconciliation shall preserve prior unresolved-state history.
`098` — Later reconciliation may update current Objective explanatory work-package partitioning where the Objective lifecycle permits current projection change.
`099` — Customer resolution shall not rewrite terminal Objective history.
`100` — Customer resolution shall not silently regroup Objective temporal components.
`101` — Any material Objective regrouping consequence shall follow `0158`.
`102` — Correcting an erroneous RFC Customer assignment shall preserve the RFC's immutable identity.
`103` — Customer correction shall append reconciliation/correction history.
`104` — Prior accepted Customer evidence shall not be deleted merely because current ownership is corrected.
`105` — Customer correction shall preserve applicable source and relationship provenance.
`106` — Customer correction shall revalidate current RFC hierarchy consistency.
`107` — Tests shall prove at-most-one RFC Customer ownership, Customer Account Number precedence over display-name evidence, review-gated WFM/SR reconciliation, Infrastructure non-authority, hierarchy-wide same-Customer enforcement for resolved members, explicit unresolved warnings, and preservation of prior truth/history through correction.
`108` — Tests shall prove unresolved Customer ownership does not block valid temporal grouping, remains an explicit distinct work-package partition, and later reconciliation updates current context without inventing prior ownership or silently restructuring terminal/history-bound Objectives.

## `BETA-REQ-0166` — prefix `LOCAL-TASK`

**Governing obligation:** Local Task is a first-class immutable SOMA Task with Task-Name-only universal descriptive requirement, no fabricated provider identity, optional context and independently reviewable planning.

`001` — A Local Task shall be a first-class Task entity.
`002` — A Local Task shall not be represented merely as temporary UI metadata.
`003` — A Local Task shall participate in governed Task lifecycle independently of WFM source existence.
`004` — A Local Task may exist even when no provider WFM will ever exist.
`005` — Local Task status shall derive from Task lifecycle authority rather than WFM source status.
`006` — Every accepted Local Task shall receive an immutable internal Task identity.
`007` — Local Task internal identity shall be unique.
`008` — Local Task internal identity shall not derive from Task Name.
`009` — Renaming a Local Task shall not change its internal identity.
`010` — Two Local Tasks with identical names shall remain distinct identities.
`011` — Objective membership shall not become Local Task identity.
`012` — Linked RFC identity shall not become Local Task identity.
`013` — Linked SR identity shall not become Local Task identity.
`014` — A Local Task shall not receive a fabricated WFM Task No.
`015` — A Local Task shall not receive an invented `TK` identifier for storage convenience.
`016` — A Local Task shall not receive an invented `TK` identifier for display consistency.
`017` — Objective creation context shall not fabricate provider identity.
`018` — RFC linkage shall not fabricate provider identity.
`019` — SR linkage shall not fabricate provider identity.
`020` — Task Name similarity to a WFM shall not fabricate provider identity.
`021` — Similar planned chronology to a WFM shall not fabricate provider identity.
`022` — Later expectation of a provider WFM shall not preassign a speculative Task No.
`023` — Local Task and WFM classifications shall remain distinct Task kinds.
`024` — Local Task kind shall remain part of accepted Task provenance.
`025` — Merely linking a Local Task to an RFC shall not convert it to WFM.
`026` — Merely placing a Local Task in an Objective containing WFMs shall not convert it to WFM.
`027` — Merely matching WFM Task Name shall not convert it to WFM.
`028` — Exact source WFM adoption shall never consume a descriptively similar Local Task under `0155`.
`029` — Task Name shall be the only universally required descriptive field for Local Task creation.
`030` — Task Name shall be nonblank under the governed text-validation contract.
`031` — Task Name shall remain descriptive rather than identity authority.
`032` — Task Name may be corrected without replacing Local Task identity.
`033` — Task Name bounds shall follow the field-specific validation contract rather than Alpha's universal 400-character rule.
`034` — A Local Task may exist without an RFC relationship.
`035` — RFC relationship shall be optional.
`036` — Where present, RFC linkage shall follow the governing SR/RFC/Task relationship rules.
`037` — Missing RFC shall not make an otherwise valid Local Task invalid.
`038` — A Local Task without RFC context shall not receive a fabricated RFC identity.
`039` — A Local Task may exist without an SR relationship.
`040` — A Local Task may link to zero or more Service Requests under the governing Task relationship contract.
`041` — Missing SR shall not make an otherwise valid Local Task invalid.
`042` — Linking an SR later shall preserve Local Task identity.
`043` — A Local Task may exist without Device/Infrastructure context.
`044` — Device relationships shall be optional.
`045` — Device Reference or Network Element context shall follow its owning Infrastructure workflow.
`046` — Missing Device context shall not make the Local Task invalid.
`047` — Later Device reconciliation shall preserve Local Task identity/history.
`048` — A Local Task may exist without spare-part relationships.
`049` — Spare Unit/Inventory relationships shall remain optional and governed by Inventory authority.
`050` — Missing spare context shall not make the Local Task invalid.
`051` — Later spare linkage shall preserve Local Task identity.
`052` — A Local Task may exist without a planned interval.
`053` — Missing planned interval shall mean unscheduled rather than invalid.
`054` — SOMA shall not fabricate a Local Task planned interval merely because the Task exists.
`055` — Current clock shall not substitute for missing Local Task plan.
`056` — RFC/WFM source chronology shall not substitute for missing Local Task plan.
`057` — A Local Task may exist without actual execution chronology.
`058` — Missing actual execution chronology before work occurs shall be valid.
`059` — Planned interval shall not fabricate actual execution interval.
`060` — Objective interval shall not fabricate actual execution interval.
`061` — Actual execution chronology shall remain owned by Task lifecycle.
`062` — Notes shall be optional for Local Tasks.
`063` — Missing Notes shall not make a Local Task invalid.
`064` — Notes shall remain separate from Task identity and lifecycle authority.
`065` — A Local Task may remain outside any Objective.
`066` — Objective membership shall therefore be optional.
`067` — An unscheduled Local Task may remain valid indefinitely until separately planned, completed, cancelled, corrected, or otherwise transitioned under Task lifecycle.
`068` — Lack of Objective membership shall not imply cancellation.
`069` — Lack of Objective membership shall not imply Draft status if the Local Task itself is already accepted.
`070` — A Local Task shall belong to at most one accepted Objective at one time.
`071` — Local Task Objective cardinality shall be `0..1`.
`072` — A second simultaneous accepted Objective membership shall be invalid.
`073` — Moving a Local Task between Objectives shall use the governed regrouping/membership workflow.
`074` — Reassignment shall preserve Local Task identity.
`075` — Accepted execution/history may lock or restrict reassignment under `0158` and `0162`.
`076` — A Local Task may be created from within an Objective planning workflow.
`077` — Such creation shall still create a first-class Local Task identity.
`078` — Objective-created Local Task shall not become an Objective-owned anonymous row.
`079` — Creating the Task inside an Objective may initialize its Objective membership to that Objective.
`080` — Initial membership shall remain subject to ordinary accepted membership validation.
`081` — When created inside an Objective, a Local Task may initialize its planned start from the current Objective envelope start.
`082` — When created inside an Objective, a Local Task may initialize its planned end from the current Objective envelope end.
`083` — Such initialization shall be an explicit creation convenience rather than permanent authority coupling.
`084` — Initialization shall not imply the Task necessarily consumes the full Objective window operationally.
`085` — The initialized interval shall be stored/treated as a Local Task planning fact once accepted.
`086` — After initialization and acceptance, the Local Task planned interval shall be independently reviewable.
`087` — Changing the Objective envelope shall not silently overwrite an already accepted Local Task plan.
`088` — Changing another member Task shall not silently overwrite the Local Task plan.
`089` — Changing the Local Task plan shall be a governed Task scheduling mutation.
`090` — Local Task planned start and end shall remain distinct from Objective envelope boundaries.
`091` — Objective envelope shall remain derived from accepted eligible member Task intervals.
`092` — An Objective shall not own member Task planned intervals as aliases.
`093` — The Objective envelope start shall continue to derive from the earliest eligible accepted member Task start.
`094` — The Objective envelope end shall continue to derive from the latest eligible accepted member Task end.
`095` — A later accepted Local Task plan adjustment may therefore change the derived Objective envelope or grouping proposal where applicable.
`096` — Any resulting Objective regrouping shall remain separately reviewed under `0158`.
`097` — Task plan may initialize from Objective envelope during creation without making future Objective envelope authority circular.
`098` — After creation, Objective envelope recalculation shall use accepted Task plans as inputs.
`099` — Recomputed Objective envelope shall not then overwrite those Task plans merely because they contributed to it.
`100` — The data model shall avoid a permanent two-way synchronization rule between Local Task plans and Objective envelope.
`101` — Adding or removing optional RFC/SR/Device/spare/Notes context shall preserve Local Task immutable identity.
`102` — Scheduling or unscheduling an eligible Local Task shall preserve Local Task immutable identity and prior scheduling history as governed.
`103` — Local Task corrections shall preserve accepted lifecycle/history rather than delete/recreate the Task once retention rules apply.
`104` — Tests shall prove first-class immutable Local Task identity, absence of fabricated WFM identity, Task-Name-only universal descriptive requirement, optional operational relationships, valid unscheduled state, at-most-one Objective membership, Objective-based initial plan convenience, subsequent independent Task planning, and Objective envelope derivation from accepted member Task intervals without circular synchronization.

## `BETA-REQ-0167` — prefix `TASK-OUTCOME`

**Governing obligation:** Task planning/execution/outcomes are independent per Task; Objective start does not start members, cancellation chronology is dual, mixed outcomes aggregate truthfully and whole-Objective cancellation is pre-execution only.

`001` — Every Task planned interval shall remain planning evidence.
`002` — Task planned start shall remain distinct from actual execution start.
`003` — Task planned end shall remain distinct from actual execution end.
`004` — Planned chronology shall not itself prove execution.
`005` — Passing planned start shall not itself start the Task.
`006` — Passing planned end shall not itself complete the Task.
`007` — A Task may execute outside its planned interval without rewriting the accepted plan.
`008` — A Task may remain unexecuted despite having a valid plan.
`009` — Actual Task execution chronology shall derive from accepted execution evidence.
`010` — Actual execution start shall not be copied automatically from planned start.
`011` — Actual execution end shall not be copied automatically from planned end.
`012` — Actual execution chronology shall not be copied from the Objective envelope.
`013` — Actual execution chronology shall remain independently reviewable from planning chronology.
`014` — Actual and planned intervals may legitimately differ.
`015` — Correcting actual chronology shall preserve prior accepted evidence/history.
`016` — Objective envelope shall remain distinct from every member Task actual interval.
`017` — Objective start shall not become every Task's actual start.
`018` — Objective end shall not become every Task's actual end.
`019` — A Task may begin after its Objective begins.
`020` — A Task may finish before its Objective finishes.
`021` — Multiple Tasks inside one Objective may have different actual execution intervals.
`022` — Every Task shall support its own reviewed outcome.
`023` — One Task's outcome shall not automatically become another Task's outcome.
`024` — Objective aggregate outcome shall not overwrite individual Task outcomes.
`025` — Local Tasks and WFMs shall use the governing Task outcome semantics where applicable.
`026` — Provider WFM status shall remain distinct from SOMA-reviewed Task outcome.
`027` — Task outcome shall derive from accepted Task lifecycle evidence rather than display aggregation.
`028` — `Completed` shall be a supported reviewed Task outcome.
`029` — Completed shall indicate the Task is accepted as successfully completed under its lifecycle contract.
`030` — Completed shall not be inferred merely because planned end passed.
`031` — Completed shall not be inferred merely because the Objective ended.
`032` — One completed Task shall not mark every Objective member Completed.
`033` — `Incomplete` shall be a supported reviewed Task outcome.
`034` — Incomplete shall remain distinct from Completed.
`035` — Incomplete shall remain distinct from Cancelled without execution.
`036` — Incomplete may preserve execution history where work began but did not reach the accepted completion condition.
`037` — Incomplete shall not erase actual execution evidence.
`038` — An incomplete Task shall not be represented as never attempted when execution evidence exists.
`039` — `Cancelled without execution` shall be a supported reviewed Task outcome.
`040` — Cancelled without execution shall require that the Task did not begin execution.
`041` — Accepted Task-start evidence shall make `Cancelled without execution` inapplicable to that Task unless the start evidence itself is separately corrected.
`042` — Cancellation without execution shall not fabricate Task execution chronology.
`043` — Cancellation without execution shall preserve the Task identity and prior planning evidence.
`044` — Cancelled-without-execution Task history shall remain retained rather than hard-deleted.
`045` — `Awaiting Review` shall be a supported Task review/outcome condition.
`046` — Awaiting Review shall indicate that accepted evidence does not yet support a final reviewed Task outcome.
`047` — Awaiting Review shall remain distinct from Completed.
`048` — Awaiting Review shall remain distinct from Incomplete.
`049` — Awaiting Review shall remain distinct from Cancelled without execution.
`050` — Review shall preserve existing execution, source, and lifecycle evidence.
`051` — Entering Awaiting Review shall not fabricate an execution outcome.
`052` — Task outcome review shall identify the exact Task.
`053` — Outcome review shall expose applicable plan and execution chronology.
`054` — Outcome review shall expose prior accepted Task lifecycle evidence where material.
`055` — Review shall not infer outcome solely from another Task's state.
`056` — Review shall not infer outcome solely from Objective aggregate state.
`057` — Outcome correction shall be history-preserving.
`058` — Starting an Objective shall create no automatic Task-start evidence for every member.
`059` — Objective start time shall not be copied into each member Task's actual start.
`060` — Each Task shall require its own accepted start evidence to be considered started.
`061` — A Task may remain unstarted while its Objective is In Progress.
`062` — Multiple Tasks may begin at different times within one Objective.
`063` — A Task may never begin even though other members execute.
`064` — Objective start shall not eliminate the distinction between started and unstarted Tasks.
`065` — Accepted Task start shall belong to that exact Task identity.
`066` — Starting Task A shall not start Task B.
`067` — Starting a WFM shall not start a Local Task merely because they share an Objective.
`068` — Starting a Local Task shall not start a WFM merely because they share an Objective.
`069` — Starting a Task shall not imply every Task in the same work-package partition started.
`070` — An individual Task that has not begun execution may be marked Cancelled without execution while its Objective is In Progress.
`071` — Such cancellation shall not stop unrelated executing Tasks.
`072` — Such cancellation shall not automatically cancel the Objective.
`073` — Such cancellation shall not automatically cancel sibling Tasks.
`074` — The Objective may continue while other executable Tasks remain.
`075` — The changed executable membership shall trigger the applicable warning behavior under `0162`.
`076` — An individual Task that never began execution may be reviewed and marked Cancelled without execution after the operational activity.
`077` — Late recording shall not fabricate Task execution.
`078` — Late recording shall not require the cancellation effective time to equal recording time.
`079` — Late review shall preserve the Objective's already accepted execution history.
`080` — Cancelling one unstarted Task after the activity shall not rewrite outcomes of Tasks that executed.
`081` — Task cancellation shall preserve recording time separately from effective cancellation time.
`082` — Recording time shall represent when SOMA accepted/recorded the cancellation fact.
`083` — Effective cancellation time shall represent when the cancellation is accepted as operationally taking effect.
`084` — Effective cancellation time may differ from recording time.
`085` — Recording time shall not silently overwrite effective cancellation chronology.
`086` — Effective cancellation time shall not rewrite the audit recording time.
`087` — Both chronologies shall follow the canonical temporal contract.
`088` — Any correction to either chronology shall preserve the prior accepted history.
`089` — SOMA shall not infer effective cancellation time solely from the time the review screen is opened.
`090` — SOMA shall not substitute Objective end as effective cancellation time without accepted evidence.
`091` — SOMA shall not substitute planned Task end as effective cancellation time without accepted evidence.
`092` — SOMA shall not rewrite prior source chronology to represent local cancellation.
`093` — Objective aggregate outcome shall be derived from accepted member Task outcomes and applicable Objective lifecycle evidence.
`094` — Objective aggregate shall not erase individual Task outcomes.
`095` — Objective aggregate shall remain a projection rather than Task-outcome authority.
`096` — Aggregate reporting shall remain explainable by member Tasks.
`097` — An Objective containing materially different Task outcomes shall expose a mixed aggregate outcome.
`098` — Completed plus Cancelled-without-execution Tasks shall not be presented as though every Task Completed.
`099` — Completed plus Incomplete Tasks shall expose mixed outcome.
`100` — Completed plus Cancelled plus Incomplete Tasks shall expose mixed outcome.
`101` — Awaiting-Review member Tasks shall remain visible as unresolved review state rather than being hidden by a clean aggregate label.
`102` — Mixed aggregate presentation shall expose enough detail to understand constituent outcomes.
`103` — Mixed shall not overwrite member Task lifecycle records.
`104` — An Objective may be marked Cancelled only when no member Task began execution.
`105` — Accepted start evidence for any member Task shall make whole-Objective cancellation unavailable under this rule.
`106` — Accepted execution evidence for any member Task shall make whole-Objective cancellation unavailable under this rule.
`107` — A completed member Task shall make whole-Objective cancellation unavailable.
`108` — An incomplete member Task with execution history shall make whole-Objective cancellation unavailable.
`109` — The Objective shall not be represented as wholly Cancelled merely because every remaining unstarted Task was later cancelled.
`110` — Whole-Objective cancellation may remain available while zero member Tasks have accepted start evidence.
`111` — Objective cancellation shall not fabricate Task execution.
`112` — Applicable unstarted member Tasks shall retain their own governed cancellation/history treatment.
`113` — Objective cancellation decision shall preserve Objective identity and prior planning history.
`114` — Cancellation shall not hard-delete the Objective or Tasks.
`115` — Once any member Task has begun, later inability to continue shall be represented through Task outcomes and Objective review/aggregate semantics rather than whole-Objective cancellation.
`116` — An In-Progress Objective losing all executable work shall follow `Incomplete — Awaiting Review` under `0162`.
`117` — An Objective with a mixture of completed/cancelled/incomplete work shall preserve that mixed history.
`118` — Later correction shall append history rather than rewrite the Objective as though execution never began.
`119` — Tests shall prove planned versus actual Task intervals remain distinct, Objective start does not start every member Task, each Task independently supports Completed, Incomplete, Cancelled without execution, and Awaiting Review, and an unstarted Task can be cancelled during or after an activity while other Tasks execute.
`120` — Tests shall prove cancellation recording/effective times remain distinct, heterogeneous member outcomes yield a truthful mixed Objective aggregate, and whole-Objective Cancelled is permitted only when no member Task has accepted start/execution evidence.

## `BETA-REQ-0168` — prefix `OBJ-IDTZ`

**Governing obligation:** Objective identity/tracking is stable; monthly ordinal is derived; selectable IANA scheduling timezone is Objective/Task-scoped and cannot rewrite canonical or other-domain chronology.

`001` — Every accepted Objective shall have an immutable internal identity.
`002` — Objective internal identity shall be unique.
`003` — Objective internal identity shall not derive from Objective title.
`004` — Objective internal identity shall not derive from planned start.
`005` — Objective internal identity shall not derive from actual start.
`006` — Objective internal identity shall not derive from monthly ordinal.
`007` — Objective internal identity shall not derive from calendar month.
`008` — Rescheduling an Objective shall not change its internal identity.
`009` — Correcting Objective chronology shall not change its internal identity.
`010` — Regrouping eligible Tasks shall not change Objective identity except where the separately governed regrouping operation explicitly creates or retires Objective entities.
`011` — Every accepted Objective shall have a stable operator-visible tracking identifier.
`012` — The tracking identifier shall remain associated with the same Objective identity.
`013` — The tracking identifier shall not change merely because chronology changes.
`014` — The tracking identifier shall not change merely because monthly ordinal changes.
`015` — The tracking identifier shall not change merely because Objective membership changes.
`016` — The tracking identifier shall not change merely because Objective outcome changes.
`017` — The tracking identifier shall not be reused for a different Objective.
`018` — Hard-deleted/discarded UI working-state identifiers shall not authorize reuse of a tracking identifier that was already assigned to an accepted Objective.
`019` — Tracking identifier generation shall remain deterministic enough to prevent accidental reuse while exact format remains governed by the design contract.
`020` — Operator-visible tracking identity shall remain distinct from the monthly chronological ordinal.
`021` — Monthly ordinal shall not be persisted as Objective identity authority.
`022` — Monthly ordinal shall not become a foreign-key target.
`023` — Monthly ordinal shall not be used to determine Objective equality.
`024` — Monthly ordinal shall not be used to merge Objective identities.
`025` — Changing ordinal shall not require rewriting relationships.
`026` — UI shall not imply that an ordinal change means a different Objective replaced the prior one.
`027` — SOMA may display a monthly chronological ordinal for an Objective.
`028` — The ordinal shall remain derived presentation only.
`029` — The ordinal shall be recomputable from accepted Objective chronology and immutable identity.
`030` — The ordinal shall not create lifecycle evidence.
`031` — The ordinal shall not create scheduling authority.
`032` — The ordinal shall not create report identity.
`033` — Effective start for ordinal calculation shall use accepted actual Objective start when known.
`034` — Accepted actual start shall take precedence over planned start for ordinal calculation.
`035` — Actual start shall derive from accepted Objective lifecycle evidence.
`036` — Current wall clock shall not fabricate actual start.
`037` — Passing planned start shall not convert planned start into actual start.
`038` — When no accepted actual start exists, effective start shall use the accepted planned Objective start.
`039` — Planned start shall remain the ordinal fallback only while actual start is unknown.
`040` — Later accepted actual start may therefore change the live effective-start ordering.
`041` — Such recalculation shall not rewrite Objective identity.
`042` — An Objective with neither usable actual nor planned start shall have no monthly chronological ordinal.
`043` — Objective creation time shall not substitute for missing effective start.
`044` — Database insertion time shall not substitute for missing effective start.
`045` — Audit recording time shall not substitute for missing effective start.
`046` — Last-update time shall not substitute for missing effective start.
`047` — An unscheduled Objective shall remain fully identifiable through its immutable/internal and tracking identifiers without an ordinal.
`048` — Monthly ordinal shall be calculated within the calendar month containing the Objective's effective start in the selected Objective scheduling timezone.
`049` — A cross-month Objective shall belong to exactly one monthly ordinal bucket.
`050` — The Objective shall belong to the month of its effective start.
`051` — Objective end month shall not determine the ordinal bucket.
`052` — Objective duration crossing midnight shall not create another ordinal.
`053` — Objective duration crossing a month boundary shall not split Objective identity.
`054` — Objective duration crossing a year boundary shall still belong to the year/month containing its effective start.
`055` — Within one monthly bucket, Objectives shall order by effective start instant.
`056` — Earlier effective start shall receive the earlier ordinal position.
`057` — Later effective start shall receive the later ordinal position.
`058` — Ordering shall use canonical instants after applying the selected presentation/grouping timezone semantics.
`059` — Display-string lexical order shall not determine chronology.
`060` — Objectives with equal effective-start instants shall resolve ordinal ties deterministically.
`061` — Tie resolution shall use immutable Objective identity.
`062` — Tie resolution shall not depend on query row order.
`063` — Tie resolution shall not depend on database insertion order unless that order is itself the immutable identity contract.
`064` — Tie resolution shall not depend on visible Objective name.
`065` — Equivalent authoritative input shall yield equivalent ordinal ordering.
`066` — Current monthly ordinals may recalculate when relevant effective-start facts change.
`067` — Accepting an actual start may recalculate current ordinal positions.
`068` — Correcting planned start before execution may recalculate current ordinal positions.
`069` — Correcting accepted actual start through governed history-preserving correction may recalculate current presentation.
`070` — Introducing/removing another current Objective from the same effective-start ordering scope may recalculate current ordinals where lifecycle rules permit current presentation changes.
`071` — Ordinal recalculation shall not mutate Objective tracking identifiers.
`072` — Ordinal recalculation shall not mutate immutable Objective identities.
`073` — Objective↔Task relationships shall use immutable Objective identity rather than ordinal.
`074` — Audit shall use immutable Objective identity rather than depending on current ordinal.
`075` — Reports shall retain sufficient identity to distinguish frozen labels from current ordinals.
`076` — Communication or Inventory references shall not become ambiguous because an Objective ordinal changed.
`077` — Ordinal recalculation shall not rewrite Task execution chronology.
`078` — A historical report shall preserve the Objective label rendered or captured under that report's accepted snapshot semantics.
`079` — A later live ordinal recalculation shall not rewrite an immutable historical report label.
`080` — A later actual-start correction shall not rewrite an already immutable report label.
`081` — A later timezone-setting change shall not rewrite an already immutable report label.
`082` — Historical report label and current live ordinal may legitimately differ.
`083` — Reports shall preserve Objective immutable/tracking identity sufficient to explain that both labels refer to the same Objective.
`084` — Report snapshot persistence strategy remains governed by the separate reporting design decision rather than being invented here.
`085` — SOMA shall allow the operator to select a supported IANA timezone for Objective/Task scheduling presentation.
`086` — The selected value shall use an IANA timezone identity rather than an ambiguous abbreviation such as `EST`.
`087` — Supported timezone validation shall reject unknown/unsupported identifiers.
`088` — `Pacific/Galapagos` shall be supportable.
`089` — Exact complete supported-zone policy may follow the platform/LLD contract provided required approved zones remain available.
`090` — The selected Objective timezone shall govern Objective scheduling presentation.
`091` — The selected Objective timezone shall govern Task scheduling presentation.
`092` — The selected Objective timezone shall govern calendar grouping for Objectives/Tasks.
`093` — The selected Objective timezone shall govern Maintenance Window presentation.
`094` — Monthly ordinal month-boundary calculation shall use the selected Objective scheduling timezone.
`095` — Calendar-day/month labels for Objective scheduling shall use the selected timezone.
`096` — Selecting another Objective scheduling timezone shall not change canonical stored UTC instants.
`097` — Changing the timezone shall re-render applicable local scheduling presentation rather than rewriting underlying instants.
`098` — The same canonical Objective instant may display a different local date/time under another selected timezone.
`099` — Such display change shall not constitute schedule mutation when the canonical instant itself did not change.
`100` — Changing Objective scheduling timezone may change which local calendar day contains an Objective.
`101` — Changing Objective scheduling timezone may change which local calendar month contains an effective start.
`102` — Current derived monthly ordinals may therefore recalculate under the newly selected scheduling timezone.
`103` — Such recalculation shall remain presentation-derived and shall not change Objective identity.
`104` — Historical frozen report labels shall not be rewritten by later timezone selection.
`105` — Objective scheduling timezone selection shall not reinterpret RFC provider-source chronology.
`106` — Objective scheduling timezone selection shall not reinterpret WFM source timestamps under their explicit source-time contract.
`107` — Objective scheduling timezone selection shall not reinterpret Communication chronology.
`108` — Objective scheduling timezone selection shall not reinterpret audit recording timestamps.
`109` — Objective scheduling timezone selection shall not reinterpret import observation chronology.
`110` — Objective scheduling timezone selection shall not reinterpret Inventory/warehouse occurrence chronology.
`111` — Objective scheduling timezone selection shall not reinterpret SLA canonical event chronology.
`112` — Domain-specific chronology shall continue to follow its owning authority even when displayed elsewhere in Objective context.
`113` — Tests shall prove immutable Objective identity and non-reusable operator tracking identity survive rescheduling, regrouping, ordinal changes, and timezone changes.
`114` — Tests shall prove ordinal ordering uses actual start when known, otherwise planned start; unscheduled Objectives receive none; cross-month Objectives belong to their effective-start month; and equal starts resolve deterministically by immutable identity.
`115` — Tests shall prove current ordinals may recalculate without rewriting frozen historical report labels or Objective identity.
`116` — Tests shall prove supported IANA timezone selection including `Pacific/Galapagos` changes only Objective/Task scheduling, calendar grouping, Maintenance Window presentation, and derived current ordinals while canonical instants and unrelated domain chronology remain unchanged.

## `BETA-REQ-0169` — prefix `BOUND-SCALE`

**Governing obligation:** Bounds are semantic and field-specific, overflow never silently truncates, and scalable rendering/export limits remain technical rather than product cardinality.

`001` — Every bounded SOMA field shall obtain its validation bounds from its owning semantic contract.
`002` — Bounds shall not be inferred merely from a generic database column template.
`003` — Bounds shall not be inherited merely because two fields share a storage type.
`004` — Two textual fields may legitimately have different limits.
`005` — Two collections may legitimately have different limits.
`006` — Structured documents may require different bounds from ordinary scalar text.
`007` — Field-specific bounds shall be explicit in the applicable LLD before implementation acceptance.
`008` — A field contract shall identify the measurement dimensions relevant to that field.
`009` — User-visible text may define a Unicode/grapheme bound where appropriate.
`010` — Persisted or transported text may define a UTF-8 byte bound where appropriate.
`011` — Multi-line text may define a line-count bound where appropriate.
`012` — Multi-line text may define per-line bounds where appropriate.
`013` — Structured data may define nesting-depth bounds where appropriate.
`014` — Lists or collections may define item-count bounds where semantically or technically required.
`015` — Structured collections may define per-container member-count bounds.
`016` — One applicable field may require more than one simultaneous bound.
`017` — Where the contract is defined in user-perceived characters, implementation shall not substitute raw UTF-16 code-unit count.
`018` — Where the contract is defined in grapheme clusters, implementation shall evaluate grapheme clusters consistently.
`019` — A composed user-visible character shall not be arbitrarily treated as multiple permitted visible characters merely because its encoding contains multiple code points.
`020` — Emoji sequences shall be handled according to the field's explicit Unicode measurement contract.
`021` — Unicode normalization used for length validation shall be explicitly defined where material.
`022` — Validation semantics shall remain deterministic for equivalent accepted input.
`023` — A UTF-8 byte bound shall be evaluated against encoded UTF-8 size.
`024` — Character count shall not substitute for an explicit byte-size limit.
`025` — Multi-byte Unicode text may therefore satisfy one character bound while exceeding a byte bound.
`026` — Byte limits shall protect applicable persistence, serialization, export, or resource boundaries without redefining semantic identity.
`027` — Where line count is bounded, newline interpretation shall be explicitly normalized.
`028` — Structured-data depth shall be measured deterministically.
`029` — Structured-data depth limits shall be checked before persistence.
`030` — Collection/cardinality bounds shall be checked before authoritative commit where applicable.
`031` — A nested document shall not bypass depth limits merely because its encoded byte size remains small.
`032` — A shallow document shall not bypass byte limits merely because its nesting depth remains low.
`033` — SOMA Beta shall not impose Alpha's universal 400-character field limit.
`034` — `400` shall not become a default text-field limit merely because no field-specific design has yet been written.
`035` — A new field shall require an appropriate bound decision rather than inheriting 400 automatically.
`036` — Existing Beta fields shall not be silently capped at 400 for implementation convenience.
`037` — Database schema generation shall not impose 400 universally.
`038` — UI components shall not impose 400 universally.
`039` — Import adapters shall not impose 400 universally.
`040` — Export logic shall not assume source values were limited to 400.
`041` — SOMA shall not replace Alpha's universal 400-character rule with another universal text constant.
`042` — A platform-wide `1000`, `4096`, or equivalent arbitrary text limit shall not become semantic authority merely because it is easier to implement.
`043` — Shared technical safeguards may exist only beneath and without contradicting field-specific product bounds.
`044` — A shared implementation safeguard shall be documented as technical protection rather than the semantic field contract.
`045` — Manual entry exceeding an applicable field bound shall be rejected before authoritative commit.
`046` — Manual overflow shall not partially commit.
`047` — Manual overflow shall leave the previous accepted value unchanged.
`048` — A newly created record with an overflowing required field shall not be accepted.
`049` — A manually edited optional field exceeding its bound shall not be silently cleared.
`050` — Manual overflow rejection shall provide actionable feedback.
`051` — Feedback shall identify the affected semantic field.
`052` — Feedback shall identify the relevant bound or correction expectation.
`053` — Where useful, feedback shall identify the observed measurement.
`054` — Feedback shall distinguish character/grapheme overflow from byte overflow where that distinction matters.
`055` — Feedback shall not require the operator to infer the problem from a generic persistence error.
`056` — Accessibility semantics shall expose the validation error programmatically.
`057` — SOMA shall never silently truncate overflowing manual text.
`058` — SOMA shall not truncate a value merely to make persistence succeed.
`059` — SOMA shall not truncate a Task Name silently.
`060` — SOMA shall not truncate Notes silently.
`061` — SOMA shall not truncate Customer or Contact descriptive data silently.
`062` — SOMA shall not truncate identifiers to make malformed identities valid.
`063` — Client-side widgets shall not silently shorten data that the service would otherwise reject.
`064` — Backend persistence shall not silently shorten data that bypassed client validation.
`065` — Imported content exceeding a semantic or technical bound shall not be silently truncated.
`066` — Imported overflow shall produce a governed import validation failure or quarantine result.
`067` — Failure/quarantine scope shall be the smallest safe scope supported by the owning import transaction contract.
`068` — Row-scoped overflow may remain row-scoped when that import supports independent safe row handling.
`069` — Identity-scoped overflow may block that identity without automatically classifying unrelated identities as overflowing.
`070` — Workbook-wide rejection may still occur when the overflow compromises safe parsing, source integrity, or another workbook-wide invariant.
`071` — This requirement shall not invent per-row atomicity where the owning import contract requires a broader transaction.
`072` — An overflowing imported field shall not be shortened and accepted as though the source supplied the shortened value.
`073` — Overflowing identity data shall not create a guessed identity.
`074` — Overflowing schedule data shall not fabricate a Task interval.
`075` — Overflowing Customer data shall not fabricate a Customer reconciliation.
`076` — Overflowing structured payload shall not be replaced by a generic placeholder that acquires domain authority.
`077` — Overflow failures shall preserve sufficient bounded provenance for review and diagnostics.
`078` — Provenance may preserve source/workbook identity.
`079` — Provenance may preserve worksheet/logical matrix location.
`080` — Provenance may preserve physical/logical row identity where safe.
`081` — Provenance may preserve the semantic field that overflowed.
`082` — Provenance may preserve observed measurement and configured bound.
`083` — Provenance may preserve a bounded fingerprint where useful.
`084` — Preserving provenance shall not require retaining the entire oversized payload.
`085` — Diagnostic/audit disclosure restrictions shall continue to apply to overflow provenance.
`086` — A UI list's rendered row count shall not define how many domain entities SOMA supports.
`087` — A table page size shall not define authoritative query cardinality.
`088` — Virtualized lists may contain more entities than are simultaneously rendered.
`089` — Off-screen entities shall remain equally authoritative.
`090` — Rendering fewer rows shall not classify remaining rows as ignored.
`091` — Beta shall not impose a product-wide page size of 100 as semantic authority.
`092` — A UI may use 100 rows in a particular measured implementation only if that remains a presentation detail.
`093` — Changing such a rendering size shall not change query truth.
`094` — Different surfaces may use different measured rendering strategies.
`095` — Beta shall not impose a product-wide maximum of 500 entities merely for list rendering.
`096` — A 501st valid record shall not become unsupported solely because a prior UI assumed a 500-row cap.
`097` — Search, filtering, grouping, and export shall not silently ignore records beyond 500.
`098` — Any actual technical processing limit shall be measured, documented, and owned by the applicable LLD rather than disguised as the number 500 business rule.
`099` — Paginated authoritative lists shall use deterministic ordering.
`100` — Cursor pagination shall use a stable ordering key sufficient to prevent ambiguous page traversal.
`101` — Immutable identity shall provide deterministic tie-breaking where needed.
`102` — Equivalent authoritative state shall produce deterministic traversal order.
`103` — Pagination shall not depend on nondeterministic database row order.
`104` — Pagination shall avoid silently losing or duplicating entries because multiple records share a displayed sort value.
`105` — UI virtualization may be used instead of or alongside pagination where appropriate.
`106` — Virtualization window size shall remain a measured presentation/technical control.
`107` — Virtualization shall not change the authoritative total.
`108` — Virtualization shall not prevent navigation to valid records outside the current rendered window.
`109` — When a UI displays an exact total, that total shall refer to the complete governed result set rather than the current rendered page.
`110` — Pagination shall not report `100 total` merely because 100 rows are currently displayed when more records match.
`111` — Filtering shall recompute totals against the complete filtered result.
`112` — Conflict/warning/error totals shall remain truthful beyond the currently rendered rows.
`113` — A governed full-result export shall not be limited to the currently rendered page.
`114` — UI virtualization shall not truncate an export.
`115` — If a separate explicit `export current page/selection` capability exists, it shall be clearly distinguished from exporting the complete governed result set.
`116` — Tests shall prove field-specific multi-dimensional bounds, rejection rather than silent manual truncation, smallest-safe-scope imported overflow handling with bounded provenance, removal of the universal 400/100/500 assumptions, deterministic scalable list traversal/virtualization, exact totals beyond the visible page, and full-result exports independent of current rendering.
