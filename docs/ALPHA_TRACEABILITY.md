# SOMA Beta Alpha Traceability Register

Status: **All 138 merged Alpha requirements and all 43 unmerged Alpha proposals dispositioned**  
Scope: one row per SOMA Alpha requirement or decision ID. Proposal revisions of an existing ID are recorded in the same row rather than creating duplicate identities.

| Alpha ID | Alpha authority | Alpha statement | Beta disposition | Beta requirement | Beta release | Normative destination | Gap | Rationale |
|---|---|---|---|---|---|---|---|---|
| `ALPHA:PROD-001` | Merged `main` | SOMA shall be a clean Service Operations Management Application, not a rename/history continuation of Zeus. | Replace | `BETA-REQ-0001` | 1.0.0 | Product Contract §2 | — | Beta preserves the clean-lineage intent but broadens the historical boundary to both Zeus and SOMA Alpha and requires explicit evaluation of any reuse. |
| `ALPHA:PROD-002` | Merged `main` | The intended UX shall be a localhost web application backed by Python. | Retain | `BETA-REQ-0002` | 1.0.0 | Product Contract §3; Architecture §§2 and 8 | — | The local Python-backed web application remains the accepted product direction. Responsive behavior, offline operation, frontend technology, and loopback security are governed separately. |
| `ALPHA:PROD-003` | Merged `main` | Operational data shall remain locally usable without a separately administered database server. | Retain | `BETA-REQ-0003` | 1.0.0 | Product Contract §3; Architecture §§4–5 | — | Beta remains locally usable and application-managed; authentication and encryption do not introduce a separately administered database service. |
| `ALPHA:PROD-004` | Merged `main` | Supported product targets are 64-bit Windows 10/11 with Python 3.13 or 3.14. Other OSes are not support targets. | Retain | `BETA-REQ-0004` | 1.0.0 | Architecture §§8–9; Roadmap acceptance matrix | Narrows `O-006` | Beta retains the proven Alpha support targets. Acceptance must cover all four Windows/Python combinations; browser, Windows-edition/build, and packaging details remain assigned to LLD. |
| `ALPHA:PROD-005` | Merged `main` | Source shall remain distributable under Apache License 2.0 with applicable attribution retained. | Replace | `BETA-REQ-0005`, `BETA-REQ-0006` | 1.0.0 | Product Contract §2; Branding Contract; Roadmap licensing boundary | — | Beta is wholly private and proprietary. The project owner confirms exclusive rights in the canonical brand assets and relicenses them separately into Beta. The Alpha repository is also private, but retains its existing Apache-2.0 license; repository visibility does not transfer that license to Beta. |
| `ALPHA:PROD-006` | Merged `main` | Runtime/customer/mail/credential/log/database/backup/export data shall not be committed to source control. | Replace | `BETA-REQ-0007` | 1.0.0 | Product Contract §§11–14; Import Contract §2; Architecture §5; repository security policy | — | Beta retains the source-control exclusion and expands it to cover every operational input, communication artifact, secret, persistence artifact, backup, export, and generated operational artifact handled by the broader Beta scope. |
| `ALPHA:PROD-007` | Merged `main` | SOMA 1 stable shall meet or deliberately replace every approved user-facing Zeus capability through explicit parity disposition. | Replace | `BETA-REQ-0008` | 1.0.0 | Alpha Traceability Register; Requirements Register; Use-Case Catalogue; Roadmap acceptance gates; UI/UX traceability | — | Beta broadens explicit parity beyond Zeus to merged Alpha requirements and decisions and clearly marked unmerged Alpha proposals, while requiring downstream traceability through use cases, design, implementation, and UI/UX. |
| `ALPHA:PROD-008` | Merged `main` | SOMA shall provide repeatable local setup plus explicit run/stop commands; Windows source deployments include direct launchers. | Retain | `BETA-REQ-0009` | 1.0.0 | Architecture deployment model; LLD deployment specification; operator setup guide; acceptance tests | Assigned LLD detail | The repeatable local setup and direct Windows start/stop launcher requirement remains valid for Beta's offline local deployment model. Launcher format, process supervision, clean shutdown, and failure reporting belong in the LLD. |
| `ALPHA:DATA-001` | Merged `main` | Internal SOMA IDs are independent strings; external business identifiers are not relational primary keys. | Replace | `BETA-REQ-0010` | 1.0.0 | Data Model; identifier contract; database LLD; import reconciliation rules | Assigned LLD detail | Beta retains independent internal identity but avoids prematurely requiring one storage representation. Opaque internal IDs protect relationships when imported or locally entered business identifiers are corrected. The identifier format and generation algorithm belong in the LLD. |
| `ALPHA:DATA-002` | Merged `main` | Service Request identity is exactly eight digits. `SR`/`TT` are display contexts only. | Replace | `BETA-REQ-0011` | 1.0.0 | Data Model; identifier contract; SR import contract; SR creation workflow | Assigned LLD detail | Beta retains the canonical eight-digit external SR identity and adds non-reusable incremental `LSR-########` identities for locally created SRs that lack an external identifier. Sequence allocation belongs in the LLD. |
| `ALPHA:DATA-003` | Merged `main` | RFC identity is `NC` plus fourteen digits. | Replace | `BETA-REQ-0012` | 1.0.0 | Data Model; identifier contract; RFC import contract; import-review UI | — | Beta retains the canonical RFC format and adds the confirmed rule that overlength source identifiers represent branch artifacts, do not create or update RFC records, and appear as skipped in the reviewed import result. |
| `ALPHA:DATA-004` | Merged `main` | WFM identity is `TK` plus fourteen digits. | Retain | `BETA-REQ-0013` | 1.0.0 | Data Model; identifier contract; WFM import contract; manual WFM registration workflow | — | The canonical WFM identity remains unchanged. Local Tasks are a distinct Task category and cannot carry or imitate the WFM identifier format. |
| `ALPHA:DATA-005` | Merged `main` | Permanent Spare Request identity is `SR` plus seven digits; pre-assignment tracking ID is twelve digits. | Replace | `BETA-REQ-0014` | 1.0.0 | Inventory Data Model; identifier contract; Spare Request lifecycle; communication reconciliation; UI | Assigned LLD detail | Every persistent Beta Spare Request record is created locally, including one registered after external initiation, receives an immutable temporary tracking identifier, and later acquires the official `SR#######` identifier on the same entity. Beta preserves both references but replaces the unexplained fixed twelve-digit constraint with an explicit collision-safe, chronologically sortable identifier whose concrete format belongs in the LLD. |
| `ALPHA:DATA-006` | Merged `main` | Requested physical spare identity is `C` plus ten digits. | Replace | `BETA-REQ-0015`, `BETA-REQ-0016` | 1.0.0 | Inventory Data Model; identifier contract; receiving workflow; manual Stock registration; reconciliation rules | Assigned LLD detail | Beta distinguishes the C10 RMA obligation from every physical unit, permits separate target, direct inbound, and return-unit relationships, and adds `LSU-########` identity for manually registered units whose official provenance or serial may be absent. Exact persistence and reconciliation mechanics belong in the LLD. |
| `ALPHA:DATA-007` | Merged `main` | Canonical scheduled timestamps use UTC `YYYY-MM-DDTHH:MM:SSZ`; MW boundaries fall on `:00`/`:30`. | Replace | `BETA-REQ-0017` | 1.0.0 | Data Model; scheduling contract; import normalization; Settings; Objective UI; overlap algorithm | Assigned LLD detail | Beta retains canonical UTC persistence but removes the mandatory half-hour boundary because imported and manually reviewed source times must not be silently changed. Time-zone representation, configuration, daylight-saving behavior, and input controls belong in the LLD. |
| `ALPHA:DATA-008` | Merged `main` | Required text PK/FK/enumerated/JSON facts are validated at the DB boundary. | Replace | `BETA-REQ-0018` | 1.0.0 | Data Model; persistence LLD; service transaction contract; import pipeline; validation tests | Assigned LLD detail | Beta retains mandatory persistence invariants but removes the premature text-ID constraint and distinguishes database-enforceable structure from domain-semantic validation performed transactionally before commit. The constraint allocation belongs in the LLD. |
| `ALPHA:DATA-009` | Merged `main` | Referenced history is normally archived/cancelled/superseded/rolled back rather than silently deleted. | Replace | `BETA-REQ-0019` | 1.0.0; general purge policy 1.1.0 | Product Contract; retention contract; audit contract; domain lifecycle specifications; destructive-action UI | Assigned LLD detail | Beta makes preservation the default, narrows hard deletion to eligible unprocessed manual records or explicit domain corrections, and requires reviewed, confirmed, transactional, audited dependent deletion. General purge policy remains deferred to Beta 1.1. |
| `ALPHA:DATA-010` | Merged `main` | Mutable logistics master data shall not rewrite historical delivery/pickup evidence; operations preserve immutable site snapshots where needed. | Retain | `BETA-REQ-0020` | 1.0.0 | Inventory Data Model; Dispatch Location contract; Spare Request lifecycle; Fault Tag workflow; audit/history views | Assigned LLD detail | The immutable-evidence rule remains valid. In Beta terminology, snapshots capture the effective Dispatch Location, including an inherited Site address where applicable. Snapshot structure, capture timing, and current-versus-historical presentation belong in the LLD. |
| `ALPHA:REF-001` | Merged `main` | SOMA shall manage Customer Organizations and Contacts. | Retain | `BETA-REQ-0021` | 1.0.0 | Settings workspace; reference Data Model; Contact management use cases; ticket workbenches | — | Customer Organizations and Contacts remain required Beta reference capabilities. The optional Contact-to-organization relationship is governed separately. |
| `ALPHA:REF-002` | Merged `main` | SOMA shall manage reusable Requesters and one local user profile. | Replace | `BETA-REQ-0022` | 1.0.0 | Product Contract; Settings workspace; identity model; Contact model; authentication LLD; operational selectors | — | Beta retains exactly one authenticating local administrator but represents requesters and other operational people as reusable Contacts fulfilling roles rather than separate person categories or login profiles. |
| `ALPHA:REF-003` | Merged `main` | Dispatch Sites represent reusable logistics locations for Spare Request/Fault Tag workflows. | Replace | `BETA-REQ-0023` | 1.0.0 | Inventory Data Model; Settings workspace; Spare Request workflow; Fault Tag workflow; Glossary | — | Beta renames Dispatch Site to Dispatch Location and defines it as a named, addressed physical logistics location reusable in operation-specific roles without implying Customer Organization ownership or Datacenter Site identity. Spare Request delivery/self-pickup and Fault Tag pickup-origin use remain distinct, and the return warehouse destination is not inferred from the pickup origin. |
| `ALPHA:REF-004` | Merged `main` | Datacenter Sites represent infrastructure locations and remain distinct from Dispatch Sites. | Replace | `BETA-REQ-0024` | 1.0.0 | Infrastructure Data Model; Settings workspace; Site creation workflow; Dispatch Location workflow; address rules | Assigned LLD detail | Beta retains separate Site and logistics entities but requires every Site to receive one dedicated Dispatch Location automatically. Standalone Dispatch Locations remain valid, linked locations inherit the Site address, and one Dispatch Location cannot represent multiple Sites. Transaction and synchronization mechanics belong in the LLD. |
| `ALPHA:REF-005` | Merged `main` | Reference records with operational history support archival. | Retain | `BETA-REQ-0025` | 1.0.0 | Reference Data Model; Settings workspace; historical views; selectors; retention contract | Assigned LLD detail | Archival remains the correct mechanism for referenced master data. Archived records remain visible in historical evidence and unavailable to new active relationships unless reactivated. Control and filtering details belong in the LLD. |
| `ALPHA:REF-006` | Merged `main` | Customer Organization↔Dispatch Site is many-to-many; at most one preferred site per customer. | Replace | `BETA-REQ-0026` | 1.0.0 | Reference Data Model; Dispatch Location contract; Settings workspace; logistics selectors; migration/reuse review | — | Beta removes direct Customer Organization assignment and preference from Dispatch Locations. A location has one physical identity regardless of customer; any customer context is derived through its optional linked Site without redefining or restricting the location. |
| `ALPHA:REF-007` | Merged `main` | Every Datacenter Site belongs to exactly one Customer Organization; Managed Elements may derive customer ownership through the site. | Replace | `BETA-REQ-0027` | 1.0.0 | Infrastructure Data Model; Site and Cloud contracts; Device regularization contract; Settings; Ticket and Inventory relationships | Assigned LLD detail | Beta preserves exclusive Site ownership but corrects the hierarchy: a Site is the physical Datacenter; reusable Cloud Types are instantiated as Site-bound Cloud Deployments. Site labels may repeat across customers, regularized Network Elements derive ownership through Site, and provisional/external Devices may remain unplaced. Duplicate-location review and regularization mechanics belong in the LLD. |
| `ALPHA:REF-008` | Merged `main` | Customer Contact relational identity uses internal SOMA ID. Names are descriptive/may collide. Normalized email is scoped matching data, not a global primary identity. | Replace | `BETA-REQ-0028` | 1.0.0 | Contact Data Model; Settings workspace; import reconciliation; operational selectors; reviewed-merge UI | Assigned LLD detail | Beta extends independent identity and ambiguity protection from customer-bound contacts to every Contact, including unbound operational people. Normalization, matching scopes, and duplicate-review presentation belong in the LLD. |
| `ALPHA:REF-009` | Merged `main` | Organization-bound Contact identity preserves the organization under which SOMA knew the person. Moving organization creates a new contact row. If an SR selects an organization-bound contact, SR organization must exist/match it. | Replace | `BETA-REQ-0029` | 1.0.0 | Contact Data Model; affiliation history; SR workbench; import reconciliation; audit contract | Assigned LLD detail | Beta preserves one known person's identity across an explicitly confirmed affiliation change through historical affiliation records instead of duplicating the Contact. Equal names never establish identity; organization mismatches require reviewed correction or confirmation without silent reassignment, duplication, or import rejection. Affiliation interval and mismatch-review mechanics belong in the LLD. |
| `ALPHA:REF-010` | Merged `main` | Advanced Search reference matching uses deterministic exact keys produced by Unicode NFKC compatibility normalization, collapse of surrounding/internal Unicode whitespace, and Unicode case folding. It does not strip accents or punctuation and does not use fuzzy similarity. If normalization maps multiple active records in the same accepted scope to one key, the result is ambiguous and SOMA must not guess. | Retain | `BETA-REQ-0030` | 1.0.0 | Import Contract; reconciliation service; Contact matching; Customer Organization matching; test vectors | Assigned LLD detail | The deterministic, dependency-light normalization and ambiguity rule remains valid. An exact normalized Contact name produces a candidate rather than identity proof. Implementation and cross-version fixtures belong in the LLD. |
| `ALPHA:REF-011` | Merged `main` | New Contacts require one active Customer Organization. A Foundation-era unbound Contact may be bound once; a bound Contact cannot move or unbind. Archived master records cannot receive new active operational references, and archive/reactivation operations must not strand active dependents. | Replace | `BETA-REQ-0031` | 1.0.0 | Contact lifecycle; reference-data lifecycle; Settings workspace; selectors; dependency validation; audit contract | Assigned LLD detail | Beta permits bound or unbound Contacts and uses reviewed, historically preserved affiliation changes. It retains the prohibition on new active references to archived master data and blocks archival that would strand active dependents. Dependency checks and transaction/UI mechanics belong in the LLD. |
| `ALPHA:REF-012` | Merged `main` | Requesters are reusable descriptive records. Archived Requesters cannot remain pinned, and a Requester cannot be archived while a nonarchived nonterminal Spare Request uses it. Terminal history may retain the reference. | Replace | `BETA-REQ-0032` | 1.0.0 | Spare Request Data Model; Contact role model; requester selector; archival validation; historical views | Assigned LLD detail | Beta removes the separate Requester entity and uses a reusable Contact in the Requester role. Active nonterminal Spare Requests block Contact archival, while terminal history retains the Contact and effective requester evidence. Terminal-state and snapshot details belong in the LLD. |
| `ALPHA:REF-013` | Merged `main` | Datacenter Site ownership is stable. Ordinary edits cannot transfer it; an explicitly reasoned correction may change an erroneous initial Customer Organization only before direct site or Managed Element operational use/evidence. Archival cannot strand active Managed Elements or unfinished active Maintenance Windows. | Replace | `BETA-REQ-0033` | 1.0.0 | Infrastructure lifecycle; Settings workspace; ownership correction workflow; dependency validation; Inventory integration; audit contract | Assigned LLD detail | Beta retains stable Site ownership and pre-use audited correction while expanding operational-use and archival protection to Cloud Deployments and every active Infrastructure, Ticket, Objective, and Inventory dependency. Detection, graph traversal, and preview mechanics belong in the LLD. |
| `ALPHA:REF-014` | Merged `main` | Dispatch Site Customer associations express optional availability/convenience rather than ownership. Preference is at most one per Customer, new active work cannot select archived sites, and a preferred or live-operation site cannot be archived until the blocking state is resolved. | Replace | `BETA-REQ-0034` | 1.0.0 | Dispatch Location lifecycle; Inventory workflows; Settings workspace; dependency validation; audit/history views | Assigned LLD detail | Beta removes customer availability and preference relationships. It preserves archive protection through active Site and logistics dependencies, prevents independent archival of a Site-dedicated location, and retains immutable historical location evidence. Terminal-state and coordinated-transaction details belong in the LLD. |
| `ALPHA:REF-015` | Merged `main` | Completing the singleton Local User Profile is one-way. Public profile/policy edits preserve protected-key metadata; username is never key material. Concrete DPAPI key creation/recovery and verified finite backup rotation remain adapter work. | Replace | `BETA-REQ-0035`, `BETA-REQ-0036`, `BETA-REQ-0037` | 1.0.0; operational at the Phase 4 security-and-recovery gate | Identity contract; Security LLD; Settings; authentication workflow; persistence contract; backup/recovery contract; acceptance tests | Assigned LLD detail | Beta separates authentication entirely from live-data and backup encryption. The password is authentication material only; live data uses an independent random key protected through Windows facilities, and portable backups use independent authenticated encryption plus a high-entropy recovery secret. Algorithms, APIs, packaging, rotation, and recovery mechanics belong in the accepted Security LLD. |
| `ALPHA:OPS-001` | Merged `main` | Service Requests are pivotal when present, but RFCs/MWs/Spare Requests/other operational records may exist independently. | Replace | `BETA-REQ-0038` | 1.0.0 | Operational relationship contract; Ticket model; Objective model; Inventory lifecycle; validation matrix | Assigned LLD detail | Beta retains SR as the pivotal connector without making it universal, while explicitly distinguishing independent entities from WFMs, Spare Needs, and official Spare Requests/RMAs that require their confirmed parent relationships. Cross-domain constraint and error mechanics belong in the LLD. |
| `ALPHA:OPS-002` | Merged `main` | SR↔RFC is many-to-many; a direct SR link may target only a master RFC. | Retain | `BETA-REQ-0039` | 1.0.0 | Ticket Data Model; SR workbench RFC tab; RFC workbench SR tab; import reconciliation; relationship selectors | Assigned LLD detail | The many-to-many, master-only direct relationship remains valid. Subordinate RFCs inherit SR context and visibility through their master. Query and reviewed transaction mechanics belong in the LLD. |
| `ALPHA:OPS-003` | Merged `main` | RFC hierarchy is exactly master/subordinate; subordinate cannot own subordinates. | Retain | `BETA-REQ-0040` | 1.0.0 | Ticket Data Model; RFC workbench subordinate tab; import reconciliation; hierarchy selectors; validation tests | Assigned LLD detail | Beta retains a strict two-level RFC hierarchy: one master may own many subordinates, each subordinate belongs to one master, and no subordinate may own another. Reassignment and protected-history mechanics belong in the LLD. |
| `ALPHA:OPS-004` | Merged `main` | Each WFM belongs to exactly one RFC and zero/one MW. | Retain | `BETA-REQ-0041` | 1.0.0 | Task Data Model; RFC workbench; Objective grouping service; WFM import reconciliation; reassignment workflow | Assigned LLD detail | A WFM retains exactly one RFC parent and at most one Objective membership. Unscheduled staging/review is valid; regrouping moves rather than duplicates membership. Atomic reassignment and import-conflict handling belong in the LLD. |
| `ALPHA:OPS-005` | Merged `main` | Within one MW, an RFC has at most one WFM; retry uses a new WFM ticket. | Retain | `BETA-REQ-0042` | 1.0.0 | Task Data Model; Objective validation; RFC workbench; retry workflow; import conflict review | Assigned LLD detail | Beta retains one WFM per RFC within an Objective and requires a distinct WFM identity for a retry. Master and subordinate RFCs remain separate for this constraint; local Tasks are not restricted by it. Constraint and supersession mechanics belong in the LLD. |
| `ALPHA:OPS-006` | Merged `main` | MW is first-class and may exist without SR/RFC/WFM/Spare Request/Managed Element. | Replace | `BETA-REQ-0043` | 1.0.0 | Objective Data Model; creation workflow; draft handling; Task grouping service; validation tests | Assigned LLD detail | Beta retains Objective as first-class and independent of SR/RFC/WFM/Inventory/Infrastructure relationships but requires one accepted timeframe and at least one Task before persistence. Draft and atomic creation mechanics belong in the LLD. |
| `ALPHA:OPS-007` | Merged `main` | Explicit SR/MW association is stored only when deliberately created; reachable SR context through WFM/RFC is derived. | Replace | `BETA-REQ-0044` | 1.0.0 | Objective Data Model; relationship query model; Objective workbench; Task editor; provenance UI | Assigned LLD detail | Beta removes direct Objective-to-SR links. SR context is derived exclusively through constituent Tasks: directly for local Tasks and through RFC/master-RFC relationships for WFMs. Query and reviewed Task-link editing belong in the LLD. |
| `ALPHA:OPS-008` | Merged `main` | One SR may have unlimited terminal MW history but at most one unfinished MW. | Replace | `BETA-REQ-0045` | 1.0.0 | Objective grouping contract; SR workbench; scheduling service; operational history; validation tests | Assigned LLD detail | Beta removes the per-SR unfinished-Objective cap. One SR may participate through Tasks in multiple non-overlapping unfinished Objectives; overlapping Task windows are consolidated by the global grouping rule. Boundary, regrouping, and concurrency mechanics belong in the LLD. |
| `ALPHA:OPS-009` | Merged `main` | MW retry is a new MW superseding one prior attempt; one attempt has at most one direct successor. | Replace | `BETA-REQ-0046` | 1.0.0 | Task lifecycle; retry workflow; Objective grouping; operational history; lineage UI | Assigned LLD detail | Beta moves retry lineage to individual Task attempts so only failed/incomplete work is retried and the normal overlap algorithm may group retries from different prior Objectives. Local retries clone reviewed relationships into a new Task; WFM retries require a new TK identity. Clone allowlists and transaction mechanics belong in the LLD. |
| `ALPHA:OPS-010` | Merged `main` | Scheduled MW start is canonical half-hour UTC; end is later than start. | Replace | `BETA-REQ-0047` | 1.0.0 | Objective Data Model; Task scheduling; import normalization; overlap algorithm; validation tests | Assigned LLD detail | Beta retains canonical UTC chronology but removes the half-hour constraint. Unscheduled Tasks receive no synthetic window and join an Objective only after an accepted timeframe exists. Duration, parsing, and error details belong in the LLD. |
| `ALPHA:OPS-011` | Merged `main` | MW selection begins from the master-RFC WFM, resolves subordinate RFCs, and surfaces missing same-window subordinate WFMs as warnings. | Replace | `BETA-REQ-0048` | 1.0.0 | Objective grouping service; RFC workbench; WFM import workflow; Objective creation flow; warning UI | Assigned LLD detail | Beta permits any eligible scheduled local Task or WFM to create or join an Objective through overlap grouping. RFC hierarchy context and colliding Tasks are resolved regardless of entry point; missing subordinate WFMs warn without blocking or fabrication. Traversal and warning mechanics belong in the LLD. |
| `ALPHA:OPS-012` | Merged `main` | SRs/WFMs/MWs link directly to affected/intervened Managed Elements without requiring a spare. | Replace | `BETA-REQ-0049` | 1.0.0 | Device-reference model; SR workbench; Task editor; Objective derived views; Infrastructure promotion workflow; Inventory integration | Assigned LLD detail | Beta retains direct affected-Device relationships for SRs and Tasks without requiring a spare, permits registered or provisional/external Device References, and derives Objective Device context exclusively from Tasks. Union/provenance and reviewed promotion mechanics belong in the LLD. |
| `ALPHA:OPS-013` | Merged `main` | Due completed/incomplete MW attempts are surfaced for operator review; incomplete attempts count as awaiting review. | Retain | `BETA-REQ-0050` | 1.0.0 | Objective lifecycle; review queue; Overview metrics; operational narrative; retry workflow | Assigned LLD detail | Beta retains due execution review and awaiting-review treatment for incomplete Objectives while preserving underlying Task outcomes, evidence, notes, and retry decisions. Due calculation and aggregation/state details belong in the LLD. |
| `ALPHA:OPS-014` | Merged `main` | Users create/edit/link/regroup/cancel/archive/retry MWs from MW management, including initially standalone MWs. Hard deletion is not the historical correction mechanism. | Replace | `BETA-REQ-0051` | 1.0.0 | Objectives workspace; Objective lifecycle; Task regrouping; retry workflow; destructive-action UI; audit contract | Assigned LLD detail | Beta preserves Objective management while requiring at least one Task, moving cross-domain relationships and retries to Tasks, and applying the accepted deletion boundary. Action availability, previews, and transactional side effects belong in the LLD. |
| `ALPHA:OPS-015` | Merged `main` | An SR supports multiple independent operational notes with author/time/archive metadata. | Replace | `BETA-REQ-0052` | 1.0.0 | SR and RFC workbenches; note Data Model; audit contract; retention rules; history UI | Assigned LLD detail | Beta expands Working Notes to SRs and RFCs and permits edits and active-view deletion while preserving immutable creation time, revision evidence, and audited deletion. Storage and restoration details belong in the LLD. |
| `ALPHA:OPS-016` | Merged `main` | SOMA shall not offer manual Service Request creation. New canonical SRs enter SOMA only through Advanced Search synchronization. | Replace | `BETA-REQ-0053` | 1.0.0 | SR creation workflow; Tickets workspace; Advanced Search reconciliation; identifier contract; reviewed-merge UI | Assigned LLD detail | Beta permits manual SR creation with either a known canonical external identity or an allocated LSR identity. Later source population preserves SOMA-owned work, and adopting an official identity for an LSR requires explicit collision-free review. Field minima and transition mechanics belong in the LLD. |
| `ALPHA:OPS-017` | Merged `main` | The Advanced Search source is a user-configurable directory. A check automatically chooses the newest stable direct regular `Advanced Search(Service Request)YYYYMMDDHHMMSS.xlsx` by embedded export timestamp and rejects an export older than accepted history. | Replace | `BETA-REQ-0054` | 1.0.0 | Import Contract; Settings; Advanced Search discovery adapter; import-review UI; background-import use cases | Assigned LLD detail | Beta retains configurable safe discovery and source chronology while also allowing manual file selection and routing every candidate through staged review or an explicitly configured safe auto-accept class before mutation. Stabilization, polling/watching, and precedence details belong in the LLD. |
| `ALPHA:OPS-018` | Merged `main` | Source discovery/stabilization/XLSX parsing/header+row validation complete before mutation. Unreadable/malformed/unsafe input or omission of any contract-v4 minimum header (`SRNo`, `Problem Summary`, `Report Date`, `Customer Contact`, `Customer Severity`) rejects that synchronization with an actionable message and no fallback to older data. The remaining eight recognized headers are optional enrichment; provider-only headers are ignored without warning and excluded from logical identity. Duplicate/invalid SR rows reject the whole snapshot. | Replace | `BETA-REQ-0055` | 1.0.0 | Import Contract; Advanced Search adapter; staging model; import-review UI; validation tests | Assigned LLD detail | Beta retains complete pre-mutation validation, requires only the SR identity column for source acceptance, treats missing nonidentity allowlisted columns as coverage warnings, resolves fields by normalized header identity rather than column position, discards unallowlisted data, and distinguishes exact duplicates from conflicting same-SR rows. Header aliases, equivalence, and conflict mechanics belong in the LLD. |
| `ALPHA:OPS-019` | Merged `main` | Advanced Search synchronization uses SHA-256 of deterministic validated logical content, not raw XLSX bytes/filesystem metadata. The same contract/export-time/fingerprint is a no-op. A newer export with identical content records accepted-run/check recency without duplicate per-SR observations or change audit. | Replace | `BETA-REQ-0056` | 1.0.0 | Shared import architecture; source adapters; import-run model; replay protection; golden fixtures | Assigned LLD detail | Beta generalizes logical-content SHA-256 fingerprinting and no-op replay behavior to all official workbook families while excluding discarded data and physical row/column order. Canonical serialization, source chronology, and fingerprint versioning belong in the LLD. |
| `ALPHA:OPS-020` | Merged `main` | SQLite remains authoritative for accumulated SOMA-owned state/history. First disappearance from the newest accepted Advanced Search marks the SR `Removed / Pending removal`; no second source absence is required. A terminal source Status also makes the SR pending report finalization. Row reappearance before finalization clears pending removal. Terminal-pending state clears only when `Status` is actually observed with a recognized nonterminal value; omitted/blank/unknown Status cannot erase terminal evidence. Synchronization itself never finalizes/purges the SR and cannot overwrite SOMA-owned relationships/evidence/local operational state. | Replace | `BETA-REQ-0057` | 1.0.0 | SR lifecycle; Import Contract; Tickets views; Historical view; warning system; retention contract | Assigned LLD detail | Beta removes Alpha's removal/report-finalization lifecycle. Source absence is a reviewed high-risk warning rather than deletion evidence; terminal statuses control period-based muted/historical presentation; unknown status cannot erase terminal evidence; imports never purge or overwrite SOMA-owned work. Warning and period mechanics belong in the LLD. |
| `ALPHA:OPS-021` | Merged `main` | Advanced Search Customer Organization/Contact labels are matching/reconciliation data, not durable IDs. Unknown/ambiguous references never block an otherwise valid SR import and never cause display-name identity invention. | Replace | `BETA-REQ-0058` | 1.0.0 | Import Contract; Customer Organization model; Contact reconciliation; import-review UI; SR workbench | Assigned LLD detail | Beta retains label-safe reviewed reconciliation, adds Customer Account Code as the preferred stable external organization key when valid, and forbids silent identity invention or equal-name merging. Normalization, conflicts, and proposal mechanics belong in the LLD. |
| `ALPHA:OPS-022` | Merged `main` | The Advanced Search adapter enumerates direct regular files only, rejects link/reparse traversal and unsupported content, enforces reviewed archive/file/row/column/shared-string/text limits, executes no formulas, fetches no external workbook resources, and fails safely on limits. | Replace | `BETA-REQ-0059` | 1.0.0 | Shared import-security contract; all XLSX adapters; threat model; parser tests; diagnostics | Assigned LLD detail | Beta generalizes untrusted-workbook containment, resource limits, inactive-content parsing, offline behavior, and safe pre-mutation failure to all three official XLSX adapters. Concrete limits and parser mechanics belong in the LLD. |
| `ALPHA:OPS-023` | Merged `main` | The Phase 1C Advanced Search logical source shape recognizes `SRNo`, `Problem Summary`, `Report Date`, `Customer Contact`, `Customer Severity`, `Product`, `Current Handler`, `Status`, `ResolveBy`, `Resolve By Suspend`, `Suspend Planned End Date`, `Suspension Duration`, and `Customer Org.`. Contract v4 requires the first five headers in every legitimate export and records all recognized coverage in accepted-run `observed_fields`. `SRNo` is the only universally required usable row value; the other four required-column cells remain non-destructive when blank/malformed/unknown. | Replace | `BETA-REQ-0060` | 1.0.0 | Import Contract; Advanced Search adapter; staging schema; source-observation model; golden fixtures | Assigned LLD detail | Beta replaces Alpha's obsolete field set with the confirmed active and future-use allowlists, rejects Product, adds Last Update and Customer Account Code, keeps SRNo as the only universal row identity, and discards every unallowlisted field. Header aliases, types, blanks, and provenance belong in the LLD. |
| `ALPHA:OPS-024` | Merged `main` | Advanced Search timestamps without explicit offset are interpreted in `America/Guayaquil` (UTC-05:00) and normalized to aware UTC. Recognized Status/Severity vocabularies are exactly those versioned in `ADVANCED_SEARCH_CONTRACT.md`; unknown nonblank values warn and cannot overwrite the last recognized canonical value. | Replace | `BETA-REQ-0061` | 1.0.0 | Import Contract; source-family profiles; Settings; timestamp normalization; vocabulary registry; golden fixtures | Assigned LLD detail | Beta separates source timezone from display timezone, honors explicit offsets, retains accepted Ecuador operational and China filename defaults, and generalizes versioned controlled vocabularies and non-destructive unknown-value warnings to all source families. Configuration and warning details belong in the LLD. |
| `ALPHA:OPS-025` | Merged `main` | Product is latest-source-controlled product/device-family guidance and Current Handler is latest-source-controlled assignment guidance. Product does not itself create a Managed Element relationship; SOMA does not maintain a conflicting local handler authority. | Replace | `BETA-REQ-0062` | 1.0.0 | Import Contract; SR source projection; Contact reconciliation; SR workbench; discarded-field tests | Assigned LLD detail | Beta discards Advanced Search Product entirely while retaining Current Handler as a source-owned current-assignment fact. Handler reconciliation never silently creates/merges Contacts, blank source assignment does not destroy Contact identity, and local operational context remains separate. Presentation and matching details belong in the LLD. |
| `ALPHA:OPS-026` | Merged `main` | `ResolveBy` is the original expiration date. A valid `Resolve By Suspend` is the revised expiration after suspension/save and prevails for effective expiration. `Suspend Planned End Date` and decimal-day cumulative `Suspension Duration` remain distinct optional source facts. Duration converts to signed-64-bit integer microseconds using Decimal/half-even rounding, has no business maximum, and is never inferred between exports. Tickets with no valid expiration remain trackable without synthetic expiration. | Replace | `BETA-REQ-0063` | 1.0.0 | Import Contract; SLA contract; SR source projection; warning rules; golden fixtures | Assigned LLD detail | Beta defers both Resolve fields from active 1.0 behavior, conditionally activates Suspend Planned End Date only for a current future suspension, treats blank Suspension Duration as zero/no reported suspension history, and reviews contradictions with prior nonzero evidence. Representation and contradiction mechanics belong in the LLD. |
| `ALPHA:OPS-027` | Merged `main` | Configurable ticket age-risk defaults are Critical 5, Major 10, Minor 30, and Non-fault inquiry 45 calendar days from effective Report Date. Separate configurable suspension-KPI defaults are Critical 3, Major 10, Minor 15, and Non-fault inquiry 30 cumulative source-reported days. Neither schedule is a duration, retention, or extension limit. | Replace | `BETA-REQ-0064`, `BETA-REQ-0065` | 1.0.0 | Contract Product Line and SLA Contract; Customer Organization, Contract, and Product Line Data Model; import classification; Settings; Overview and reporting; audit | Assigned LLD detail | Beta removes Alpha's global severity-based age-risk and suspension-KPI schedules as controlling policy. Every Contract belongs to one Customer Organization, while reusable Product Lines appear through distinct Contract Product Lines whose policies may differ by customer and Contract. The confirmed IT and NFV templates measure the percentage of eligible SRs resolved or closed within inclusive durations, and Non-fault inquiry derives from Minor by multiplying durations by 1.5 while preserving its tier structure. An accepted policy revision recalculates all existing SRs classified under it, including older and terminal SRs, without mutating completed report snapshots. Suspension remains an elapsed-time source fact, not the rejected global severity-threshold schedule. Each SR has at most one active same-customer Contract Product Line classification; automatic assignment is deterministic and reviewed when ambiguous; discarded Product text cannot classify; manual reclassification is audited; and Cancelled SRs are excluded. Exact cohort membership, denominator, snapshot, and rounding mechanics belong in the use cases and LLD. |
| `ALPHA:OPS-028` | Merged `main` | The newest accepted Advanced Search observation prevails for current source-controlled facts. Retained historical observations are deliberately compact rather than full workbook copies, but preserve progression/KPI facts needed for useful history: report date, severity, Product, Current Handler, recognized status, deadline/suspension values, and allowlisted warnings. Problem Summary is not repeated per observation; its final accepted value is retained once in the terminated SR snapshot. Customer Org/Contact source labels and contact channels are not copied into every observation. | Replace | `BETA-REQ-0066` | 1.0.0 | Import Contract; source-observation model; SR current projection; Historical view; audit; import reconciliation LLD | Assigned LLD detail | Beta retains newest-accepted usable field precedence and compact history but replaces Alpha's field set, terminated-SR snapshot, and purge assumptions. Compact deltas contain only meaningfully observed or changed allowlisted values plus provenance and warnings; discarded Product and complete workbook copies are never retained. Current Problem Summary remains on the SR with audited revisions, unchanged labels/channels are not repeated, and Historical view uses the surviving SR, compact observations, and audit history. Delta representation, reconstruction, compaction, and chronology-tie mechanics belong in the LLD. |
| `ALPHA:OPS-029` | Merged `main` | Unknown source Customer Organizations/Contacts may produce user-reviewed minimal-reference creation proposals after import. Contact matching is scoped by Customer Organization and follows REF-010. A usable/resolved Contact reference and contact email/phone are not required for ordinary SR/MW handling, even though the contract-v4 `Customer Contact` header is required; a later Spare Request operation may require/collect missing contact details when it actually needs them. | Replace | `BETA-REQ-0028`, `BETA-REQ-0029`, `BETA-REQ-0030`, `BETA-REQ-0031`, `BETA-REQ-0055`, `BETA-REQ-0058`, `BETA-REQ-0067` | 1.0.0 | Contact Data Model; Import Contract; reconciliation service; Ticket and Objective workflows; Inventory communications; MSG generation; audit | Assigned LLD detail | Beta retains reviewed minimal-reference proposals and just-in-time channel collection while removing Alpha's mandatory Customer Contact header and organization-exclusive identity scope. Contacts may be affiliated or unbound, equal names never prove identity, unknown references do not block SR import, and ordinary operational work requires no communication channel. Only the action that needs a recipient is blocked when no usable channel is supplied; artifact evidence snapshots the effective recipient. Matching, channel validation, and recipient-selection mechanics belong in the LLD. |
| `ALPHA:OPS-030` | Merged `main` | A pure invocation policy defaults Advanced Search automatic synchronization to 10:00 `America/Guayaquil` daily and accepts a configurable whole-minute local time. It identifies a due scheduled/startup check, catches up when the application was offline past the boundary, treats explicit **Sync now** as always runnable, and avoids rerunning an already satisfied daily boundary. The Phase 2 application shell owns timers, persistence of the last successful check, and runtime invocation; no trigger asks the operator to select a workbook. | Replace | `BETA-REQ-0068` | 1.0.0 | Settings; Import Contract; application shell; tray/background lifecycle; import staging; scheduler LLD; acceptance tests | Assigned LLD detail | Beta retains the useful daily default, configurable whole-minute schedule, one-time startup catch-up, manual invocation, and satisfied-boundary protection while replacing automatic mutation with the reviewed staging/safe-auto-accept boundary. Automatic checks operate only after inbox configuration, may be disabled, discover the newest eligible stable workbook without a picker, and remain independently protected by logical-content fingerprinting. Timer, interruption, retry, and persistence mechanics belong in the LLD. |
| `ALPHA:OPS-031` | Merged `main` | `Closed`, `Resolved`, and `Cancelled` suppress active expiration/risk reminder notifications immediately while preserving historical KPI/source evidence. Dashboard risk state remains the primary continuous warning surface; supplemental notification for the same ticket/active condition occurs at most once per calendar day. | Replace | `BETA-REQ-0069` | 1.0.0 | SR lifecycle; Contract Product Line and SLA Contract; Overview and Needs Attention; local notification policy; Historical view; audit | Assigned LLD detail | Beta retains terminal suppression, historical evidence, a primary continuous in-app surface, and daily supplemental-notification deduplication while removing Alpha's global expiration model. Resolved and Closed SRs retain terminal-duration evidence and applicable cohort participation; Cancelled SRs are excluded from SLA cohorts. Resolve fields generate no 1.0 expiration alerts, while suspected terminal reversal remains a distinct high-risk review warning. Notification persistence and tray/in-app mechanics belong in the LLD. |
| `ALPHA:OPS-032` | Merged `main` | Approaching `Suspend Planned End Date` produces a distinct suspension-ending-soon alert in addition to age-risk/expiration state. Risk presentation supports no deadline, healthy, at-risk/old, expiring, overdue, suspended/extended, suspension ending soon, source warning, pending source removal, and terminal semantics using icon/text plus color rather than color alone. | Replace | `BETA-REQ-0070` | 1.0.0 | SR lifecycle; Contract Product Line and SLA Contract; Overview and Needs Attention; warning model; accessibility; use cases; LLD | Assigned LLD detail | Beta retains the distinct accessible suspension-ending-soon warning but replaces Alpha's global deadline and removal-state taxonomy with independent dimensions for classification, individual duration evidence, cohort compliance, active suspension, source integrity, and terminal state. Individual duration evidence is not itself a percentage-tier failure. Exact lead-time defaults, simultaneous-state priority, and responsive presentation belong in the accepted use cases and LLD. |
| `ALPHA:OPS-033` | Merged `main` | Export captures durable membership/lifecycle evidence, the matching compact source observation, report-time Customer Contact, report-time organization assignment/scope, and an immutable report-data snapshot for one Customer Organization or all organizations before writing the artifact. Artifact success enables a second explicit cleanup prompt; it does not finalize automatically. Later Customer Contact drift, captured compact-fact drift, or organization reassignment makes the member stale for both organization-scoped and all-organization reports. Problem Summary-only drift is the explicit exception and a newer identical provenance-only check is not stale. The operator may confirm or opt individual eligible tickets out and may return later through a manual cleanup action. Failed/cancelled generation performs no cleanup. | Replace | `BETA-REQ-0071` | 1.0.0 | Overview and Reporting; report snapshot model; Excel export; Customer Organization scope; SLA evidence; audit; retention | Assigned LLD detail | Beta retains consistent immutable report evidence, organization scoping, report-time display values, and safe failure behavior while removing Alpha's report-triggered cleanup, member-staleness, and special drift exceptions. Completed snapshots contain only report-required values and reproducibility provenance; later changes affect current and future reports without mutating completed evidence. Exact columns, filters, workbook layout, and bounded failed-attempt retention belong in the use cases and LLD. |
| `ALPHA:OPS-034` | Merged `main` | Immediate cleanup finalizes only explicitly confirmed report members whose pending/report/source/scope evidence still matches. In the same rollback-by-default UoW, it archives/minimizes current SR state, removes resolved customer/contact links and SR communication associations, deletes only communications with no surviving ownership/evidence, appends minimized audit, and commits once. Separately, at the configurable 180-day default due instant, retention housekeeping creates one 27-field allowlisted final snapshot excluding Customer Contact and deep-purges remaining SR-episode source/report/note/history/link/cache data while preserving independent operational records, global contacts, append-only minimized audit, and legitimately shared evidence. | Replace | `BETA-REQ-0019`, `BETA-REQ-0057`, `BETA-REQ-0066`, `BETA-REQ-0071`, `BETA-REQ-0072` | 1.0.0 | Product Contract; operational-history retention; Historical view; audit; deletion safeguards; future Beta 1.1 retention design | Confirmed | Beta removes report-confirmed finalization, immediate minimization, the 180-day timer, the 27-field final snapshot, and deep purge. Historical placement is presentation only; accepted relationships and evidence survive. Domain-authorized correction remains previewed, confirmed, transactional, and audited. Technical cache/staging/log/backup maintenance remains distinct, while any operational purge requires a new Beta 1.1 contract and cannot inherit Alpha defaults silently. |
| `ALPHA:OPS-035` | Merged `main` | Accepted/frozen migration `002_phase1_application.sql` separates accepted import runs, purgeable latest source state, compact observations, durable report runs/items/decisions, lifecycle, retention authorization, final snapshots, and purge tombstones. Finalization references the generated report whose confirmed snapshot member authorizes it. Future persistent corrections use `003+`. | Replace | `BETA-REQ-0073` | 1.0.0 | Architecture; Data Model LLD; migration strategy; schema runner; startup integrity; future Alpha importer decision | Assigned LLD detail | Beta starts an independent clean schema and migration lineage. It retains explicit persistence boundaries and forward-only accepted migrations while rejecting Alpha migration numbering, purge/finalization structures, and direct Alpha database upgrade. Draft Beta migrations remain revisable until accepted; afterward their identity, order, content, and integrity evidence are immutable and corrections use new forward migrations. Exact tooling, checksums, concurrency, and recovery belong in the LLD. |
| `ALPHA:OPS-036` | Merged `main` | Before mutation, synchronization returns a compact inserted/changed/unchanged/missing/customer-shift/warning summary. A valid empty authoritative population affecting existing nonterminated SRs requires explicit confirmation bound to the exact source identity. Exact nonempty population/customer-distribution thresholds remain deferred until representative operational volumes establish reviewed defaults; the current result still exposes the counts needed by that later policy. | Replace | `BETA-REQ-0074` | 1.0.0 | Import Contract; all official source adapters; import-review UI; safe auto-accept policy; source profiles; acceptance tests | Assigned LLD detail | Beta generalizes the pre-mutation impact summary to all official sources, uses proposal terminology before acceptance, and infers absence only for a declared authoritative scope. Empty authoritative populations require exact-source high-risk confirmation and remain nondestructive. Beta 1.0 displays nonempty population and organization-distribution comparisons but adopts no unvalidated numeric accept/reject threshold. Exact presentation and source-specific summary fields belong in the use cases and LLD. |
| `ALPHA:OPS-037` | Merged `main` | Every legitimate export contains the five contract-v4 minimum headers, and every source row requires a canonical nonblank SRNo. Other recognized values are merged only when usable; omission of optional fields or invalidity preserves prior valid state, while a new SR may begin incomplete. Missing/unusable Report Date records the captured import instant at canonical UTC whole-second persistence precision as `system_assigned`, and a later real value replaces that fallback. Before irreversible report-confirmed finalization, the accumulated projection must contain nonblank Problem Summary, recognized Severity, and effective Report Date. Product and Current Handler remain optional enrichment. | Replace | `BETA-REQ-0055`, `BETA-REQ-0060`, `BETA-REQ-0062`, `BETA-REQ-0075` | 1.0.0 | Import Contract; Advanced Search adapter; SR source projection; Contract Product Line SLA; incomplete-state UX; reporting; use cases; LLD | Assigned LLD detail | Beta retains SRNo as the sole universal identity requirement and non-destructive field precedence while removing Alpha's five-header minimum, synthetic import-time Report Date, Product retention, and report-finalization gate. New SRs may remain incomplete; import capture time is provenance only and never SLA evidence. Later usable values fill missing facts, and actions validate genuinely required information at point of use. Exact incomplete labels and contextual requirements belong in the use cases and LLD. |
| `ALPHA:OPS-038` | Merged `main` | A finalized SR episode never reactivates. Terminal reappearance is ignored with bounded provenance. If a finalized or purged/tombstoned SRNo reappears nonterminally, Phase 1C fails closed and returns a bounded new-episode proposal; it cannot create a new anchor or mutate old history. Actual authorization/creation of a same-number new episode requires a future explicitly reviewed forward migration/application service. Unsigned XLSX validation detects structural/lineage anomalies but is not represented as proof that a person did not edit the file. | Replace | `BETA-REQ-0057`, `BETA-REQ-0069`, `BETA-REQ-0076` | 1.0.0 | SR lifecycle; Import Contract; terminal-reversal review; source-disappearance reconciliation; SLA recalculation; report snapshots; audit; workbook trust model | Assigned LLD detail | Beta removes finalized episodes, tombstones, and same-number episode creation. One official SRNo identifies one surviving SR. Later terminal observations reconcile normally; reappearance may clear a disappearance warning. A terminal-to-nonterminal value is never auto-accepted but may be explicitly confirmed on the same SR with preserved history and recalculated current projections. Unsigned workbook validation proves processing integrity, not publisher authenticity or absence of human editing. Exact review presentation belongs in the use cases and LLD. |
| `ALPHA:OPS-039` | Merged `main` | Deep retention is per local user, defaults to 180 exact elapsed UTC days from effective Report Date, uses the accepted daily Guayaquil due policy when later housekeeping is wired, and exempts active tickets until finalization. Policy changes apply to terminated episodes that have not entered authorized purge; there is no per-ticket postponement/legal hold. | Replace | `BETA-REQ-0072`, `BETA-REQ-0077` | 1.0.0 | Product Contract; Settings; Historical view; operational-history retention; technical housekeeping; future Beta 1.1 retention design | Confirmed | Beta 1.0 has no operational retention clock, due timestamp, or purge Settings. Daily/Weekly/Monthly controls main-view presentation only. Operational retention is installation-level governance, not a Local User preference. Technical backup/log/staging/cache policies remain separate. A future Beta 1.1 retention contract must decide scope, timing, dependencies, exceptions/holds, preview, recovery, and audit without inheriting Alpha defaults. |
| `ALPHA:OPS-040` | Merged `main` | First-run profile requires editable name, username, email, and phone. Username is never cryptographic key material. A random data key must be protected by Windows DPAPI or an equivalently reviewed provider and referenced by non-secret key metadata. SOMA retains a configurable positive finite backup count, default five, and prunes only after verifying the replacement backup. | Replace | `BETA-REQ-0035`, `BETA-REQ-0036`, `BETA-REQ-0037`, `BETA-REQ-0078` | 1.0.0 | Identity and Settings; authentication; Contact model; Security LLD; key lifecycle; managed backup and portable recovery; acceptance tests | Assigned LLD detail | Beta retains one Local Administrator, independent random live-data encryption, Windows-bound key protection, portable backup encryption, and verified finite managed-backup rotation. It removes username entry from single-user login and mandatory email/phone from the login profile; operational channels belong to Contacts. Seven random words or equivalent 128-bit recovery replaces a weak seven-character secret. Exact algorithms, verifier parameters, Windows API, backup format, reset authorization, and recovery mechanics belong in the Security LLD. |
| `ALPHA:SPR-001` | Merged `main` | A Spare Need represents a persistent operational requirement, not a consumed request attempt. | Replace | `BETA-REQ-0079` | 1.0.0 | Inventory Lifecycle Contract §§2–4; SR workbench; Inventory Data Model; use cases; LLD | Assigned LLD detail | Beta retains persistence and non-consumption while moving the Need from one Device to one Service Request and aggregating matching Device Part Unit contributors by BOM. |
| `ALPHA:SPR-002` | Merged `main` | A Spare Need may carry BOM/model/quantity/notes/positions/faulty serial evidence and links to SR/WFM/Managed Elements. | Replace | `BETA-REQ-0080` | 1.0.0 | Inventory Lifecycle Contract §§2–5; Stock eligibility; SR workbench; Task allocation; LLD | Assigned LLD detail | Beta separates Device Part Unit facts from the SR-level Need, derives demand from contributor units, and makes compatible Stock review precede—but not prohibit—the external request choice. |
| `ALPHA:SPR-003` | Merged `main` | A Spare Need remains requestable multiple times until explicitly resolved/cancelled; each request attempt retains its Need link. | Replace | `BETA-REQ-0081` | 1.0.0 | Inventory Lifecycle Contract §§3–4; Need lifecycle; deletion safeguards; audit; LLD | Assigned LLD detail | Beta retains repeated selection and request linkage while defining explicit Need lifecycle, reactivation, history-preserving removal, and narrow untouched-record deletion. |
| `ALPHA:SPR-004` | Merged `main` | A Spare Request is an independent request/tracking container and may optionally link SR, Dispatch Site, and Requester. | Replace | `BETA-REQ-0082` | 1.0.0 | Inventory Lifecycle Contract §§5–6; Contact roles; Dispatch Location; MSG workflow; UI; LLD | Assigned LLD detail | Beta retains an independent request identity but requires one SR through selected Needs, derives Customer Organization, suggests the ticket-owner Contact, and records receiver plus delivery/self-pickup logistics. |
| `ALPHA:SPR-005` | Merged `main` | A Spare Request may contain multiple BOM lines with positive quantity; submitted lines and Need membership lock after draft. | Replace | `BETA-REQ-0083` | 1.0.0 | Inventory Lifecycle Contract §§5–7; allocations; submission snapshot; offline mail; LLD | Assigned LLD detail | Beta replaces free BOM lines with quantity-bearing Need allocations, locks the evidenced or manually confirmed submitted version rather than mere draft generation, and preserves later partial progress append-only. |
| `ALPHA:SPR-006` | Merged `main` | RMA is a normalized first-class return authorization; correcting canonical number preserves old number as immutable alias. One Spare Request may have multiple canonical RMAs. | Replace | `BETA-REQ-0084` | 1.0.0 | Inventory Lifecycle Contract §§3, 7–9; RMA identity; assignment; receipt; return; LLD | Assigned LLD detail | Beta retains first-class C10 identity, multiple RMAs, and immutable aliases while redefining each RMA as a two-sided obligation bridge with separate target, inbound-unit, and return-unit relationships. |
| `ALPHA:SPR-007` | Merged `main` | Physical units are separate from request lines; requested units require matching request item/BOM and C10 identity. | Replace | `BETA-REQ-0085` | 1.0.0 | Inventory Lifecycle Contract §§2–3, 8–10; physical identity; compatibility; dismantling; LLD | Assigned LLD detail | Beta retains physical separation but assigns C10 to the RMA, allows reviewed requested-versus-actual BOM differences, distinguishes Device Part Units from Inventory units, and supports extracted-unit provenance. |
| `ALPHA:SPR-008` | Merged `main` | Scrapped/on-site unit has no request item/C10 and is identified by BOM plus serial. | Replace | `BETA-REQ-0086` | 1.0.0 | Inventory Lifecycle Contract §§3–5, 10; Stock; manual registration; disposition; LLD | Assigned LLD detail | Beta replaces BOM-plus-serial identity with immutable LSU identity, makes official provenance optional, and separates origin, location, condition, and disposition. |
| `ALPHA:SPR-009` | Merged `main` | A physical unit may be manually allocated to an MW; released/unused unit may be reused, used unit may not be used twice without explicit rollback. | Replace | `BETA-REQ-0087` | 1.0.0 | Inventory Lifecycle Contract §§9–10; Tasks; replacement outcome; return selection; LLD | Assigned LLD detail | Beta allocates units to Tasks, prohibits concurrent contradictions, permits legitimate sequential reuse through physical events, and derives the RMA return unit from the reviewed maintenance outcome. |
| `ALPHA:SPR-010` | Merged `main` | Spare lifecycle evidence is append-oriented with source/time/target/optional communication evidence/supersession-suppression. | Replace | `BETA-REQ-0088` | 1.0.0 | Inventory Lifecycle Contract §§7–12; lifecycle journal; projection; audit; LLD | Assigned LLD detail | Beta retains append-oriented evidence while distinguishing correction rollback from genuine warehouse rejection/resend loops and requiring reproducible transactional projections. |
| `ALPHA:SPR-011` | Merged `main` | Email-derived lifecycle events reference the stored relevant communication supporting them. | Replace | `BETA-REQ-0089` | 1.0.0 | Inventory Lifecycle Contract §§6–8, 12; offline mail; proposal review; evidence boundary; LLD | Assigned LLD detail | Beta retains exact indexed-message provenance but uses reviewed PST/OST proposals, supports incremental identifiers and dispatch, permits manual confirmation without evidence, and exposes no attachment control in 1.0. |
| `ALPHA:SPR-012` | Merged `main` | Lifecycle transitions are available manually and through reviewed email-derived proposals; compatible bulk actions support explicit rollback/correction. | Replace | `BETA-REQ-0090` | 1.0.0 | Inventory Lifecycle Contract §§7–12; transition matrix; bulk actions; correction and projection services; audit; use cases; LLD | Assigned LLD detail | Beta retains manual and reviewed communication-derived transitions while constraining bulk compatibility, preserving per-target events, and separating correction from genuine rejection/resend history. |
| `ALPHA:SPR-013` | Merged `main` | SOMA supports Create and Export & Create plus recording a request created/sent outside SOMA; export is not proof of email send. | Replace | `BETA-REQ-0091` | 1.0.0 | Inventory Lifecycle Contract §§5–8, 12; MSG workflow; external registration; communication reconciliation; audit; use cases; LLD | Assigned LLD detail | Beta separates local draft creation, `.msg` generation, accepted submission, and external-request registration while preserving the rule that export does not prove sending. |
| `ALPHA:SPR-014` | Merged `main` | Lifecycle correction/supersession targets the exact same Spare Request/item/unit; target identity is immutable. | Replace | `BETA-REQ-0092` | 1.0.0 | Inventory Lifecycle Contract §§2–3, 7–12; identity map; relationship correction; projection rebuild; duplicate reconciliation; audit; use cases; LLD | Assigned LLD detail | Beta retains immutable identity and exact-target correction while extending protection to every Inventory entity, event, allocation, and relationship in the reconciled model. |
| `ALPHA:SPR-015` | Merged `main` | Submitted Spare Requests may preserve requested/default delivery snapshot; each physical unit may separately preserve actual delivery snapshot/site. | Replace | `BETA-REQ-0093` | 1.0.0 | Inventory Lifecycle Contract §§5–10, 12; logistics event model; submission snapshot; partial dispatch/receipt; custody history; use cases; LLD | Assigned LLD detail | Beta retains immutable requested and actual logistics evidence while distinguishing delivery/self-pickup, Dispatch Location from Infrastructure Site, shared logistics events from per-unit participation, and parent-assembly receipt from extracted-unit provenance. |
| `ALPHA:FTT-001` | Merged `main` | Fault Tags/returns are SOMA 1 scope. | Replace | `BETA-REQ-0094` | 1.0.0 | Inventory Lifecycle Contract §§3, 7, 9–12; Product Contract §8; Inventory workspace; Fault Tag use cases; UI; LLD | Assigned LLD detail | Beta retains Fault Tags and returns as mandatory 1.0.0 scope, makes Fault Tags a primary Inventory view, and requires the complete cross-request warehouse and resend lifecycle without depending on manual uploads. |
| `ALPHA:FTT-002` | Merged `main` | Fault Tag has tracking identity, optional pickup site, state, immutable first-send evidence. | Replace | `BETA-REQ-0095` | 1.0.0 | Inventory Lifecycle Contract; Fault Tag Data Model; return-method and logistics contract; submission workflow; audit; UI; LLD | Assigned LLD detail | Beta retains Fault Tag identity, conditional pickup logistics, projected state, and immutable first-submission evidence while defining the Dispatch Location as the pickup origin, separating any destination, and preserving manual submission without uploaded evidence. |
| `ALPHA:FTT-003` | Merged `main` | Fault Tag items may be faulty removed hardware or new/unused replacement units. | Replace | `BETA-REQ-0096` | 1.0.0 | Inventory Lifecycle Contract; Fault Tag membership model; RMA return selection; eligibility query; resend workflow; UI; LLD | Assigned LLD detail | Beta retains faulty removed and unused replacement units while deriving eligibility from the open RMA return obligation and reviewed maintenance outcome, preserving physical identity, and supporting later resend without contradictory active membership. |
| `ALPHA:FTT-004` | Merged `main` | Item may reference SR/Spare Request/Managed Element/physical unit/canonical RMA without duplicating RMA values. | Replace | `BETA-REQ-0097` | 1.0.0 | Inventory Lifecycle Contract; Fault Tag membership model; RMA and Service Request derivation; snapshot allowlist; correction workflow; UI; LLD | Assigned LLD detail | Beta replaces optional independent links with one mandatory RMA-and-physical-unit membership chain, derives ticket and Device context, avoids competing copies, and preserves only immutable submitted display evidence. |
| `ALPHA:FTT-005` | Merged `main` | Membership/item identity lock after first send; correction cancels/archives original and creates one explicit replacement. | Replace | `BETA-REQ-0098` | 1.0.0 | Inventory Lifecycle Contract; Fault Tag submission and replacement lifecycle; correction service; resend workflow; audit; UI; LLD | Assigned LLD detail | Beta locks membership only after accepted real submission, distinguishes false-submission correction from material replacement, preserves linear replacement lineage, avoids replacement for alias-only corrections, and models genuine resend separately. |
| `ALPHA:FTT-006` | Merged `main` | Warehouse confirmation/closure requires evidence communication for every item plus explicit user confirmation; confirmed evidence/timestamps lock and closed is terminal. | Replace | `BETA-REQ-0099` | 1.0.0 | Inventory Lifecycle Contract; Fault Tag warehouse lifecycle; proposal review; partial-processing UI; RMA closure; correction and resend services; LLD | Assigned LLD detail | Beta replaces mandatory communication evidence and single-step closure with reviewed or manual per-membership receipt and final decision, explicit final confirmation, partial processing, correctable immutable events, accepted closure, rejected unresolved obligations, and linked resend attempts. |
| `ALPHA:FTT-007` | Merged `main` | Product “Delete Fault Tag” means archive/cancel/optional replace when history exists; hard delete only true unsent draft without dependent history. | Replace | `BETA-REQ-0100` | 1.0.0 | Inventory Lifecycle Contract; Fault Tag lifecycle; deletion policy; destructive preview; audit; UI; LLD | Assigned LLD detail | Beta replaces generic Delete with state-specific hard-delete, cancellation, replacement, archival, and resend actions; preserves every evidenced record and independent dependency; and limits transactional hard deletion to a truly untouched draft. |
| `ALPHA:FTT-008` | Merged `main` | Sent Fault Tag pickup selection/snapshot is immutable; snapshot cannot exist without selected pickup site. | Replace | `BETA-REQ-0101` | 1.0.0 | Inventory Lifecycle Contract; Fault Tag logistics contract; Dispatch Location roles; submission and actual-pickup events; replacement/resend workflows; UI; LLD | Assigned LLD detail | Beta retains immutable submitted pickup evidence while defining Dispatch Location as the pickup origin, making the snapshot conditional on pickup return method, separating actual pickup and destination, and requiring new snapshots for replacement or resend attempts. |
| `ALPHA:DEV-001` | Merged `main` | UI may use “Device Manager”; domain uses Managed Element for physical/logical infrastructure. | Replace | `BETA-REQ-0102` | 1.0.0 | Infrastructure Contract §§2–3; Product Contract §9; Glossary; Infrastructure workspace; use cases; LLD | — | Beta retires Device Manager and Managed Element as canonical terms, uses Infrastructure as the workspace, Device Reference for operational identity, and Network Element for a registered device instance. |
| `ALPHA:DEV-002` | Merged `main` | Managed Element requires name/type and may store model/serial/rack/U/site/username/credential-reference/notes. | Replace | `BETA-REQ-0103` | 1.0.0 | Infrastructure Contract §§3–4, 9; Infrastructure Data Model; progressive registration; placement; Notes; use cases; LLD | Assigned LLD detail | Beta distributes the Alpha fields to their owning entities and relationships, requires identity/name/Site, permits progressive completion, derives placement facts, and keeps SSH credentials outside 1.0.0. |
| `ALPHA:DEV-003` | Merged `main` | Managed Element has zero-many IPs and at most one primary; logical elements need not have IP. | Retain | `BETA-REQ-0104` | 1.0.0 | Infrastructure Contract §5; IP-address Data Model; import/export workbook; use cases; LLD | Assigned LLD detail | Beta retains zero-many stored Network Element IP addresses, at most one primary, and no mandatory address. Address inventory does not activate deferred connectivity, discovery, reachability, or SSH. |
| `ALPHA:DEV-004` | Merged `main` | Containment is an acyclic parent/child forest. | Retain | `BETA-REQ-0105` | 1.0.0 | Infrastructure Contract §6; containment Data Model; correction/history; use cases; LLD | Assigned LLD detail | The acyclic forest invariant remains complete and applies to Network Element and compound-sub-element containment, separately from placement and connectivity. |
| `ALPHA:DEV-005` | Merged `main` | Cloud/Solution↔Datacenter Site and Managed Element↔Cloud/Solution are many-to-many. | Replace | `BETA-REQ-0106` | 1.0.0 | Infrastructure Contract §7; Site/Cloud Data Model; placement and import review; use cases; LLD | — | Beta replaces the direct many-to-many model with reusable Cloud Types, site-bound Cloud Deployments, and optional at-most-one same-Site Network Element assignment. |
| `ALPHA:DEV-006` | Merged `main` | Network/logical connectivity is stored separately from containment with provenance/confidence/times where applicable. | Replace | `BETA-REQ-0107` | 1.x.0 | Infrastructure Contract §8; future connectivity/topology contract; provenance; correction; LLD | Assigned future contract | Beta preserves strict separation from containment, placement, Cloud assignment, and IP inventory while deferring canonical connectivity edges, discovery, and topology to 1.x.0. |
| `ALPHA:DEV-007` | Merged `main` | Passwords are never stored in ordinary SOMA tables; only opaque future credential-provider references may persist. | Retain | `BETA-REQ-0108` | 1.0.0; future references 1.x.0 | Infrastructure Contract §9; Security LLD; workbook contract; future credential-provider ADR | Assigned LLD detail | Beta retains the prohibition across domain data, Notes, audit, logs, and workbooks; 1.0 exposes no device credential controls and a later capability may store only an opaque approved-provider reference. |
| `ALPHA:DEV-008` | Merged `main` | SQLite remains authoritative; graph database may only be introduced later as a derived projection if measurements justify it. | Retain | `BETA-REQ-0109` | 1.0.0; possible projection later | Infrastructure Contract §10; Architecture §4; persistence LLD; future benchmark/ADR | Assigned LLD detail | SQLite remains authoritative and sufficient for every 1.0 Infrastructure invariant. Any later graph engine must be measured, derived, rebuildable, disposable, and unable to accept independent authoritative writes. |
| `ALPHA:MAIL-001` | Merged `main` | Mail scanning does not run until at least one trackable operational entity exists; Managed Elements alone do not trigger scanning. | Replace | `BETA-REQ-0111` | 1.0.0 | Communications Contract §§2–3; scheduler gate; source validation; UI; LLD | — | Beta applies the no-target gate to read-only PST/OST processing, requires an accepted trackable entity, and confirms that Device References, Network Elements, and Infrastructure workbook operations never activate scanning. |
| `ALPHA:MAIL-002` | Merged `main` | Trackable entities include SR/TT context, Spare Requests, RFCs, WFMs, MWs, Fault Tags. | Replace | `BETA-REQ-0112` | 1.0.0 | Communications Contract §3; identifier registry; matching; workbenches; LLD | Assigned LLD detail | Beta translates the taxonomy, adds first-class RMA obligations and current/historical aliases, preserves contextual SR/TT display, and excludes descriptive device/reference facts from communication identity. |
| `ALPHA:MAIL-003` | Merged `main` | Initial scan begins no earlier than earliest relevant report/external creation date; later scans incremental from high-water mark. | Replace | `BETA-REQ-0113` | 1.0.0 | Communications Contract §§4–5; scan coverage; high-water state; adapter; LLD | Assigned LLD detail | Beta retains bounded initial and incremental scanning while defining unknown chronology, source/folder-scoped watermarks, overlap replay, successful-checkpoint advancement, and reset/rescope review. |
| `ALPHA:MAIL-004` | Merged `main` | Adding older object may trigger bounded targeted backfill; Deep Scan is explicit user action. | Replace | `BETA-REQ-0114` | 1.0.0 | Communications Contract §5; backfill/Deep Scan jobs; coverage; UI; LLD | Assigned LLD detail | Beta scopes backfill to exact identities/sources/folders/ranges, protects forward progress, permits only bounded safe automatic work, and keeps every Deep Scan explicitly confirmed. |
| `ALPHA:MAIL-005` | Merged `main` | Only matched communications persist; one communication may link multiple entities. | Replace | `BETA-REQ-0115` | 1.0.0 | Communications Contract §7; communication/link Data Model; proposal review; privacy; LLD | Assigned LLD detail | Beta retains only accepted/safely matched communications, stores one canonical message with independently correctable entity links, and keeps unmatched content transient and nonpersistent. |
| `ALPHA:MAIL-006` | Merged `main` | Relevant mail retained by default while its active/retained entity context requires it; report-confirmed SR termination applies OPS-034 minimization rather than an unconditional forever-retention rule. | Replace | `BETA-REQ-0116` | 1.0.0 | Communications Contract §8; terminal unlink/orphan purge; Settings; Historical view; backup boundary; LLD | Assigned LLD detail | Beta rejects report-driven OPS-034 behavior but removes direct links after accepted SR/RFC termination, places genuine orphans in a configurable seven-day-default grace state, revalidates dependencies, and purges reconstructable content while preserving bounded non-reconstructable evidence. |
| `ALPHA:MAIL-007` | Merged `main` | Fetch/matching schedules default hourly/linked unless user explicitly separates them. | Replace | `BETA-REQ-0117` | 1.0.0 | Communications Contract §9; scheduler/job service; Settings; LLD | Assigned LLD detail | Beta replaces provider fetch with one hourly configurable local processing pipeline so unmatched content need not persist between schedules; source scan, matching, retention, proposals, and progress commit in order. |
| `ALPHA:MAIL-008` | Merged `main` | Message uniqueness is account-scoped; recipients are valid JSON. | Replace | `BETA-REQ-0118` | 1.0.0 | Communications Contract §§2 and 6; source-scope/participant Data Model; Contact matching; LLD | Assigned LLD detail | Beta preserves mailbox-scope uniqueness through a path-independent Communication Source Scope and replaces mandatory JSON storage with structured participant roles and optional versioned serialization. |
| `ALPHA:MAIL-009` | Merged `main` | Microsoft adapter requests provider-stable message IDs; any fallback requires explicit identity/canonicalization/collision policy. | Replace | `BETA-REQ-0119` | 1.0.0 | Communications Contract §§6 and 11; PST/OST adapter ADR; identity aliases/collisions; MSG reconciliation; LLD | Assigned LLD detail | Beta reads stable provider-origin identity from local stores, scopes it to source, requires versioned fallback canonicalization and collision-safe separation, and treats generated MSG identity separately from sent communication. |
| `ALPHA:MAIL-010` | Merged `main` | Long provider work exposes progress/phase/count/cancellation/bounded retry/redacted diagnostics. | Replace | `BETA-REQ-0120` | 1.0.0 | Communications Contract §10; background jobs; diagnostics; recovery; LLD | Assigned LLD detail | Beta applies the controls to long local-store jobs with honest progress, durable checkpoints, cooperative cancellation, bounded idempotent retry, restart recovery, and non-reconstructable redaction. |
| `ALPHA:MAIL-011` | Merged `main` | Mail fetch/matching, Advanced Search synchronization, and local draft persistence remain independent workflows with no implicit cross-write. | Replace | `BETA-REQ-0121` | 1.0.0 | Communications Contract §11; Architecture; application services; all adapters; HLD/LLD | Assigned LLD detail | Beta extends workflow isolation to every official import, Infrastructure workbook operation, PST/OST job, MSG generation, and orphan housekeeping, with explicit proposals/services instead of hidden cross-write. |
| `ALPHA:MAIL-012` | Merged `main` | SR/Spare Request views distinguish received/sent counts/direction and last-interaction age without duplicating message bodies. | Replace | `BETA-REQ-0122` | 1.0.0 | Communications Contract §12; Workbench Contract; summary projection; Historical view; UI/LLD | Assigned LLD detail | Beta generalizes deduplicated communication summaries to every trackable detail view, distinguishes direction/coverage/unknown chronology, excludes drafts, and freezes non-reconstructable SR/RFC terminal summaries after unlinking. |
| `ALPHA:UX-001` | Merged `main` | List navigation separates selection/opening: click selects, double-click/Enter opens, arrows move selection, wheel scrolls surface. | Replace | `BETA-REQ-0123` | 1.0.0 | UI/UX Contract §§2–3; Workbench Contract; shared list/table components; accessibility; LLD | Assigned LLD detail | Beta globalizes selection/opening semantics, separates focus/active/multi-selection/open state, gives wheel and trackpad input to the hovered scrollable surface, and preserves context on return. |
| `ALPHA:UX-002` | Merged `main` | Autocomplete is theme-consistent, input-triggered, bounded, internally scrollable. | Replace | `BETA-REQ-0124` | 1.0.0 | UI/UX Contract §4; relationship selectors; async query service; accessibility; LLD | Assigned LLD detail | Beta retains themed bounded internal scrolling while adding deliberate query/browse triggers, stale-result suppression, accessible keyboard/touch semantics, viewport positioning, and explicit entity creation. |
| `ALPHA:UX-003` | Merged `main` | Narrow layouts reflow controls, preserve horizontal table access, center empty states, prevent overflow, and use icon/tool-tip actions when needed. | Replace | `BETA-REQ-0125` | 1.0.0 | UI/UX Contract §§5–6; Workbench Contract; responsive shell; deliberate-confirmation registry; LLD | Assigned LLD detail | Beta preserves complete capability across responsive panes, table-owned scrolling, viewport-safe overlays and empty states, accessible icon labelling, and allowlisted three-second deliberate holds including Device Reference promotion. |
| `ALPHA:UX-004` | Merged `main` | Typography/status badges/semantic colors/focus/dialogs/motion use shared accessible tokens and never rely only on color/blinking. | Replace | `BETA-REQ-0126` | 1.0.0 | UI/UX Contract §§7–8; Branding Contract; design tokens; accessibility; LLD | Assigned LLD detail | Beta defines primitive/semantic tokens, accessible typography/status/focus/dialogs, purposeful reduced motion, and a restrained game-inspired language subordinate to operational truth. |
| `ALPHA:UX-005` | Merged `main` | Editable workflows use consistent draft protection, selective save/discard, meaningful undo, reload/navigation confirmation. | Replace | `BETA-REQ-0127` | 1.0.0 | UI/UX Contract §9; draft/recovery/conflict services; all editors; LLD | Assigned LLD detail | Beta distinguishes unsaved UI working copies from persistent domain Draft state and accepted evidence, permits selective save only for valid transactions, prevents stale overwrite, and restricts Undo to safe bounded inverses. |
| `ALPHA:UX-006` | Merged `main` | SR dashboards support customer filtering, mail summaries, spare lifecycle summaries, MW context, and expiration/KPI presentation without reintroducing removed `Resolved Status Date`. | Replace | `BETA-REQ-0128` | 1.0.0 | UI/UX Contract §10; Product Contract §5; Communications/Inventory/SLA contracts; Overview; LLD | Assigned LLD detail | Beta derives customer-scoped dashboard summaries from owning domains, separates individual duration/cohort SLA/main-view timing, and permanently excludes or reconstructs no Resolved Status Date. |
| `ALPHA:UX-007` | Merged `main` | Spare Request/Fault Tag actions are stage-contextual, support compatible bulk transitions, manual/export flows, and never strand valid lifecycle. | Replace | `BETA-REQ-0129` | 1.0.0 | UI/UX Contract §11; Inventory Lifecycle Contract §13; action registry; batch service; LLD | Assigned LLD detail | Beta derives actions from accepted lifecycle projections, preflights and partitions bulk targets, preserves manual continuation without evidence, separates export from transition, and exposes remediation for every nonterminal blocker. |
| `ALPHA:UX-008` | Merged `main` | Pixel-accurate layouts/export artifacts require approved sanitized fixtures rather than guessed legacy screenshots. | Replace | `BETA-REQ-0130` | 1.0.0 | UI/UX Contract §§12–14; fixture catalogue; export contracts; visual regression; acceptance | Assigned LLD detail | Beta classifies directional references, visual acceptance fixtures, and export golden fixtures; adopts vSphere only as information-architecture inspiration; requires sanitized versioned deterministic baselines and never guesses or hides regressions. |
| `ALPHA:NFR-001` | Merged `main` | Foreign keys enabled on every app connection; reverse FK indexes support normal joins/deletes. | Replace | `BETA-REQ-0131` | 1.0.0 | Foundation Runtime Contract §2; persistence LLD; schema verification | Assigned LLD detail | Beta governs one verified connection factory, all authoritative workflows, effective leading-prefix index coverage, isolated rebuild exceptions, and post-migration/restore integrity checks. |
| `ALPHA:NFR-002` | Merged `main` | Migration + ledger commit atomically or roll back together; concurrent startup does not apply a migration twice. | Replace | `BETA-REQ-0132` | 1.0.0 | Foundation Runtime Contract §3; migration/startup LLD; failure injection | Assigned LLD detail | Beta adds exclusive data-instance migration ownership, state reread after lock, safe cancellation/retry, no readiness before integrity, and deterministic concurrency/failure testing. |
| `ALPHA:NFR-003` | Merged `main` | Migrations accepted into `main` are checksum-verified and immutable. Branch-only candidates may be corrected/consolidated until explicit merge approval, when names/order/content/checksums freeze. Never alter a ledger checksum to hide drift. | Replace | `BETA-REQ-0133` | 1.0.0 | Foundation Runtime Contract §4; migration manifest; repository policy | Assigned LLD detail | Beta preserves the branch/accepted boundary and defines canonical cross-Windows bytes, manifest/ledger comparison, forward-only correction, drift classifications, and honest checksum limits. |
| `ALPHA:NFR-004` | Merged `main` | Migration status is read-only and does not create a missing DB/sidecars. | Replace | `BETA-REQ-0134` | 1.0.0 | Foundation Runtime Contract §5; status command; tests | Assigned LLD detail | Beta makes status completely observational for missing, invalid, offline, and live targets and requires filesystem before/after proof. |
| `ALPHA:NFR-005` | Merged `main` | Meaningful state/identity/relationship changes go to application audit; business lifecycle evidence remains separate. | Replace | `BETA-REQ-0135` | 1.0.0 | Foundation Runtime Contract §§1 and 6; domain contracts; Audit LLD | Assigned LLD detail | Beta defines four separate authorities, atomic mutation/event/audit behavior, exact corrections, projection boundaries, and minimized surviving deletion evidence. |
| `ALPHA:NFR-006` | Merged `main` | Draft payloads/local settings are valid JSON. | Replace | `BETA-REQ-0136` | 1.0.0 | Foundation Runtime Contract §7; JSON/settings/draft LLD | Assigned LLD detail | Beta replaces syntax-only validity with typed versioned schemas, bounds, compatibility, atomic upgrade/write, UI-working-copy separation, and data minimization. |
| `ALPHA:NFR-007` | Merged `main` | Automated tests cover identifiers/hierarchy/immutability/lifecycle/referential integrity/migrations/schema integrity. | Replace | `BETA-REQ-0137` | 1.0.0 | Foundation Runtime Contract §8; test strategy; all LLDs | Assigned LLD detail | Beta converts broad coverage words into traced invariant/failure-path proof and explicitly tests Infrastructure Device workbook discovery-export/round-trip contracts. |
| `ALPHA:NFR-008` | Merged `main` | CI runs supported suite on Python 3.13/3.14 Windows; draft PR updates may skip, non-draft revisions and main pushes validate. | Replace | `BETA-REQ-0138` | 1.0.0 | Foundation Runtime Contract §9; CI/release LLD; branch protection | Assigned LLD detail | Beta preserves the matrix while defining draft minimums, current-head invalidation, required protected/release runs, reproducibility, isolated synthetic data, redaction, and desktop supplementation. |
| `ALPHA:NFR-009` | Merged `main` | Application acceptance tests cover keyboard/pointer/responsive/focus/reduced-motion/semantic labels/destructive confirmation. | Replace | `BETA-REQ-0139` | 1.0.0 | Foundation Runtime Contract §10; UI/UX Contract; acceptance suite | Assigned LLD detail | Beta adds hovered-pane scroll ownership, tiered three-second hold testing, accessible deliberate alternatives, proposal review, full UI states, and governed manual inspection. |
| `ALPHA:NFR-010` | Merged `main` | Local startup migrates before serving, loopback-only, prevents duplicate instances, authenticates shutdown, validates exact registry URL/PID birth/health, bypasses proxies/redirects. | Replace | `BETA-REQ-0140` | 1.0.0 | Foundation Runtime Contract §11; launcher/runtime/security LLD | Assigned LLD detail | Beta defines canonical data-instance ownership, per-run identity, exact process/birth/health proof, host/origin protection, proxy/redirect rejection, safe stale-registry recovery, and idempotent shutdown. |
| `ALPHA:NFR-011` | Merged `main` | Technical diagnostics live outside SQLite with UTC/severity/component/event/run-id/context/traceback and fail-open behavior. | Replace | `BETA-REQ-0141` | 1.0.0 | Foundation Runtime Contract §12; Diagnostics LLD | Assigned LLD detail | Beta adds structured versioning, monotonic durations, owned bounded rotation/queues, crash usefulness, no automatic upload, and qualifies fail-open so primary safety failures remain failures. |
| `ALPHA:NFR-012` | Merged `main` | Diagnostics redact credentials/tokens/password/body-like values; pattern redaction is defense in depth and does not permit deliberate sensitive logging. | Replace | `BETA-REQ-0142` | 1.0.0 | Foundation Runtime Contract §13; Security/Diagnostics LLD | Assigned LLD detail | Beta makes prevention/minimization primary, adds semantic allowlists and output-wide pre-queue sanitization, defines safe redactor failure, and requires canary non-disclosure tests. |
| `ALPHA:NFR-013` | Merged `main` | `audit_events` is append-only: update/delete/replace/conflict-skip existing rows are forbidden through normal SQL; duplicate event ID is error. | Replace | `BETA-REQ-0143` | 1.0.0 | Foundation Runtime Contract §14; Audit/Persistence LLD | Assigned LLD detail | Beta closes direct and indirect SQL bypasses, separates command idempotency, makes audit transactional with mutation, preserves correction/history/restore, and states the local-file tamper boundary honestly. |
| `ALPHA:NFR-014` | Merged `main` | Audit JSON is action-specific/minimal/allowlisted and excludes credentials, secrets, full mail/note bodies, uncontrolled provider payloads, arbitrary exceptions, whole-object copies. | Replace | `BETA-REQ-0144` | 1.0.0 | Foundation Runtime Contract §15; Audit LLD; History/export acceptance | Assigned LLD detail | Beta establishes closed versioned action schemas, relational core facts, transactional validation, minimal changes/provenance, permanent historical readers, and per-action canary fixtures. |

## Unmerged proposal checkpoint — 23 of 43 reviewed

These Alpha proposal rows were never merged authority. Their source statements are preserved exactly; the recorded Beta decision is normative.

| Proposal | Exact Alpha proposal statement | Decision | Beta mapping |
|---|---|---|---|
| `ALPHA-PROP:OPS-008` | One SR may have unlimited terminal MW history but at most one unfinished MW across deliberate direct SR↔MW links and deterministic RFC/WFM-derived reachability. Derived context is not duplicated merely to enforce the rule. | Replace | `BETA-REQ-0045` |
| `ALPHA-PROP:OPS-013` | Due completed/incomplete MW attempts are surfaced for operator review. Review completes only through explicit operator confirmation that records the selected outcome and `reviewed_at`; outcome correction preserves append-only evidence. | Replace | `BETA-REQ-0050` |
| `ALPHA-PROP:OPS-027` | Product Line SLA replaces the obsolete severity-only age-risk schedule. Separate general suspension-warning defaults remain Critical 3, Major 10, Minor 15, and Non-fault inquiry 30 cumulative source-reported days; they are warnings, never SLA, retention, or extension limits. | Replace | `BETA-REQ-0064`, `BETA-REQ-0065` |
| `ALPHA-PROP:OPS-032` | Approaching `Suspend Planned End Date` produces a distinct alert. SLA milestone state, source Resolve By guidance, cumulative-suspension warnings, suspension-ending alerts, retention, and source-removal lifecycle are separate concepts and are never collapsed into one deadline. | Replace | `BETA-REQ-0070` |
| `ALPHA-PROP:OPS-035` | Editable migration `002_phase1_application.sql` owns Service Request import/report/retention persistence without the rejected age-risk fields. Editable `003` owns reference/logistics plus Product Lines and SR/profile Product Line relationships. Editable `004` owns Phase 1E operations. All refreeze together only at branch acceptance; development databases are recreated. | Replace | `BETA-REQ-0073` |
| `ALPHA-PROP:OPS-041` | RFC and WFM imports discover the newest stable eligible files in the same user-configurable directory (default Downloads) using separate configurable filename patterns and optional Windows/browser ` (n)` collision suffixes. Suffix numbers are not chronology and may reset after Downloads cleanup. WFM uses its embedded export timestamp; the timestamp-less RFC source uses stable modification time with deterministic tie-breaking. A malformed/unsafe newest candidate fails the invocation without fallback to an older file. | Replace | `BETA-REQ-0145` |
| `ALPHA-PROP:OPS-042` | RFC/WFM workbooks are untrusted operational observations, not complete-database replacements. Validation, normalization, logical fingerprinting, diff, and warnings complete before write ownership. Accepted source high-water marks are independent; exact replay is a no-op and older replay fails closed absent an explicit recovery workflow. | Replace | `BETA-REQ-0146` |
| `ALPHA-PROP:OPS-043` | RFC source absence produces a bounded review warning only. It never archives/closes an RFC or changes links/WFMs/MWs. **Stop tracking** is a separate audited local state; later imports continue safe source/provenance refresh while routine tracking alerts are suppressed, and reappearance only proposes resuming explicit tracking. | Replace | `BETA-REQ-0147` |
| `ALPHA-PROP:OPS-044` | RFCs/WFMs may be registered manually and later adopted in place by matching canonical ticket number. `ever_imported` is irreversible; imported ticket identity and source-owned current facts cannot be manually overwritten. Source Create Time fills `external_created_at`; a later conflict requires review. | Replace | `BETA-REQ-0148` |
| `ALPHA-PROP:OPS-045` | RFC status matching is case-insensitive. Accepted `Closed` and `Cancelled` source evidence is irreversible and cannot switch terminal meaning. RFC progression cannot regress after `Implement`; post-`Implement` cancellation is a high-risk terminal conflict. Terminal evidence and its pending cascade proposal commit atomically but do not mutate local lifecycle before confirmation. Unknown nonblank status warns without destructive authority. | Replace | `BETA-REQ-0149` |
| `ALPHA-PROP:OPS-046` | Only an RFC in accepted `Implement` status may generate or accept nonterminal WFM work. Accepting the exact WFM import preview may atomically create a missing RFC or promote a referenced pre-`Implement` RFC to `Implement`; the preview shows every promotion and never fabricates a Service Request. A provider `Complete` row may instead be accepted as terminal historical evidence under an already-terminal RFC without promotion/reactivation. Later RFC source import cannot regress that state. | Replace | `BETA-REQ-0150` |
| `ALPHA-PROP:OPS-047` | RFC Summary is the primary source of bounded standalone eight-digit SR candidates; WFM Task Name is fallback only when Summary yields none. Every existing match is proposed and warned for review, no missing SR is created, subordinate candidates resolve to the master-only direct relationship, and later omission never silently unlinks history. | Replace | `BETA-REQ-0151` |
| `ALPHA-PROP:OPS-048` | Every ungrouped RFC is a master. One group has exactly one master and direct subordinates only; a subordinate cannot own another subordinate and must share its master's Customer Organization. Provider files never infer hierarchy. A used RFC cannot be reparented after WFM history; before history, manual/adopted ownership correction is reviewed and audited. The default maximum is 20 direct subordinates; a higher configured ceiling requires a prominent warning and confirmation. Hard deletion is forbidden. | Replace | `BETA-REQ-0152` |
| `ALPHA-PROP:OPS-049` | A master RFC archive may cascade only archival state to eligible active subordinates and is blocked by active WFM work anywhere in the affected set. It never cascades to SRs/WFMs/MWs or rewrites provider status. Exact cascade membership is persisted so restoration affects only eligible children archived by that operation. | Replace | `BETA-REQ-0153` |
| `ALPHA-PROP:OPS-050` | SR terminality never closes an RFC. New RFC links to pending/terminal SRs require explicit confirmation; finalized/purged episodes reject new links. Unlink is an audited reasoned correction, preserves execution history, and may clean archived relationships. Duplicate link/absent unlink requests are no-ops. | Replace | `BETA-REQ-0154` |
| `ALPHA-PROP:OPS-051` | WFM may be manually registered under an eligible RFC or while creating/planning an MW, then adopted by a matching import. Before any MW/execution history, adoption may fill blanks and propose RFC reassignment; afterward accepted identity, ownership, and attempt evidence are immutable. `Complete` may be adopted as terminal historical evidence, including under an already-terminal RFC, and creates its retention anchor without RFC reactivation. A `Plan Cancel` source row is informational only: it cannot create/update/terminate a WFM, bootstrap/promote an RFC, or cascade into an MW. | Replace | `BETA-REQ-0155` |
| `ALPHA-PROP:OPS-052` | WFM source planned start/end are immutable attempt facts separate from the MW envelope. Both may be blank; otherwise both must be usable. Source local time is `America/Guayaquil`, persistence is canonical UTC whole seconds, boundaries are half-hour, and end is later than start. A half-present/malformed pair rejects the workbook. The same Task No is never rescheduled; cancellation/retry uses a new Task No. | Replace | `BETA-REQ-0156` |
| `ALPHA-PROP:OPS-053` | Automatic MW grouping is a previewed proposal. Eligible WFM intervals are partitioned by resolved Customer Organization and RFC master hierarchy, then grouped by transitive overlap; exactly touching intervals count as continuous. Different unknown-customer hierarchies stay separate. The envelope is the minimum start through maximum end. An operator may explicitly merge reviewed work groups, including multiple customers, into one MW. | Replace | `BETA-REQ-0157` |
| `ALPHA-PROP:OPS-054` | Automatic regrouping never commits silently. The operator reviews one exact diff containing customer/work-package partitions, envelope, members, relationships, warnings, and conflicts. It may propose split/merge/membership changes only for unfinished MWs, never reschedules a WFM, and never rewrites terminal attempts. Rejected exact proposals retain only a minimized fingerprint decision; newer source invalidates it. | Replace | `BETA-REQ-0158` |
| `ALPHA-PROP:OPS-055` | This correction branch keeps `001` frozen and deliberately rewrites editable `002`–`004` against an empty local database. The three candidates refreeze together only when the owner accepts the branch for merge; no compatibility with their discarded development checksums is claimed. | Replace | `BETA-REQ-0073`, `BETA-REQ-0132`, `BETA-REQ-0133` |
| `ALPHA-PROP:OPS-056` | RFC workbook required headers are `Task ID`, `Summary`, `Status`, `Create Time`, `Creator`, `L1 Handler Name`, `L2 Handler Name`, and `Customer Account Name`; only Task ID must be usable per row. The RFC organization value may be blank but blocks automatic grouping. WFM fixed recognized columns include RFC No., Task No., Task Status, planned start/end, customer, Task Name, and Description; only RFC No. and Task No. must be usable, except planning is blank-or-valid-pair. Header recognition uses explicit normalized aliases, never fuzzy matching. | Replace | `BETA-REQ-0159` |
| `ALPHA-PROP:OPS-057` | Repeated RFC No. across different WFM Task Nos is valid. Duplicate RFC Task ID, duplicate WFM Task No., or an identity-less row inside the recognized logical matrix rejects the complete workbook. Parsing ignores content outside the matrix and enforces 25 MiB compressed, 200 MiB expanded, 10,000 meaningful rows, 256 columns, 32,767 source-cell characters, and 500 displayed preview/warning items per category. | Replace | `BETA-REQ-0160` |
| `ALPHA-PROP:OPS-058` | A confirmed terminal subordinate permanently terminates that RFC and its active WFMs. A confirmed terminal master atomically terminates the master, all direct subordinates, and active WFMs throughout the two-level hierarchy. Cascades are all-or-nothing; contradictory newer active-subordinate evidence or changed RFC/WFM/MW membership blocks acceptance. Candidate `004` rejects unrelated proposal members at capture and revalidates relationships at confirmation. Pending terminal confirmation persists across restart. Provider WFM Task Status is preserved while SOMA records local provenance. | Replace | `BETA-REQ-0161` |
| `ALPHA-PROP:OPS-059` | An `in_progress` MW losing its last executable WFM/standalone task becomes `incomplete`, preserves scope-loss evidence, and awaits review. If any executable work survives, it continues with warning; its interval freezes while current Guayaquil time is inside it and may otherwise be recalculated. Only a never-started empty shell without any surviving relationship/evidence may be deleted. | Replace | `BETA-REQ-0162` |
| `ALPHA-PROP:OPS-060` | WFM has no independent master flag. Within an MW, master-side/subordinate-side role is derived exclusively from its RFC hierarchy. No provider field, global `is_master`, or separate `master_wfm_id` becomes another authority. | Replace | `BETA-REQ-0163` |
| `ALPHA-PROP:OPS-061` | RFC, WFM, and MW retention each default to 180 days, accept independent whole-day values `0..365`, and begin at confirmed terminal time. Active/planned/in-progress records are exempt. Shared 10:00 `America/Guayaquil` housekeeping with startup catch-up postpones protected dependencies, deletes only orphaned communications, and cleans a master hierarchy atomically while preserving minimized compact evidence. | Replace | `BETA-REQ-0072`, `BETA-REQ-0077`, `BETA-REQ-0116`, `BETA-REQ-0121` |
| `ALPHA-PROP:OPS-062` | Warning acknowledgement and similar reversible operator actions support only one latest undo; undo history does not stack. Generic draft/undo cannot reverse source-terminal evidence, completed execution history, or another irreversible lifecycle mutation. | Replace | `BETA-REQ-0164` |
| `ALPHA-PROP:OPS-063` | Each RFC belongs to at most one Customer Organization. Imported `Customer Account Name` is its primary source label: blank remains reviewable, a usable WFM organization may propose filling it, and any disagreement is a high-risk conflict. Reviewed linked-SR then Datacenter/Managed Element context are fallback only. Unresolved/ambiguous RFCs cannot enter automatic grouping; master/subordinate ownership must match. Active RFCs cannot target an archived organization, and active nonterminal RFCs block organization archival while terminal/history references may remain. | Replace | `BETA-REQ-0165` |
| `ALPHA-PROP:OPS-064` | An MW may own any number of standalone manual tasks without an RFC, WFM, SR, or fabricated external identifier. Time is inherited from the MW; task name, description, customer, and relationships are optional. | Replace | `BETA-REQ-0166` |
| `ALPHA-PROP:OPS-065` | MW planned and actual execution intervals are distinct. Actual time and completed/incomplete outcome corrections preserve prior values through append-only actor/time/reason evidence. Cancellation may be confirmed before, during, or after the planned interval only when execution did not occur. | Replace | `BETA-REQ-0167` |
| `ALPHA-PROP:OPS-066` | MW display labels are recalculated chronological indices within the Guayaquil month/year of effective execution start—actual when recorded, otherwise planned—including reviewed history and late historical insertions. Unplanned MWs have no ordinal; cross-month work belongs to its effective start month. Opaque IDs and immutable report/audit snapshots never change when indices recalculate. | Replace | `BETA-REQ-0168` |
| `ALPHA-PROP:OPS-067` | Every persisted human/source text field is at most 400 Unicode characters after canonicalization. Manual/source overflow is rejected without truncation. Structural audit/proposal payloads use separate minimized byte/item/depth bounds. RFC/WFM/MW reads default to 100 and reject limits above 500 with deterministic cursor ordering. | Replace | `BETA-REQ-0169` |
| `ALPHA-PROP:OPS-068` | Two WFM Task Nos for one RFC are distinct attempts and cannot share one MW. Overlapping attempts produce separate MW proposals; an unexecuted attempt may later be confirmed cancelled but identities are never collapsed. | Replace | `BETA-REQ-0042`, `BETA-REQ-0170` |
| `ALPHA-PROP:OPS-069` | An imported terminal `Complete` WFM without MW membership and with a usable planned interval produces a reviewable historical-MW proposal. Without a usable interval it remains unassigned terminal evidence; SOMA never fabricates execution time. | Replace | `BETA-REQ-0171` |
| `ALPHA-PROP:OPS-070` | `Plan Cancel` rows are shown as informational ignored rows and participate only in workbook fingerprint/provenance. Existing SOMA WFM/MW state remains untouched, and a missing RFC/WFM is not created. | Replace | `BETA-REQ-0150`, `BETA-REQ-0155`, `BETA-REQ-0167` |
| `ALPHA-PROP:OPS-071` | Source planning is the default MW proposal. A conflicting manual plan is displayed beside it and requires explicit selection. Automatic recalculation stops while current Guayaquil time is inside the accepted interval; planned and actual execution facts remain separate. | Replace | `BETA-REQ-0172` |
| `ALPHA-PROP:OPS-072` | Product and Product Line are distinct. Advanced Search Product remains mutable source description. Product Line is operator-controlled, belongs to one Customer Organization, drives SLA, and must match an assigned SR's organization. Batch assignment is bounded and reviewed. | Replace | `BETA-REQ-0062`, `BETA-REQ-0064`, `BETA-REQ-0065`, `BETA-REQ-0173` |
| `ALPHA-PROP:OPS-073` | First profile setup requires name, username, email, phone, and protected-key metadata. Customer Organization/Product Line setup is strongly recommended but skippable. An unresolved SR remains usable with IT fallback calculations and no fabricated Product Line relationship. | Replace | `BETA-REQ-0065`, `BETA-REQ-0078`, `BETA-REQ-0168`, `BETA-REQ-0173` |
| `ALPHA-PROP:OPS-074` | Built-in IT defaults are Critical 7; Major 15 at 85%/30 at 100%; Minor 45 at 85%/60 at 100%. Built-in NFV defaults are Critical 3, Major 15, Minor 90, all at 100%. Custom Product Lines define optional 85% and required 100% milestones for Critical/Major/Minor. Non-fault inquiry is always `ceil(Minor 100% × 1.5)` and has no 85% tier. | Replace | `BETA-REQ-0064` |
| `ALPHA-PROP:OPS-075` | SLA running duration is continuous elapsed time from effective Report Date minus cumulative source-reported Suspension Duration. Exact fractional days are supported without binary-float arithmetic. SLA state is derived, not persisted; policy/assignment/source changes recalculate immediately, while exported report snapshots remain immutable. | Replace | `BETA-REQ-0174` |
| `ALPHA-PROP:OPS-076` | Monthly SLA cohorts are formed by non-cancelled SRs opened in the selected calendar month using effective Report Date. Terminal SRs are measured at first accepted terminal observation; active SRs remain pending or breached. Weekly/selectable-range reports are progress snapshots of monthly cohorts and distinguish pending, met, at-risk, and breached populations. | Replace | `BETA-REQ-0175` |
| `ALPHA-PROP:OPS-077` | Every RFC/WFM import requires exact-source review. Nonterminal rows may be approved/ignored individually; ignored rows count as present but do not mutate entities. Accepted runs preserve approved/ignored identities. Terminal RFC evidence cannot be ignored individually—the operator rejects the whole import instead. | Replace | `BETA-REQ-0176` |
| `ALPHA-PROP:OPS-078` | Application timestamps are persisted as canonical UTC whole-second instants and displayed/interpreted operationally in `America/Guayaquil` (UTC−05:00). Source-local timestamps are converted at the adapter boundary. | Replace | `BETA-REQ-0177` |
