{"schema":"SOMA-LLD-ALGORITHM-V2","lld_id":"LLD-12","name":"BackupCreationV1","snapshot":["request LLD-01 consistent backup snapshot while authoritative DB uses verified SQLCipher connection","snapshot descriptor freezes source data_instance_id/schema/migration identity and SHA-256","copy snapshot plus exact source 32-byte live DEK only into protected in-process backup packaging context; live DEK is never written plaintext to staging metadata"],"keying":["generate independent random 32-byte backup key for every backup","portable: generate independent 20-byte random recovery secret; derive 32-byte KEK with HKDF-SHA256 using public random 32-byte salt and info SOMA-BACKUP-PORTABLE-KEK-V1","managed: unwrap independent installation managed-backup-root secret from DPAPI; derive per-backup KEK with HKDF-SHA256 using public salt and info SOMA-BACKUP-MANAGED-KEK-V1","wrap random backup key using AES-256-GCM-SIV with random 12-byte nonce and canonical public-header AAD"],"payload":["build encrypted key record containing live DEK + source data-instance identity; encrypt under backup key","stream snapshot in 4 MiB chunks; each chunk gets independently random 12-byte nonce and AAD=(header_digest,index,plaintext_length,record_kind)","build encrypted manifest containing snapshot/payload digests, schema/migration/app identity and bounded file inventory","write complete artifact to same-directory temporary output, never final name"],"self_verify":["re-open temporary artifact from disk","derive/unwrap key using the just-created recovery secret or managed root path","authenticate/decrypt every record and recompute all lengths/digests","extract into isolated verification directory and invoke LLD-01 verify_restored_database with recovered live DEK","only after cryptographic + database/schema/audit integrity success atomically publish final artifact and record verified catalog/audit result"],"portable_secret":"portable recovery secret is returned/displayed exactly once outside the artifact; SOMA never persists it","failure":"temporary/verification files removed best-effort; no success/catalog verified state; no existing backup pruned","rules":["outer backup AEAD is independent of SQLCipher live encryption","including live DEK only inside backup-key-encrypted payload makes portable restore possible without tying backup recovery to source Windows DPAPI","hashes supplement but never replace authenticated encryption"]}