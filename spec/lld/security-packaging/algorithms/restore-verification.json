{"schema":"SOMA-LLD-ALGORITHM-V2","lld_id":"LLD-12","name":"RestoreVerificationAndCommitV1","preconditions":["operator explicitly selects backup artifact and supplies portable recovery secret when kind=portable","restore runs in maintenance/quiescing flow with no ordinary mutation accepted","artifact/source path treated as untrusted and opened without modifying it"],"verify_before_live_mutation":["parse bounded public header/magic/version without trusting sizes/paths","derive portable KEK or unwrap managed root only for recognized kind","authenticate wrapped backup key with header AAD; wrong secret/tamper uses bounded nondisclosing failure","authenticate/decrypt every record/chunk to fresh isolated staging root; reject duplicate/missing/out-of-order indexes, nonce/length/size violations and path traversal","decrypt key record to obtain source live DEK and require source data_instance_id match authenticated header/manifest","authenticate manifest and verify chunk/snapshot SHA-256 plus complete file inventory","open staged database only through SQLCipher provider with recovered live DEK and exact cipher profile","run LLD-01 database restore verification: SQLite integrity/foreign-key/schema/migration/audit protections and supported-origin/future-version checks","construct RestorePreviewV1 and require explicit confirmation bound to backup_id + artifact SHA-256 + target current data_instance revision"],"commit":["freshly revalidate current instance ownership/quiescing state and preview fingerprint","create emergency pre-restore managed backup of current live state when a valid current database exists; it must verify before replacement","ask LLD-01 replace_live_database_from_verified_stage using atomic/recoverable file choreography while holding canonical instance ownership","after replacement re-open restored DB with recovered DEK and rerun cipher + schema/integrity verification","DPAPI-protect recovered DEK to target installation live-data-key envelope only as part of recoverable publication choreography","preserve restored data_instance_id; generate new run/session identities on next readiness","append required restore audit/security evidence in restored database according to LLD-01 recovery transaction design","retain emergency pre-restore backup; normal rotation may consider it only after restored instance is verified ready"],"failure":"before commit: live database/key envelope untouched; during commit: recoverable choreography restores prior verified live files/key envelope or startup remains blocked with emergency backup available","rules":["never decrypt directly over live paths","never accept a future schema by rewriting manifests","portable recovery secret/live DEK/backup key never enter database/audit/diagnostics","restore does not convert Alpha database/backup into supported Beta origin"]}