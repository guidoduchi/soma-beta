{"schema":"SOMA-LLD-ALGORITHM-V2","lld_id":"LLD-09","name":"MsgDraftGenerationV1","input":["originating domain/command/target identity","versioned template id/version","subject/body snapshot","ordered TO/CC/BCC recipient snapshot","operator-selected destination supplied outside URL"],"steps":["validate bounded recipients/content/destination and compute content_fingerprint from StrictVersionedJson semantic input","build IPM.Note bytes with msgforge 1.0.0 outside DB transaction; sent flag false; no send API/COM/Outlook invocation","verify CFB/OXMSG structure with independent verifier and compute final artifact SHA-256/byte length","publish the verified file atomically to the operator-selected destination; never overwrite a protected PST/OST source path","inside one short LLD-01 UnitOfWork resolve committed replay/idempotency and reconcile any already-published artifact with the same semantic draft key/hash","insert one command_receipt before receipt-FK children, then insert/reuse immutable msg_draft identity by origin_command_id + content_fingerprint, ordered recipient snapshot, export metadata, audit/results and commit","if publication fails no DB draft/export mutation occurs; if publication succeeds but DB commit fails, retry with the same command/semantic input verifies the published artifact hash and records/reuses the same semantic draft without creating sent Communication","later PST/OST discovery follows normal CanonicalMessageIdentityV1; reviewed reconciliation may set reconciled_communication_id but draft identity remains distinct"],"rules":["draft/export never changes source coverage/high-water or communication sent counts","artifact filename/path never proves sending","draft bodies are local authored evidence, not provider source evidence","the final authoritative DB mutation uses exactly one short transaction after artifact verification/publication; there is no earlier draft-provenance transaction"],"acceptance_test_ids":["LLD09-A036","LLD09-A037","LLD09-A038","LLD09-F010","LLD09-P006","LLD09-C001"]}