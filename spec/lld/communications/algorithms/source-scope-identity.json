{"schema":"SOMA-LLD-ALGORITHM-V2","lld_id":"LLD-09","name":"SourceScopeIdentityV1","steps":["probe candidate PST/OST through CommunicationSourceAdapter in read-only mode before any authoritative mutation","derive provider account/mailbox identity candidate when adapter exposes stable account/store metadata; normalize with adapter-versioned rule","compare candidate against existing source scopes by adapter_family + accepted scope identity, never by filesystem path/name alone","if exact accepted scope identity exists, offer reviewed location/folder update to that same source_scope_id","if provider identity is unavailable, require explicit operator confirmation of a new OPERATOR_CONFIRMED_LOCAL_SCOPE identity; generate immutable random scope identity independent of path","if overlapping/replacement store may represent an existing scope but evidence is ambiguous, return COMM_SOURCE_SCOPE_CONFLICT and require explicit reconciliation rather than share message identity/coverage","persist configuration only through ConfigureSourceScope and append source-scope event"],"invariants":["rename/move/remount of same confirmed store cannot create a second logical scope","similar filenames/paths cannot merge different mailboxes","source location may change without changing source_scope_id or canonical communication identities","adapter version change never silently reinterprets prior provider checkpoint or identity bytes"],"acceptance_test_ids":["LLD09-A001","LLD09-A002","LLD09-A003","LLD09-F001"]}