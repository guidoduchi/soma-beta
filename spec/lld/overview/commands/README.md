# LLD-11 command surface

LLD-11 Overview owns no authoritative mutation commands. This directory exists only to satisfy the common LLD packet layout. Navigation to mutations or report generation invokes the owning packet directly; Overview remains a read-only projection layer.
