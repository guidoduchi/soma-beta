{
  "schema":"SOMA-LLD-COMMANDS-V1",
  "lld_id":"LLD-07",
  "commands":[
    {"name":"AcceptInventoryProposal","input":["proposal_id","revision","input_fingerprint","selected target ids or all eligible","explicit confirmation when risk requires","command_id"],"steps":["outside lock display exact source evidence reference, target partition and proposed effects","inside one UoW re-read proposal/targets and every owning Inventory projection/dependency","reject stale/incompatible target rather than coercing; material final decisions require explicit confirmation","insert one receipt and optional batch; dispatch each target to its owning Inventory mutation reducer without nested commit/receipt","append one independent lifecycle/result event per accepted target","transition proposal accepted only for exact accepted scope; preserve independently rejected/deferred partitions as separate dispositions when applicable","rebuild projections/audit and commit"],"rules":["communication evidence never becomes authority by itself","no message body copied into Inventory"]},
    {"name":"RejectInventoryProposal","input":["proposal_id","revision","reason","command_id"],"steps":["inside UoW verify exact pending proposal","insert receipt; transition rejected and audit source/target refs","commit"],"rule":"rejection mutates no Inventory lifecycle entity"},
    {"name":"AcceptInventoryBulkAction","input":["exact preview fingerprint","action kind","eligible target expected revisions","required confirmation/context","command_id"],"steps":["inside one UoW revalidate all intended eligible targets and exact material fingerprint","if any intended target changed or became incompatible abort whole selected mutation","insert receipt and inventory_lifecycle_batch","append one independently addressable lifecycle event/result per target","rebuild each projection, audit batch+targets and commit"],"rules":["atomic across selected compatible scope","correcting one later target does not affect other batch members"]},
    {"name":"CorrectInventoryEvidence","input":["target event/relationship identity","exact current-effective revision","replacement or suppression semantics","reason","command_id"],"steps":["outside lock classify correction type: false event, relationship correction, alias correction, material submitted correction, or genuine later development","reject genuine later development from correction path","inside UoW prove exact disputed fact and all affected dependencies","insert receipt and append targeted correction/supersession event; never update/delete original evidence","rebuild only affected current projections and attention","if submitted Fault Tag membership materially changes return REPLACEMENT_REQUIRED rather than mutating it","audit original/correction/result refs and commit"],"rules":["identity-bearing entities never replaced by relationship correction","Task outcome correction remains upstream LLD-05"]},
    {"name":"HardDeleteUntouchedInventoryDraft","input":["preview fingerprint","target revisions","deliberate confirmation","command_id"],"steps":["inside UoW rerun complete dependency classification and require CLEAR","insert receipt","delete only exact untouched draft-owned rows in FK-safe order; never cascade independently surviving entities or exported artifacts","append minimized non-reconstructable deletion audit/result refs and commit"],"rules":["allocator values are never reused","hard deletion is correction for genuinely untouched drafts only, not ordinary history cleanup"]}
  ],
  "pure_preview_queries":[
    {"name":"PreviewInventoryBulkAction","authority":"queries/inventory.json#PreviewInventoryBulkAction","route":"POST /api/v1/inventory/bulk/preview","rule":"side-effect-free stable-Snapshot partition; creates no command receipt, audit or lifecycle evidence"},
    {"name":"PreviewInventoryHardDelete","authority":"queries/inventory.json#PreviewInventoryHardDelete","route":"POST /api/v1/inventory/destructive/preview","rule":"side-effect-free dependency classification; creates no deletion, receipt or audit evidence"}
  ],
  "participant_behavior":{
    "InventoryTaskDependencyProvider":["task hard delete is BLOCKED by accepted allocation history that is not untouched draft-safe, any physical consequence, or other protected Inventory lifecycle evidence","retry preview exposes only current eligible planning relationships selected by operator","retry apply runs in caller LLD-05 UoW and may create new planning allocation/Need relationship references only when unit is not contradictory; it never copies installed/removed/return/Fault Tag/warehouse history"],
    "InventoryProposalTargetService":["LLD-09 supplies exact evidence/proposal context and one outer UoW when orchestrated","LLD-07 revalidates current lifecycle and appends owned events only","no nested commit/receipt"]
  }
}
