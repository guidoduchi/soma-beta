{
  "schema":"SOMA-LLD-COMMANDS-V1",
  "lld_id":"LLD-07",
  "ordering":"Pure normalization, duplicate/compatibility lookup and previews occur before BEGIN IMMEDIATE. Authoritative commands then resolve idempotency, revalidate exact current projections/dependencies, insert one command_receipt, append owned facts/relationships, rebuild affected projections/audit and commit.",
  "commands":[
    {"name":"RegisterDevicePartUnit","input":["service_request_id","device_reference_id","bom_code","optional serial/slot/condition/effective time","command_id"],"steps":["normalize BOM/serial outside lock and show duplicate candidates","inside UoW revalidate SR↔Device Reference operational context","allocate immutable device_part_unit_id and stable creation_sequence","insert receipt/unit/registered event and optional fault_observed event","run NEED_AGGREGATION_V1 for same SR+BOM: create active Need if absent or add contributor to existing active Need without changing planned_quantity","rebuild Device Part/Need projections and audit/commit"],"rules":["matching serial/BOM never silently merges identities","fault registration may aggregate Need automatically","no Infrastructure registration required"]},
    {"name":"SetSpareNeedPlannedQuantity","input":["spare_need_id","base revision","positive whole quantity","reason","command_id"],"steps":["inside UoW require current Need and exact revision","insert receipt/planned_quantity_changed event","rebuild Need projection only; contributor count unchanged","audit/commit"],"rule":"planned quantity is independent of contributor/request/local quantities"},
    {"name":"ChangeSpareNeedLifecycle","input":["spare_need_id","base revision","action resolve|cancel|reactivate|history_remove","reason","command_id"],"steps":["inside UoW revalidate active/nonactive key and request dependencies","history_remove requires every associated request terminal cancelled/rejected and preserves allocations/history","reactivate requires no competing active SR+BOM Need; restore active key","insert receipt/lifecycle event, update active key/rebuild projection/audit/commit"],"rules":["fulfillment never automatically resolves Need","history removal is not hard delete"]},
    {"name":"RegisterSparePartUnit","input":["origin manual_local|legacy|reviewed_reconciliation","bom","optional serial/condition/location/custody/origin RMA","command_id"],"steps":["normalize keys and show duplicate candidates","inside UoW revalidate optional RMA provenance and no identity adoption conflict","allocate unit id and LSU when required","insert receipt/unit/registered lifecycle event","rebuild unit projection/Stock eligibility and audit/commit"],"rules":["SR7/C10/serial are provenance/candidate evidence only","local unit without official provenance is valid"]},
    {"name":"ReserveSparePartUnitForTask","input":["task_id","spare_part_unit_id","optional spare_need_id","unit/task/current allocation revisions","command_id"],"steps":["outside lock preview Stock eligibility","inside UoW revalidate Task exists, unit current Stock eligibility, no active allocation/installation/return conflict and optional Need relation","insert receipt/reserve allocation event/current row","rebuild unit Stock projection and audit/commit"],"rules":["one active Task allocation per unit","reservation is planning only and never proves use/installation or changes Task lifecycle"]},
    {"name":"ReleaseSparePartUnitReservation","input":["allocation_id","revision","reason","command_id"],"steps":["inside UoW require exact active planning relationship and no physical consequence that depends on it as immutable history","insert receipt/release event/remove current allocation","rebuild unit Stock eligibility/audit/commit"],"rule":"historical allocation event remains preserved"},
    {"name":"RecordLocalNeedFulfillmentSelection","input":["need_id","unit_id","optional task_id","command_id"],"steps":["inside UoW revalidate Need active and unit available/compatible","insert receipt/local selected event","optionally reserve through same Task allocation semantics","rebuild Need quantity summary and unit projection/audit/commit"],"rule":"selection does not consume/resolve Need automatically"}
  ]
}
