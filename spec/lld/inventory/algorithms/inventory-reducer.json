{
  "schema":"SOMA-LLD-ALGORITHM-V1",
  "lld_id":"LLD-07",
  "name":"INVENTORY_REDUCER_V1",
  "principle":"Current state is a deterministic reduction of accepted append-oriented Inventory facts. Projection rows are transactionally maintained caches and never independent editable truth.",
  "reducers":[
    {"target":"SpareNeed","inputs":["lifecycle events","active contributor relationships","draft/request allocation summaries","local fulfillment selections"],"output":"lifecycle_state, planned_quantity, contributor_count, attention/dependency flags","rules":["allocations/fulfillment never auto-resolve","reactivate restores active key only if unique"]},
    {"target":"SpareRequest","inputs":["submission correction-effective state","SR7 aliases","RMA count","request lifecycle events"],"output":"draft/submitted_awaiting_response/acknowledged/partially_authorized/authorized/cancelled/rejected plus warning chronology/current effective submission snapshot","rules":["response timer begins only from current-effective accepted submission","authorization is quantity-relative to immutable submitted quantity; partial remains explicit","false-submission correction clears current effective snapshot without deleting historical snapshot"]},
    {"target":"SparePartUnit","inputs":["unit lifecycle events","active allocation","physical consequences","return obligation/Fault Tag/logistics facts"],"output":"condition/disposition/location/custody and Stock eligibility","rules":["latest accepted current-effective targeted facts reduce by dimension","correction supersedes targeted fact without deleting it"]},
    {"target":"RmaReturnObligation","inputs":["return selection events","Fault Tag membership events","warehouse final decisions"],"output":"not_established/open/closed_accepted plus selected typed unit","rules":["warehouse receipt has no closing authority","warehouse rejection/reopen preserves selected unit and open obligation"]},
    {"target":"RmaLifecycle","inputs":["current assignment","direct inbound relationship","actual logistics","physical consequence/current return obligation","active Fault Tag membership and warehouse state"],"output":"promised/dispatched/received/physical_consequence_pending/return_open/fault_tagged/warehouse_received/closed_accepted/return_rejected plus current relation pointers","rules":["state is derived only","partial request progress never forces sibling RMAs","accepted final warehouse decision is the only closure path"]},
    {"target":"FaultTagMembership","inputs":["submission/warehouse/correction events"],"output":"draft/submitted_awaiting_receipt/warehouse_received/accepted/rejected/cancelled/superseded and active_submitted","rules":["accepted/rejected deactivate active-submitted exclusivity","corrected false submission returns to draft only through exact correction chain"]},
    {"target":"FaultTag","inputs":["current-effective tag lifecycle","all submitted membership projections","archive events"],"output":"draft/submitted/in_warehouse_review/terminal_completed/terminal_with_rejected/cancelled/superseded, current effective submission snapshot and counts","rules":["terminal only when every submitted membership final/cancelled/superseded","rejected members keep unresolved-obligation attention","false-submission correction clears current effective snapshot but preserves historical snapshots"]}
  ],
  "correction_order":"For each reducible dimension, follow exact target_event/relationship correction chain; reject cycles/multiple competing current-effective corrections. Genuine later operational events are ordered by accepted domain chronology and recording identity, never reclassified as corrections.",
  "attention_authority":{"attention_id":"attention_id is SHA-256 of canonical JSON {schema:'SOMA_INVENTORY_ATTENTION_KEY_V1',target_kind,target_id,attention_kind}; one current target/kind condition has one stable identity.","command_cache":"Every Inventory command rebuilds only its affected command-detectable attention target scope in the same writer UnitOfWork: delete obsolete rows and upsert currently true rows before commit. Unbounded global rebuilds are forbidden and projection failure rolls back the authoritative mutation.","derived_only_kinds":["spare_request_response_overdue","task_outcome_consequence_pending"],"derived_only_rule":"These conditions can become true without an Inventory command, so Beta 1.0 InventoryAttentionQuery derives them read-only from one stable Snapshot and they are not treated as persisted lifecycle authority. Pure query evaluation performs no write.","response_overdue_rule":"spare_request_response_overdue is true exactly when the current effective Spare Request lifecycle_state is submitted_awaiting_response, response_warning_start_utc is non-null, and as_of_utc-response_warning_start_utc >= 86400 whole seconds. It is false at 86399 seconds and true at exactly 86400 seconds.","task_review_rule":"task_outcome_consequence_pending is true when a current accepted physical consequence exists and TaskOperationalEvidenceReader.review_fingerprint(snapshot,task_id) differs from that consequence's stored task_review_fingerprint. An indeterminate/inconsistent upstream review fails closed; Inventory never rewrites Task history."},
  "commit_rule":"Command appends facts and rebuilds every affected command-detectable projection/attention cache row before commit; projection failure rolls back authoritative mutation. Time-driven and upstream-owner-driven attention uses the read-only overlay rules above."
}
