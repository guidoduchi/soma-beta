{
  "schema":"SOMA-LLD-TESTS-V1",
  "lld_id":"LLD-07",
  "suite":"failure_injection",
  "cases":[
    {"id":"LLD07-FI001","inject":"tracking allocator write fails after candidate identity generated","expect":"no entity/event/receipt persisted; retry may allocate correctly without duplicate handle"},
    {"id":"LLD07-FI002","inject":"Need creation succeeds in-memory but active-key insert constraint fails","expect":"Device Part/Need/contributor mutation fully rolls back"},
    {"id":"LLD07-FI003","inject":"contributor insert fails after Device Part event","expect":"entire RegisterDevicePartUnit UoW rolls back; no mismatched contributor_count"},
    {"id":"LLD07-FI004","inject":"projection rebuild fails after Need lifecycle event","expect":"event and active-key mutation roll back together"},
    {"id":"LLD07-FI005","inject":"second concurrent reservation wins unit first","expect":"loser receives stale/not eligible; no two active allocations"},
    {"id":"LLD07-FI006","inject":"audit write fails during unit reservation","expect":"reservation/current projection/receipt all roll back"},
    {"id":"LLD07-FI007","inject":"Spare Request allocation #3 fails constraint","expect":"request creation and all earlier allocations/SPR allocation roll back atomically"},
    {"id":"LLD07-FI008","inject":"submission snapshot row fails after submission event","expect":"submission event/allocations/response timer all roll back; request remains draft"},
    {"id":"LLD07-FI009","inject":"recipient/location master changes after submission preview","expect":"commit stale/revalidates; no mixed old/new snapshot"},
    {"id":"LLD07-FI010","inject":"SR7 alias uniqueness fails after identifier event insert","expect":"identifier event/current alias mutation rolls back"},
    {"id":"LLD07-FI011","inject":"RMA batch creates first N RMAs then C10 conflict on N+1","expect":"whole authorization batch rolls back including assignments and batch ordinal"},
    {"id":"LLD07-FI012","inject":"RMA auto-assignment target taken concurrently","expect":"batch revalidates/recomputes inside writer lock or fails; never duplicate current target"},
    {"id":"LLD07-FI013","inject":"RMA assignment audit fails","expect":"assignment event/current relationship roll back"},
    {"id":"LLD07-FI014","inject":"physical unit row created but direct RMA inbound relationship insert fails","expect":"unit/receipt/logistics rows all roll back; no orphan fabricated receipt unit"},
    {"id":"LLD07-FI015","inject":"receipt actual BOM mismatch warning projection fails","expect":"receipt authority also rolls back rather than commit without required projection"},
    {"id":"LLD07-FI016","inject":"shared logistics event participant #4 insert fails","expect":"event and all participants roll back"},
    {"id":"LLD07-FI017","inject":"participant correction targets stale relation","expect":"no participant or shared event mutation"},
    {"id":"LLD07-FI018","inject":"Task review fingerprint changes after consequence preview","expect":"TASK_REVIEW_STALE; no physical consequence/installation/return selection"},
    {"id":"LLD07-FI019","inject":"physical consequence event persists but return-selection validation fails","expect":"same UoW rolls back consequence and any unit/device lifecycle changes"},
    {"id":"LLD07-FI020","inject":"unit lifecycle projection rebuild fails after installed relation","expect":"installation/consequence all roll back"},
    {"id":"LLD07-FI021","inject":"Task correction occurs while Inventory correction preview open","expect":"commit detects changed review fingerprint and requires re-preview"},
    {"id":"LLD07-FI022","inject":"Fault Tag draft membership #N becomes active submitted elsewhere before submission","expect":"entire submission rolls back; no snapshot/member submitted events"},
    {"id":"LLD07-FI023","inject":"Fault Tag snapshot member insert fails mid-submission","expect":"tag/membership submission events and exclusivity rows fully roll back"},
    {"id":"LLD07-FI024","inject":"pickup Dispatch Location changes after preview","expect":"submission re-snapshots exact current reviewed location or fails stale; never mixed snapshots"},
    {"id":"LLD07-FI025","inject":"false-submission correction attempted after warehouse receipt accepted","expect":"blocked; original submitted state remains"},
    {"id":"LLD07-FI026","inject":"warehouse receipt bulk target #3 stale","expect":"whole selected bulk transaction rolls back; no partial receipt events"},
    {"id":"LLD07-FI027","inject":"warehouse final accepted event append succeeds but RMA close event fails","expect":"both roll back; obligation remains open and membership awaiting final"},
    {"id":"LLD07-FI028","inject":"warehouse rejection event append succeeds but active-submitted release fails","expect":"rejection rolls back; no contradictory active state"},
    {"id":"LLD07-FI029","inject":"replacement new tag created but lineage insert fails","expect":"new tag/members and predecessor supersede all roll back"},
    {"id":"LLD07-FI030","inject":"replacement lineage would branch/cycle under concurrent correction","expect":"REPLACEMENT_LINEAGE_CONFLICT and no successor mutation"},
    {"id":"LLD07-FI031","inject":"resend selected obligation was closed/claimed concurrently","expect":"RESEND_NOT_ELIGIBLE; no new resend tag/lineage"},
    {"id":"LLD07-FI032","inject":"proposal evidence reindexed with same source key","expect":"idempotent existing proposal; no duplicate target mutation"},
    {"id":"LLD07-FI033","inject":"proposal target base token stale","expect":"PROPOSAL_STALE; evidence retained, Inventory unchanged"},
    {"id":"LLD07-FI034","inject":"communication participant attempts nested commit","expect":"rejected by UoW contract; outer orchestration rolls back all domain participants"},
    {"id":"LLD07-FI035","inject":"LLD-05 retry participant clone fails on selected second Inventory relationship","expect":"outer retry UoW rolls back new Task and all Inventory clone rows"},
    {"id":"LLD07-FI036","inject":"Task hard-delete dependency provider unavailable","expect":"INDETERMINATE; Task deletion fails closed"},
    {"id":"LLD07-FI037","inject":"bulk preview eligible target changes before commit","expect":"whole selected mutation rejected stale; zero target events"},
    {"id":"LLD07-FI038","inject":"audit fails after final target event in bulk","expect":"batch and every per-target event/projection roll back"},
    {"id":"LLD07-FI039","inject":"hard-delete dependency provider indeterminate","expect":"no deletion"},
    {"id":"LLD07-FI040","inject":"hard-delete FK/dependency changes after preview","expect":"commit revalidation blocks deletion; independent entities untouched"},
    {"id":"LLD07-FI041","inject":"report contributor interrupted mid-stream","expect":"LLD-06 report attempt fails/restarts by its snapshot contract; Inventory authority unchanged"},
    {"id":"LLD07-FI042","inject":"application crash after transaction commit but before UI response","expect":"command_id replay returns committed semantic result without duplicate Inventory events"},
    {"id":"LLD07-FI043","inject":"application crash before transaction commit","expect":"SQLite rollback leaves last committed lifecycle/projections; command safely retryable"},
    {"id":"LLD07-FI044","inject":"projection row intentionally corrupted in test fixture","expect":"rebuild from append-oriented facts restores deterministic state; no new lifecycle evidence"}
  ],
  "rule":"Every injected failure verifies foreign-key integrity, idempotency receipt behavior, absence of partial authoritative mutation and preservation of upstream/downstream ownership boundaries."
}
