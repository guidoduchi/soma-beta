{
  "schema":"SOMA-LLD-ALGORITHM-V1",
  "lld_id":"LLD-08",
  "name":"INFRASTRUCTURE_WORKBOOK_PROFILE_V1",
  "format_id":"SOMA-INFRA-XLSX",
  "version":"1.0",
  "sheets":[
    {"name":"Metadata","headers":["Key","Value"],"required_keys":["FormatId","WorkbookVersion","Mode","SourceInstallationScopeId","GeneratedAtUtc","ExportScopeJson"],"rules":["one row per key","unknown required-format key does not change semantics; unsupported version blocks import","Mode is registration_template|discovery|round_trip"]},
    {"name":"Network Elements","headers":["SomaNetworkElementId","OperationalName","ManufacturerSerial","SomaModelId","ModelName","SomaSiteId","SiteName","SiteAddress","SomaRoomId","RoomName","SomaRackId","RackName","RackHeightU","RackRow","RackColumn","RackUStart","RackUSpan","SomaCloudDeploymentId","CloudTypeName","CloudDeploymentName","SomaParentNetworkElementId","ImportComment"],"identity":"Soma* ids are authoritative targets only when SourceInstallationScopeId equals current installation scope; on foreign scope they are provenance/candidate evidence only","required_for_new":["OperationalName","SiteName","SiteAddress"],"blank_rule":"blank optional existing-target cells preserve prior accepted value/relationship; explicit clearing requires a separately represented reviewed operation and is not encoded by blank"},
    {"name":"IP Addresses","headers":["SomaIpId","SomaNetworkElementId","OperationalName","Address","Primary"],"required":["Address","one resolvable Network Element target"],"rules":["same-installation SomaIpId may target existing IP relation","Primary accepts explicit TRUE/FALSE only; blank preserves existing primary state for existing relation and means non-primary proposal for new relation","missing IP rows never remove existing IPs"]}
  ],
  "generation":["registration_template writes Metadata plus header-only Network Elements/IP Addresses sheets with blank same-installation ids","discovery writes human-readable current rows and same-installation ids for deterministic provenance but is observational","round_trip writes same current data and stable same-installation IDs intended for reviewed updates","no formulas/macros/external links/hidden authoritative sheets/credentials/topology fields","workbook generation never mutates domain state"] ,
  "preflight":["file must pass LLD-08 resource bounds and shared hardened ZIP/path/active-content checks before openpyxl","reject VBA/macro content, formulas in authoritative cells, external links, embedded executable/OLE objects, malformed duplicate metadata keys, unsupported required sheets/headers and forbidden secret/topology headers","source file opened read-only and never rewritten"] ,
  "row_normalization":["strings pass bound + Unicode normalization appropriate to field","IP uses IP_NORMALIZATION_V1","numeric Rack values require exact integers, never spreadsheet float rounding","same-installation local IDs require canonical UUIDv4 syntax when nonblank","foreign local IDs retained only in normalized staging provenance","each normalized row receives SHA-256 row_fingerprint over versioned canonical JSON"] ,
  "logical_fingerprint":["canonical Metadata semantic fields excluding GeneratedAtUtc are encoded with profile/version","normalized Network Elements rows are sorted by same-installation local id when authoritative, else stable row fingerprint with duplicate occurrence ordinal","normalized IP rows likewise sorted; duplicate rows remain multiplicity-significant","SHA-256 over canonical UTF-8 JSON stream yields logical_fingerprint","workbook row order changes alone do not create a different logical content identity"] ,
  "staging_and_review":["parsing writes only unpublished technical run/staging rows","same-installation exact local ID may propose update to exactly that entity after current-state checks","foreign/local-missing identity uses candidate search and can only propose reviewed create/link; descriptive fields never prove target","related Site/Room/Rack/Model/Cloud references use same-installation IDs when valid, otherwise explicit candidate/creation review","Network Element row proposal and its IP rows are grouped by target key for impact display","review partitions creations, updates, unchanged, ambiguity, duplicate candidates, unknown references, invalid relationships, skips and warnings","missing rows and blank optional cells are nondestructive"] ,
  "acceptance":["accepted run revalidates every target/current relation using the same manual Infrastructure services inside one UnitOfWork","cross-Site Rack/Cloud, U occupancy, multiple-primary, containment cycle, archived target, foreign identity overwrite and secret/topology fields fail before mutation","reviewed batch is atomic; one invalid/stale intended accepted target aborts whole acceptance","after success record bounded row decisions/replay index and allow technical staging minimization"]
}
