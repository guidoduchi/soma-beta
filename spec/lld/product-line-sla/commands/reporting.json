{
  "schema":"SOMA-LLD-COMMANDS-V1",
  "lld_id":"LLD-06",
  "commands":[
    {"name":"StartSlaReportGeneration","input":["period_type","period/calendar selection","customer scope all|specific","validated destination request","command_id"],"steps":["resolve Daily/Weekly/Monthly/selected-range boundaries in fixed America/Guayaquil and canonical UTC; validate optional Customer scope and output extension/path","capture canonical as_of_utc","inside short UoW insert receipt/report_attempt state=staging and one durable LLD-01 report job ref containing only bounded attempt/scope/destination tokens","audit start and commit","worker begins REPORT_SNAPSHOT_V1"],"rules":["Objective timezone never changes boundaries","start does not create completed evidence or alter operational state","selected range is a progress/report view over canonical monthly cohorts, not a new contractual cohort definition"]},
    {"name":"StageReportSnapshotBatch","visibility":"internal durable-job writer","input":["report_attempt_id","snapshot_generation_token","batch_ordinal","batch_payload_sha256","expected attempt revision","ordered normalized ReportMemberSnapshotStagingRowV1/ReportMemberTierStagingRowV1/ReportCohortSnapshotStagingRowV1/ReportSectionSnapshotStagingRowV1 arrays","durably assigned per-batch command_id"],"steps":["require at least one row; validate exact section schemas, canonical row ordering, payload hashes and batch_payload_sha256 outside writer lock","inside bounded UoW resolve command receipt first: exact committed command_id/command_type/request_hash replay returns the original ReportSnapshotBatchResultV1 before stale-revision checks; any mismatch is IDEMPOTENCY_CONFLICT","revalidate attempt=staging, expected revision and exact current durable-job snapshot_generation_token","insert one command receipt before any attempt-owned staging row or last_command_id reference","insert only normalized/minimized report snapshot rows produced by the still-open stable accepted-state reader","update exact member/cohort/section counters, attempt revision and last_command_id; construct ReportSnapshotBatchResultV1 and commit atomically"],"rules":["the durable worker assigns one UUID command_id to the exact generation/batch before first invocation and reuses it for every retry of that same batch","COMMAND_ENVELOPE_V1 binds report_attempt_id, generation token, batch ordinal, canonical batch hash, expected revision and normalized row content","staging rows are not CompletedReportEvidence","batch never reads live domain state itself and cannot mutate SR/classification/policy/contributor authority","rollback removes receipt, rows, counters and revision together; a committed replay cannot duplicate rows or counters"]},
    {"name":"SealReportSnapshot","visibility":"internal durable-job transition","input":["report_attempt_id","snapshot_generation_token","exact final member/cohort/section counts","canonical snapshot_hash","expected attempt revision","durably assigned seal command_id"],"steps":["after every registered contributor reaches complete EOF, compute canonical snapshot_hash and exact final counts from the same stable read generation","inside one bounded UoW resolve command receipt first: exact committed command_id/command_type/request_hash replay returns the original ReportAttemptTransitionResultV1 before current state/revision/token checks; any mismatch is IDEMPOTENCY_CONFLICT","revalidate attempt=staging, expected revision and exact current durable-job snapshot_generation_token","insert one command receipt before last_command_id reference","recompute persisted member/tier/cohort/registered-section canonical order and snapshot SHA-256 together with exact report request metadata; recompute exact persisted member/cohort/section counts","require all recomputed values equal the request exactly; any count/order/hash/generation mismatch fails closed with no transition","set snapshot_hash/final counts/state=ready_to_generate/revision/last_command_id, append bounded progress audit, construct ReportAttemptTransitionResultV1 and commit atomically"],"rules":["the durable worker assigns one UUID command_id to the exact seal intent before first invocation and reuses it for every retry","COMMAND_ENVELOPE_V1 binds report_attempt_id, snapshot_generation_token, expected revision, exact final counts and canonical snapshot_hash","stable read snapshot must remain logically complete through contributor EOF and the seal token must identify that exact staging generation","no completed authority exists at seal; staged rows become sealed generation inputs only","rollback removes receipt, seal transition and audit together; exact committed replay cannot reseal or advance revision twice"]}
  ],
  "worker_recovery":["startup resumes report jobs only from committed attempt state and sealed snapshot metadata","staging attempt with interrupted stable-read generation cannot guess completeness; discard all staged child rows through Fail/restart path or rebuild from a new attempt according to durable-job policy","ready_to_generate/generating may regenerate artifact deterministically from sealed snapshot","verifying may reverify an existing candidate only when destination token/candidate identity is exact and safe; otherwise regenerate from sealed snapshot","completed attempts are never regenerated into changed evidence; optional re-export of a completed snapshot is a distinct future/explicit artifact action and does not alter evidence"]
}
