{
  "schema":"SOMA-LLD-SCHEMA-V1",
  "lld_id":"LLD-06",
  "tables":[
    {"name":"sla_report_attempts","columns":["report_attempt_id TEXT PRIMARY KEY","period_type TEXT NOT NULL CHECK(period_type IN ('daily','weekly','monthly','selected_range'))","period_start_utc INTEGER NOT NULL CHECK(period_start_utc>=0)","period_end_utc INTEGER NOT NULL CHECK(period_end_utc>period_start_utc)","period_timezone TEXT NOT NULL CHECK(period_timezone='America/Guayaquil')","scope_kind TEXT NOT NULL CHECK(scope_kind IN ('all_customers','customer'))","customer_org_id TEXT NULL REFERENCES customer_organizations(customer_org_id) ON DELETE RESTRICT","as_of_utc INTEGER NOT NULL CHECK(as_of_utc>=0)","state TEXT NOT NULL CHECK(state IN ('staging','ready_to_generate','generating','verifying','completed','failed','cancelled'))","snapshot_hash TEXT NULL CHECK(snapshot_hash IS NULL OR length(snapshot_hash)=64)","snapshot_member_count INTEGER NOT NULL DEFAULT 0 CHECK(snapshot_member_count>=0)","snapshot_cohort_count INTEGER NOT NULL DEFAULT 0 CHECK(snapshot_cohort_count>=0)","snapshot_section_row_count INTEGER NOT NULL DEFAULT 0 CHECK(snapshot_section_row_count>=0)","artifact_filename TEXT NULL","artifact_sha256 TEXT NULL CHECK(artifact_sha256 IS NULL OR length(artifact_sha256)=64)","artifact_size_bytes INTEGER NULL CHECK(artifact_size_bytes IS NULL OR artifact_size_bytes>=0)","verified_at_utc INTEGER NULL CHECK(verified_at_utc IS NULL OR verified_at_utc>=0)","failure_code TEXT NULL","created_at_utc INTEGER NOT NULL CHECK(created_at_utc>=0)","completed_at_utc INTEGER NULL CHECK(completed_at_utc IS NULL OR completed_at_utc>=0)","revision INTEGER NOT NULL DEFAULT 1 CHECK(revision>0)","created_command_id TEXT NOT NULL REFERENCES command_receipts(command_id) ON DELETE RESTRICT","last_command_id TEXT NOT NULL REFERENCES command_receipts(command_id) ON DELETE RESTRICT","CHECK((scope_kind='all_customers' AND customer_org_id IS NULL) OR (scope_kind='customer' AND customer_org_id IS NOT NULL))"],"rules":["state=completed requires snapshot_hash, verified artifact hash/size/verified_at and completed_at","failed/cancelled attempts retain bounded metadata but their staged snapshot child rows are removed","artifact path is not historical authority; filename/hash/size verify the represented artifact even if operator later moves/deletes it"]},
    {"name":"sla_report_member_snapshots","columns":["report_member_snapshot_id TEXT PRIMARY KEY","report_attempt_id TEXT NOT NULL REFERENCES sla_report_attempts(report_attempt_id) ON DELETE RESTRICT","member_ordinal INTEGER NOT NULL CHECK(member_ordinal>0)","service_request_id TEXT NOT NULL REFERENCES service_requests(service_request_id) ON DELETE RESTRICT","customer_org_id TEXT NULL REFERENCES customer_organizations(customer_org_id) ON DELETE RESTRICT","contract_id TEXT NULL REFERENCES contracts(contract_id) ON DELETE RESTRICT","contract_product_line_id TEXT NULL REFERENCES contract_product_lines(contract_product_line_id) ON DELETE RESTRICT","policy_revision_id TEXT NULL REFERENCES sla_policy_revisions(policy_revision_id) ON DELETE RESTRICT","classification_event_id TEXT NULL REFERENCES sr_classification_events(classification_event_id) ON DELETE RESTRICT","severity TEXT NULL","report_date_utc INTEGER NULL CHECK(report_date_utc IS NULL OR report_date_utc>=0)","status_class TEXT NOT NULL CHECK(status_class IN ('active','resolved','closed','cancelled','unknown'))","endpoint_utc INTEGER NULL CHECK(endpoint_utc IS NULL OR endpoint_utc>=0)","suspension_num INTEGER NOT NULL CHECK(suspension_num>=0)","suspension_den INTEGER NOT NULL CHECK(suspension_den>0)","elapsed_num INTEGER NULL","elapsed_den INTEGER NULL CHECK(elapsed_den IS NULL OR elapsed_den>0)","calculation_state TEXT NOT NULL","sla_input_token TEXT NOT NULL CHECK(length(sla_input_token)=64)","source_report_date_evidence_id TEXT NULL","source_status_evidence_id TEXT NULL","source_suspension_evidence_id TEXT NULL","display_values_json TEXT NULL CHECK(display_values_json IS NULL OR json_valid(display_values_json))"],"rules":["unique report_attempt/member_ordinal and report_attempt/service_request","display_values_json belongs to named REPORT_MEMBER_DISPLAY_V1 allowlist and is populated only for values actually rendered/needed to interpret the report","no complete source observations, communications or Working Notes are copied"]},
    {"name":"sla_report_member_tier_results","columns":["report_member_tier_result_id TEXT PRIMARY KEY","report_attempt_id TEXT NOT NULL REFERENCES sla_report_attempts(report_attempt_id) ON DELETE RESTRICT","report_member_snapshot_id TEXT NOT NULL REFERENCES sla_report_member_snapshots(report_member_snapshot_id) ON DELETE RESTRICT","policy_tier_id TEXT NOT NULL REFERENCES sla_policy_tiers(policy_tier_id) ON DELETE RESTRICT","individual_state TEXT NOT NULL CHECK(individual_state IN ('active_within','active_exceeded','terminal_met','terminal_exceeded'))","inclusive_boundary_met INTEGER NOT NULL CHECK(inclusive_boundary_met IN (0,1))"],"rules":["exact tier threshold values are reproducible through immutable policy_tier_id and policy_revision_id captured on member"]},
    {"name":"sla_report_cohort_snapshots","columns":["report_cohort_snapshot_id TEXT PRIMARY KEY","report_attempt_id TEXT NOT NULL REFERENCES sla_report_attempts(report_attempt_id) ON DELETE RESTRICT","cohort_ordinal INTEGER NOT NULL CHECK(cohort_ordinal>0)","calendar_month TEXT NOT NULL","customer_org_id TEXT NOT NULL REFERENCES customer_organizations(customer_org_id) ON DELETE RESTRICT","contract_id TEXT NOT NULL REFERENCES contracts(contract_id) ON DELETE RESTRICT","contract_product_line_id TEXT NOT NULL REFERENCES contract_product_lines(contract_product_line_id) ON DELETE RESTRICT","policy_revision_id TEXT NOT NULL REFERENCES sla_policy_revisions(policy_revision_id) ON DELETE RESTRICT","policy_tier_id TEXT NOT NULL REFERENCES sla_policy_tiers(policy_tier_id) ON DELETE RESTRICT","severity TEXT NOT NULL","denominator INTEGER NOT NULL CHECK(denominator>=0)","terminal_met_count INTEGER NOT NULL CHECK(terminal_met_count>=0)","terminal_exceeded_count INTEGER NOT NULL CHECK(terminal_exceeded_count>=0)","active_within_count INTEGER NOT NULL CHECK(active_within_count>=0)","active_exceeded_count INTEGER NOT NULL CHECK(active_exceeded_count>=0)","state TEXT NOT NULL CHECK(state IN ('pending','currently_met','at_risk','breached','final_met'))","is_final INTEGER NOT NULL CHECK(is_final IN (0,1))","input_fingerprint TEXT NOT NULL CHECK(length(input_fingerprint)=64)"],"rules":["cohort identity excludes compliance outcome/state","counts must sum exactly to denominator when denominator>0","report preserves as-of projection result and immutable policy revision"]},
    {"name":"report_section_snapshots","columns":["report_section_snapshot_id TEXT PRIMARY KEY","report_attempt_id TEXT NOT NULL REFERENCES sla_report_attempts(report_attempt_id) ON DELETE RESTRICT","section_kind TEXT NOT NULL","schema_name TEXT NOT NULL","schema_version INTEGER NOT NULL CHECK(schema_version>0)","section_ordinal INTEGER NOT NULL CHECK(section_ordinal>0)","row_ordinal INTEGER NOT NULL CHECK(row_ordinal>0)","canonical_row_key TEXT NOT NULL","payload_json TEXT NOT NULL CHECK(json_valid(payload_json))","payload_sha256 TEXT NOT NULL CHECK(length(payload_sha256)=64)"],"rules":["section_kind/schema_name/version must exist in closed ReportSectionContributorRegistry before insert/read interpretation","payload validates against that contributor's exact named schema including field/sensitivity/size bounds; generic/unrestricted details JSON is forbidden","rows are read-only report snapshot evidence after completed transition and do not become duplicate domain authority"]},
    {"name":"sla_report_job_refs","columns":["report_attempt_id TEXT PRIMARY KEY REFERENCES sla_report_attempts(report_attempt_id) ON DELETE RESTRICT","job_id TEXT NOT NULL UNIQUE","destination_request_token TEXT NOT NULL CHECK(length(destination_request_token)=64)","last_command_id TEXT NOT NULL REFERENCES command_receipts(command_id) ON DELETE RESTRICT"],"rules":["job ref contains no snapshot bodies or credentials","destination_request_token binds the validated operator-selected output request without making external path immutable domain truth"]}
  ],
  "required_indexes":["sla_report_attempts(state,created_at_utc,report_attempt_id)","sla_report_attempts(customer_org_id,period_start_utc,report_attempt_id)","sla_report_member_snapshots(report_attempt_id,member_ordinal) UNIQUE","sla_report_member_snapshots(report_attempt_id,service_request_id) UNIQUE","sla_report_member_tier_results(report_member_snapshot_id,policy_tier_id)","sla_report_cohort_snapshots(report_attempt_id,cohort_ordinal) UNIQUE","sla_report_cohort_snapshots(report_attempt_id,calendar_month,customer_org_id,contract_product_line_id,severity,policy_tier_id)","report_section_snapshots(report_attempt_id,section_ordinal,row_ordinal) UNIQUE","report_section_snapshots(report_attempt_id,section_kind,canonical_row_key) UNIQUE","all command/FK columns receive LLD-01 leading-prefix FK coverage"],
  "authority_transition":"Snapshot child rows are technical staging while attempt state is staging/ready_to_generate/generating/verifying. They become immutable CompletedReportEvidence only in the same transaction that transitions the verified attempt to completed. For failed/cancelled attempts, cleanup deletes staged child rows before/with terminal attempt handling; completed snapshot rows are never updated/deleted by ordinary application workflows."
}
