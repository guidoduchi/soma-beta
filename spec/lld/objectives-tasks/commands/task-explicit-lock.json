{
  "schema":"SOMA-LLD-COMMAND-LEAF-V2",
  "lld_id":"LLD-05",
  "family":"task_explicit_lock_authority",
  "commands":[
    {"name":"SetExplicitTaskLock","input":["task_id","task_revision","lock_projection_revision where 0 means no projection","lock_kind plan|membership","action lock|unlock","reason_category","command_id"],"steps":["pure-preflight require canonical task_id UUIDv4 text, positive integer task_revision, nonnegative integer lock_projection_revision, exact lock_kind plan|membership, exact action lock|unlock, and bounded one-line nonempty reason_category (1..128 UTF-8 bytes, no NUL/CR/LF)","inside one writer UnitOfWork load exact Task and task_lock_projection; missing Task returns TASK_NOT_FOUND, task revision mismatch returns TASK_STALE, projection absence is represented only by lock_projection_revision=0, and any projection presence/revision mismatch returns TASK_STALE before receipt/domain mutation","derive the requested dimension's current explicit state from task_lock_projection, treating absent projection as both explicit_plan_lock=false and explicit_membership_lock=false; if requested action already equals that explicit state return semantic NO_CHANGE only after exact Task/projection freshness and append no lock event/audit","for plan unlock, fail closed with TASK_PLAN_LOCKED before receipt if accepted non-correction Task execution exists; current terminal WFM source authority for the exact source_projection_revision is unresolved/protected (pending review exists or no exact retain_local_work resolution exists); or current Objective authority is historical_provider_complete, superseded, in_progress, awaiting_review, reviewed or otherwise terminal/protected such that plan authority is independently frozen","for membership unlock, fail closed with TASK_MEMBERSHIP_LOCKED before receipt if the Task has accepted non-correction execution; current terminal WFM source authority is unresolved/protected for the exact source_projection_revision; or current Objective membership belongs to an in_progress, awaiting_review, reviewed, historical_structure or superseded/protected Objective where regrouping is independently constrained; current/projection rows that cannot be reconciled to same-Task owned history fail closed rather than permitting unlock","insert receipt only after the selected dimension is proven eligible for the requested explicit-state change","append exactly one task_lock_events row, create task_lock_projection at revision 1 when previously absent or update only the selected explicit dimension and advance projection revision exactly once, preserve the other explicit dimension, and increment tasks.revision exactly once","audit one task.lock_changed.v1 event with exact lock_event result ref, selected lock_kind/action, resulting Task revision and resulting lock projection revision; commit atomically"],"rules":["explicit lock state and effective derived locking are different authorities: an explicit unlock only changes the selected explicit boolean and never removes or rewrites execution, source-terminal, Objective, membership or other protected history","an ineffective unlock is rejected before receipt and never appends a misleading unlock event","lock_projection_revision=0 means exact absence; later projection creation makes that request stale","explicit lock history is durable and current time never creates, clears or changes a lock automatically","material explicit lock/unlock increments tasks.revision and task_lock_projection.revision exactly once; semantic NO_CHANGE increments neither","the non-selected explicit dimension is preserved exactly on every material mutation"]}
  ]
}
