{
  "schema":"SOMA-LLD-COMMAND-LEAF-V2",
  "lld_id":"LLD-05",
  "family":"task_execution_authority",
  "commands":[
    {"name":"StartTaskExecution","input":["task_id: canonical UUIDv4","task_revision: int>=1","execution_revision: int>=0 where 0 means exact absence of task_execution_projection and task_execution_events","effective_start_utc: known UTC whole second integer >=0 or exact ASCII string canonical-now","command_id"],"steps":["pure-preflight require canonical task_id UUIDv4 text, positive integer task_revision, nonnegative integer execution_revision and effective_start_utc either an integer UTC whole second >=0 or the exact case-sensitive ASCII string canonical-now","inside one writer UoW load the exact Task and execution authority; missing Task returns TASK_NOT_FOUND, task revision mismatch returns TASK_STALE, execution_revision=0 is current only when both task_execution_projection and task_execution_events are absent, and when a projection exists its positive revision must exactly equal execution_revision; projection/history inconsistency fails closed rather than fabricating state","fold current-effective execution evidence strictly by immutable per-Task execution_revision and require execution_state=not_started with no current terminal reviewed outcome; a current-effective start returns TASK_EXECUTION_ALREADY_STARTED and terminal execution/outcome returns TASK_EXECUTION_ALREADY_TERMINAL before receipt/domain mutation","after all freshness/eligibility checks, resolve canonical-now exactly once to the server canonical UTC whole second for this first execution; the request hash continues to bind the literal canonical-now token rather than the resolved instant","insert one receipt then append exactly one start execution event with execution_revision=prior execution revision+1, therefore 1 from exact no-history authority, at the accepted effective instant","create task_execution_projection at revision 1 when prior execution authority was exactly absent, otherwise rebuild it from complete execution history and advance its revision exactly once; increment tasks.revision exactly once; projection revision/last_event_id must equal the final contiguous execution event revision/identity","rebuild any affected Objective aggregate projection in the same UoW from current member execution/outcome authority; do not change Objective identity/envelope or any Task plan/source fact","audit one task.execution_changed.v1 event with the start execution-event ref plus resulting Task/execution revisions and commit atomically"],"rules":["Task may start independently; no other member Task starts implicitly","plan is not actual execution","execution_revision=0 is an exact absence freshness token, not a persisted projection revision and not a synonym for every logical not_started state; corrected history may yield not_started with a positive execution revision","recorded_at_utc is recording evidence only; accepted execution events may share a recording second and authority order comes only from execution_revision, never UUID lexical order","canonical-now is a semantic request token, not a transport omission/default: exact command replay returns the immutable original result and never re-resolves current time","every material accepted execution mutation advances the umbrella tasks.revision as well as its independent execution projection revision so cross-domain Task-revision captures cannot cross unseen lifecycle changes"]},
    {"name":"EndTaskExecution","input":["task_id: canonical UUIDv4","task_revision: int>=1","execution_revision: int>=1","effective_end_utc: known UTC whole second >=0","command_id"],"steps":["pure-preflight require canonical task_id UUIDv4 text, positive integer task_revision/execution_revision and known UTC whole-second effective_end_utc","inside one writer UoW load exact Task plus positive execution projection and reconcile it to owned history ordered by contiguous execution_revision; missing Task returns TASK_NOT_FOUND, Task or execution revision mismatch returns TASK_STALE, missing/current-effective start returns TASK_EXECUTION_NOT_STARTED, and current terminal execution returns TASK_EXECUTION_ALREADY_TERMINAL","require effective_end_utc>=current accepted actual_start_utc","insert one receipt and append exactly one end execution event with execution_revision=prior execution projection revision+1","rebuild execution projection from complete current revision-ordered history and advance execution revision exactly once; increment tasks.revision exactly once; with no current accepted outcome derive Awaiting Review without fabricating an outcome row","rebuild any affected Objective aggregate/Needs Attention projection in the same UoW while preserving Task plan/source and Objective envelope authority","audit one task.execution_changed.v1 event with exact end-event ref plus resulting Task/execution revisions and commit atomically"],"rules":["ending execution never fabricates Completed/Incomplete outcome","execution_revision must be positive because accepted start authority already exists","same-second start/end recording is valid and deterministic because execution_revision, not recorded_at_utc or execution_event_id, orders immutable history","material end advances both umbrella Task revision and execution projection revision exactly once"]},
    {"name":"CorrectTaskExecutionEvidence","input":["task_id: canonical UUIDv4","task_revision: int>=1","execution_revision: int>=1","target_execution_event_id: canonical UUIDv4","target_execution_revision: int>=1","correction_action: replace_time|withdraw","replacement_effective_at_utc: UTC whole second >=0 iff replace_time, else null","reason_category: bounded nonempty code","correction_review_fingerprint: lowercase SHA-256 from TaskExecutionCorrectionPreview","accept_outcome_invalidation: bool","command_id"],"steps":["pure-preflight validates UUIDs, revisions, action/replacement shape, bounded reason, lowercase fingerprint and boolean acknowledgement without reading owner state","inside one writer UoW load the exact Task, reconcile execution projection/history by contiguous execution_revision, and reconcile current outcome history/projection; task_revision or execution_revision mismatch returns TASK_STALE and integrity disagreement fails closed","recompute SOMA_TASK_EXECUTION_CORRECTION_REVIEW_V1 from current authority and exact request and require byte-equivalent fingerprint before receipt; changed target/current execution/current outcome/resulting fold returns TASK_REVIEW_STALE","require target_execution_event_id belongs to this Task at exactly target_execution_revision, is a non-correction base event with target revision strictly less than the new correction revision, and remains current-effective after all prior corrections; an already withdrawn or different target fails stale","forbid withdraw of rfc_terminal_terminate or source_terminal_consequence because those terminal facts are bound to separate governed decisions; any allowed correction must preserve a valid complete execution fold","if the simulated correction would make a currently valid reviewed outcome cease to satisfy the Task outcome acceptance matrix, require accept_outcome_invalidation=true; otherwise return TASK_OUTCOME_INVALID_FOR_EXECUTION before receipt. Acceptance never edits/deletes/clears the outcome event/current pointer: it only makes that prior review non-current by corrected execution semantics until a governed outcome correction/review resolves it","insert one receipt then append exactly one correction execution event at prior execution projection revision+1 targeting the immutable base event; rebuild execution projection from the complete revision-ordered history and increment tasks.revision exactly once while outcome revision/history remain unchanged","rebuild any affected Objective aggregate from exact current member authority and invalidate prior Objective review by fingerprint when material; append one bounded task.execution_changed.v1 audit event with correction/target/resulting revision refs and commit exact immutable response atomically"],"rules":["prior execution events remain immutable","recorded_at_utc and UUID lexical order never establish target/current chronology","preview fingerprint is review evidence, not trusted authority; every safety-critical input is reread/recomputed under the writer lock","a compatible prior reviewed outcome may remain current after a time correction; an incompatible one is preserved as historical reviewed evidence but is not treated as a valid current Task outcome","correction cannot manufacture cancelled_without_execution while a current-effective accepted start remains","replay occurs before prepare/owner-state reads and never reuses or recomputes current review state"]}
  ]
}
