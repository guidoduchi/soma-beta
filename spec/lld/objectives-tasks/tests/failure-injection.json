{
  "schema":"SOMA-LLD-FAILURE-TESTS-V1",
  "lld_id":"LLD-05",
  "cases":[
    {"id":"LLD05-F001","inject":"CreateLocalTask after receipt+tasks insert before plan/current relationship writes","expect":"whole UoW rolls back; no Task/plan/link/audit partial state; idempotent retry can execute normally"},
    {"id":"LLD05-F002","inject":"CreateObjectiveFromPreview after MW sequence allocation/update and Objective insert before first membership","expect":"allocator/Objectives/memberships/projections/audit all roll back; tracking sequence is not consumed by failed transaction"},
    {"id":"LLD05-F003","inject":"CreateObjectiveFromPreview after first of several membership events/current rows","expect":"no empty/partial Objective survives; every Task remains in prior membership state"},
    {"id":"LLD05-F004","inject":"SetTaskPlan after immutable plan revision insert before task_plan_current update","expect":"plan revision/current pointer/audit all roll back; Objective membership pin/envelope unchanged"},
    {"id":"LLD05-F005","inject":"SetTaskPlan succeeds but grouping proposal durable job crashes before proposal publication","expect":"accepted Task plan remains valid; membership pin remains prior; no partial regroup mutation; recompute can resume/recreate proposal deterministically"},
    {"id":"LLD05-F006","inject":"AcceptRegroupProposal after first Task move/repin before remaining Task changes","expect":"all membership/current/event/projection/supersession changes roll back and proposal remains pending/currently retryable"},
    {"id":"LLD05-F007","inject":"AcceptRegroupProposal consolidation after survivor membership writes before merged Objective supersession","expect":"no partial consolidation; all original Objectives/memberships/envelopes restored atomically"},
    {"id":"LLD05-F008","inject":"another accepted Objective/plan change creates strict overlap after preview but before regroup acceptance","expect":"commit-time material fingerprint/neighbor overlap check returns stale/OBJECTIVE_OVERLAP before domain mutation"},
    {"id":"LLD05-F009","inject":"Task plan revision/lock changes after persisted proposal preview","expect":"GROUPING_PROPOSAL_STALE; proposal cannot overwrite newer plan/lock/membership state"},
    {"id":"LLD05-F010","inject":"StartObjectiveExecution after first selected Task start event before second selected Task","expect":"all selected starts roll back; Objective aggregate remains prior; no partial start set"},
    {"id":"LLD05-F011","inject":"CancelObjectiveBeforeExecution after first Task cancellation/outcome","expect":"all member cancellation/outcome events and Objective review roll back; no partial whole-Objective cancel"},
    {"id":"LLD05-F012","inject":"EndTaskExecution after end event before execution/Objective projection rebuild","expect":"end event, projection/audit all roll back; Task remains in_progress"},
    {"id":"LLD05-F013","inject":"CorrectTaskExecutionEvidence after correction event before rebuild","expect":"correction and all derived changes roll back; original current execution interpretation remains"},
    {"id":"LLD05-F014","inject":"CorrectTaskOutcome after append correction event before current outcome pointer","expect":"new outcome event/current Objective aggregate/audit all roll back; prior accepted outcome remains current"},
    {"id":"LLD05-F015","inject":"CreateLocalTaskRetry after successor Task+plan insert before retry relation","expect":"new Task/plan/selected links roll back; predecessor still has no successor"},
    {"id":"LLD05-F016","inject":"LLD-07 Inventory retry participant returns failure/INDETERMINATE after LLD-05 successor writes","expect":"outer retry UoW rolls back every LLD-05 and LLD-07 mutation; no partial retry attempt"},
    {"id":"LLD05-F017","inject":"LLD-04 outer import applies WFM source projection then LLD-05 source-terminal review marker insert fails","expect":"entire LLD-04 accepted proposal UoW rolls back including source-projection handoff/domain mutation; one outer receipt semantics preserved"},
    {"id":"LLD05-F018","inject":"ResolveWfmSourceTerminalConsequence after local termination execution event before review transition/projection rebuild","expect":"termination event rolls back; review remains pending; provider source projection unchanged"},
    {"id":"LLD05-F019","inject":"source-terminal review created, then Task execution/plan/membership changes before decision","expect":"fingerprint revalidation rejects stale review; no local termination occurs; fresh review may supersede old one"},
    {"id":"LLD05-F020","inject":"LLD-03 RFC terminal cascade applies first WFM termination then second participant Task write fails","expect":"caller UoW rolls back all WFM/Task/Objective participant changes and RFC cascade execution; provider statuses unchanged"},
    {"id":"LLD05-F021","inject":"LLD-03 RFC terminal cascade Task mutations complete but Objective projection rebuild fails","expect":"all Task terminations and caller RFC/Communication participant work roll back atomically"},
    {"id":"LLD05-F022","inject":"historical Objective acceptance creates historical_source_structure plan then Objective insert fails","expect":"new plan/current pointer/receipt/audit roll back; Task remains without operational plan and unassigned; proposal pending"},
    {"id":"LLD05-F023","inject":"historical Objective acceptance inserts Objective then membership/projection fails","expect":"Objective/MW allocation/plan if newly created/membership all roll back; no history structure partial state"},
    {"id":"LLD05-F024","inject":"historical proposal source projection/source plan changes after preview","expect":"HISTORICAL_PROPOSAL_STALE; no plan/Objective created from obsolete evidence"},
    {"id":"LLD05-F025","inject":"different current operational plan appears after historical proposal","expect":"historical acceptance fails closed and never overwrites it; normal source-plan conflict review required"},
    {"id":"LLD05-F026","inject":"SetHistoricalTaskOperationalCountInclusion after inclusion event before aggregate rebuild","expect":"inclusion event/current projection/aggregate/audit roll back together"},
    {"id":"LLD05-F027","inject":"SetObjectiveTimezone LLD-02 setting write/audit validation fails","expect":"setting transaction rolls back; no Task/Objective instant is touched in either success or failure path"},
    {"id":"LLD05-F028","inject":"HardDeleteTask cross-domain dependency changes CLEAR->BLOCKED after preview","expect":"current-UoW guard blocks deletion before destructive write; Task and dependencies remain"},
    {"id":"LLD05-F029","inject":"HardDeleteTask FK/delete fails after first eligible dependent draft row removal","expect":"all deleted rows restore by rollback; audit does not claim deletion"},
    {"id":"LLD05-F030","inject":"HardDeleteObjective after membership/current projection removal before Objective delete","expect":"entire deletion rolls back; persisted Objective remains nonempty/current"},
    {"id":"LLD05-F031","inject":"Objective review fingerprint changes after preview because Task outcome corrected","expect":"OBJECTIVE_REVIEW_STALE; no review event accepted for obsolete aggregate"},
    {"id":"LLD05-F032","inject":"whole Objective cancellation preview had no starts, concurrent Task start commits before cancel","expect":"cancel UoW recheck sees accepted start and returns OBJECTIVE_CANCEL_AFTER_EXECUTION; start survives and no cancel rows commit"},
    {"id":"LLD05-F033","inject":"WFM activity lineage review after one lineage event before second member/current projection","expect":"all lineage changes roll back; distinct Task identities and prior conflict state preserved"},
    {"id":"LLD05-F034","inject":"large grouping durable job crashes between persisted proposal pages before publication/final state","expect":"incomplete technical staging is not reviewable/acceptable; restart safely recomputes/publishes exact proposal without Task/Objective mutation"},
    {"id":"LLD05-F035","inject":"invalid DST nonexistent/ambiguous local schedule reaches command","expect":"validation rejects before BEGIN IMMEDIATE; no Task plan/receipt/domain mutation"},
    {"id":"LLD05-F036","inject":"projection rebuild/startup validation detects current accepted strict-overlap drift","expect":"writes fail closed into diagnostics/recovery; runtime never silently repairs, deletes or regroups operational history"}
  ],
  "global_assertions":[
    "every injected authoritative failure leaves audit/domain mutation atomic according to LLD-01",
    "cross-domain participant failures never create nested/partial commits",
    "provider source status/evidence is not rewritten by LLD-05 rollback or correction",
    "failed grouping/execution/review commands never manufacture Task outcomes or overlapping accepted Objectives",
    "all retries after rollback resolve through command idempotency semantics without duplicate receipts/events"
  ]
}
