{
  "schema":"SOMA-LLD-SCHEMA-LEAF-V2",
  "lld_id":"LLD-02",
  "family":"reference_metadata_local_profile_customer",
  "id_convention":"All LLD-02 persistent entity/event/relationship identities use lowercase canonical UUIDv4 text from DomainIdFactory; descriptive/business values are never primary keys.",
  "table_mode":"Every table in this leaf is SQLite STRICT.",
  "tables":[
    {"name":"reference_metadata","purpose":"self-describe governed matching profile and provide O(1) freshness generation for reviewed Customer-reference conflicts","columns":["singleton_guard INTEGER PRIMARY KEY CHECK(singleton_guard=1)","matching_profile_id TEXT NOT NULL CHECK(length(matching_profile_id)>0)","customer_reference_generation INTEGER NOT NULL DEFAULT 0 CHECK(customer_reference_generation>=0)","created_at_utc INTEGER NOT NULL CHECK(created_at_utc>=0)"],"rules":["Beta 1.0 initializes exactly one row with matching_profile_id='UNICODE_MATCH_V1'","startup/reference-service readiness verifies stored profile before matching queries/writes","matching_profile_id is ordinary-runtime immutable; future profile change requires forward migration/reindex","customer_reference_generation is changed only by protected database triggers that increment it whenever Customer Organization lifecycle/descriptive state or Customer Account Code claim state changes","review snapshots may use this generation as conservative freshness evidence; unrelated Customer edits may intentionally stale a review rather than risk accepting against changed reference state","ordinary DELETE is forbidden"]},
    {"name":"local_user_profiles","purpose":"singleton installation actor/profile descriptive metadata; no credential material or login identifier","columns":["local_user_profile_id TEXT PRIMARY KEY","singleton_guard INTEGER NOT NULL UNIQUE CHECK(singleton_guard=1)","display_name TEXT NOT NULL CHECK(length(display_name)>0)","revision INTEGER NOT NULL DEFAULT 1 CHECK(revision>0)","created_at_utc INTEGER NOT NULL CHECK(created_at_utc>=0)","updated_at_utc INTEGER NOT NULL CHECK(updated_at_utc>=created_at_utc)"],"rules":["exactly one row after first-run setup; second insert rejected","password verifier/auto-login/data-key material are absent and belong to LLD-12","display_name is descriptive only, defaults server-side to 'Local Administrator', and may be edited only through UpdateLocalUserProfileDisplayName without changing local_user_profile_id or authentication authority","Beta 1.0 persists no username/login-name field; login requires password only"]},
    {"name":"customer_organizations","columns":["customer_org_id TEXT PRIMARY KEY","name TEXT NOT NULL CHECK(length(name)>0)","name_match_key TEXT NOT NULL CHECK(length(name_match_key)>0)","lifecycle_state TEXT NOT NULL CHECK(lifecycle_state IN ('active','archived'))","revision INTEGER NOT NULL DEFAULT 1 CHECK(revision>0)","created_at_utc INTEGER NOT NULL CHECK(created_at_utc>=0)","updated_at_utc INTEGER NOT NULL CHECK(updated_at_utc>=created_at_utc)"],"rules":["name/name_match_key are descriptive/matching evidence and are not unique","name_match_key must equal MatchingNormalizer(name) at every accepted write under reference_metadata.matching_profile_id","ordinary runtime exposes archive/reactivate, not destructive deletion","BEFORE DELETE raises REFERENCE_DELETE_FORBIDDEN","AFTER INSERT/UPDATE increments reference_metadata.customer_reference_generation"]},
    {"name":"customer_org_identifiers","purpose":"history-preserving external Customer Organization identifier claims; Beta 1.0 supports Advanced Search Customer Account Code","columns":["customer_org_identifier_id TEXT PRIMARY KEY","customer_org_id TEXT NOT NULL REFERENCES customer_organizations(customer_org_id) ON UPDATE RESTRICT ON DELETE RESTRICT","identifier_type TEXT NOT NULL CHECK(identifier_type IN ('customer_account_code'))","value_text TEXT NOT NULL CHECK(length(value_text)>0)","match_key TEXT NOT NULL CHECK(length(match_key)>0)","lifecycle_state TEXT NOT NULL CHECK(lifecycle_state IN ('active','superseded'))","created_at_utc INTEGER NOT NULL CHECK(created_at_utc>=0)","superseded_at_utc INTEGER","created_command_id TEXT NOT NULL REFERENCES command_receipts(command_id) ON UPDATE RESTRICT ON DELETE RESTRICT","superseded_command_id TEXT REFERENCES command_receipts(command_id) ON UPDATE RESTRICT ON DELETE RESTRICT","CHECK((lifecycle_state='active' AND superseded_at_utc IS NULL AND superseded_command_id IS NULL) OR (lifecycle_state='superseded' AND superseded_at_utc IS NOT NULL AND superseded_command_id IS NOT NULL))"],"rules":["Customer Account Code is external matching evidence and never customer_org_id","value/match_key use exact UNICODE_MATCH_V1 normalized complete source value after field validation","a Customer has at most one active customer_account_code claim","same normalized code may have multiple active Customer claimants only after explicit reviewed conflict acceptance; matching remains ambiguous","ordinary assignment detects different claimants and requires review instead of stealing/merging","supersession preserves creation/superseding command identities","BEFORE UPDATE permits only one active->superseded transition setting superseded fields while immutable identity/value/owner/created fields remain unchanged; superseded rows reject further UPDATE","BEFORE DELETE raises CUSTOMER_IDENTIFIER_HISTORY_APPEND_ONLY","AFTER INSERT and accepted active->superseded UPDATE increment reference_metadata.customer_reference_generation"]}
  ],
  "indexes":["CREATE INDEX idx_customer_org_active_name_match ON customer_organizations(lifecycle_state,name_match_key,customer_org_id)","CREATE UNIQUE INDEX uq_customer_org_active_identifier_type ON customer_org_identifiers(customer_org_id,identifier_type) WHERE lifecycle_state='active'","CREATE INDEX idx_customer_active_identifier_value ON customer_org_identifiers(identifier_type,match_key,customer_org_id) WHERE lifecycle_state='active'","CREATE INDEX idx_customer_identifier_history ON customer_org_identifiers(customer_org_id,identifier_type,created_at_utc,customer_org_identifier_id)"],
  "verification":"LLD-01 exact-schema verification includes these STRICT tables, indexes, generation triggers and protection triggers once the LLD-02 migration is accepted."
}
