{
  "schema":"SOMA-LLD-SCHEMA-INDEX-V2",
  "lld_id":"LLD-02",
  "id_convention":"All LLD-02 persistent entity/event/relationship identities use lowercase canonical UUIDv4 text from DomainIdFactory; descriptive/business values are never primary keys.",
  "table_mode":"Every LLD-02 table is created with SQLite STRICT table mode; future accepted schema extensions use forward migrations rather than weakening types in place.",
  "schema_leaves":[
    {"path":"schema/reference-customer.json","tables":["reference_metadata","local_user_profiles","customer_organizations","customer_org_identifiers"]},
    {"path":"schema/reference-contact-dispatch.json","tables":["contacts","contact_channels","contact_affiliations","dispatch_locations","reference_lifecycle_events"]}
  ],
  "cross_module_schema_rule":"LLD-08 creates/owns Site↔Dispatch Location relationship with UNIQUE constraints on both Site and dispatch_location_id; LLD-02 never adds site_id/customer_org_id shortcut to dispatch_locations.",
  "verification":"LLD-01 exact-schema verification must include all LLD-02 STRICT tables, partial/index coverage, generation triggers and protection triggers once the LLD-02 migration is accepted.",
  "resolution_rule":"Each table and its indexes/guards are defined normatively in exactly one schema leaf. This index owns only the common identity/table-mode and cross-module rules."
}
