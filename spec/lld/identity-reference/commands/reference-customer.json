{
  "schema":"SOMA-LLD-COMMAND-LEAF-V2",
  "lld_id":"LLD-02",
  "family":"customer_identity_account_code",
  "inherits_ordering":"commands/reference.json",
  "commands":[
    {"name":"CreateCustomerOrganization","input":["command_id","name","optional Customer Account Code","actor/source"],"transaction_boundary":"one LLD-01 authoritative UnitOfWork after pure preflight","steps":["validate bounds and compute governed match keys","inside UnitOfWork detect any active different Account Code claimant and return ACCOUNT_CODE_CONFLICT_REVIEW with no mutation","allocate customer_org_id/optional identifier_id/lifecycle_event_id","insert command_receipt","insert active Customer revision 1","insert optional active Account Code claim","append created lifecycle event","insert audit/result references","commit"],"rules":["equal name never blocks explicit creation solely as identity duplication","Customer Account Code is external matching evidence, never relational ID","reviewed duplicate-code preservation uses ConfirmCustomerAccountCodeSharedClaim after Customer creation","no implicit merge/adoption"]},
    {"name":"SetCustomerAccountCode","input":["customer_org_id","base_revision","raw Customer Account Code","reason_category","command_id"],"transaction_boundary":"one UnitOfWork after normalization preflight","steps":["inside UnitOfWork load active Customer/revalidate revision","query complete active claimant state for normalized code","if target already owns exactly requested accepted claim apply SemanticNoChange","if any other Customer claims code and target does not already hold it return ACCOUNT_CODE_CONFLICT_REVIEW","otherwise insert receipt, supersede target's different prior active code if any, insert new active claim, increment Customer revision, audit, commit"],"rules":["prior claims remain history","source/import evidence never calls this without owning reviewed acceptance"]},
    {"name":"ConfirmCustomerAccountCodeSharedClaim","purpose":"explicit reviewed acceptance that one normalized Customer Account Code may currently have more than one canonical Customer Organization claimant","input":["customer_org_id","base_revision","raw Customer Account Code","proposed_action=CONFIRM_SHARED_CLAIM","review_snapshot_hash","optional review_context_id","reason_category","command_id"],"implementation":"algorithms/account-code-review.json commit_validation + algorithms/no-change.json","transaction_boundary":"one UnitOfWork after normalization preflight","steps":["inside UnitOfWork validate exact review_snapshot_hash against current claimant/target state","require at least one different active claimant","load/revalidate target Customer","if target already owns exact claim apply SemanticNoChange","otherwise allocate identifier ID, insert receipt, supersede target's different active code if present, insert active shared claim, increment target revision, audit exact reviewed correction, commit"],"rules":["does not merge Customers","does not choose winner","future matching remains AMBIGUOUS while multiple eligible claimants survive","review_context_id is provenance/correlation only","unavailable to safe-auto-accept paths"]},
    {"name":"ReassignCustomerAccountCode","purpose":"explicit reviewed correction moving one active Account Code claim from one Customer Organization to another","input":["from_customer_org_id","from_base_revision","to_customer_org_id","to_base_revision","raw Customer Account Code","proposed_action=REASSIGN_CLAIM","review_snapshot_hash","optional review_context_id","reason_category","command_id"],"implementation":"algorithms/account-code-review.json commit_validation","transaction_boundary":"one UnitOfWork after normalization preflight","steps":["inside UnitOfWork validate exact review_snapshot_hash against current claimant/source/target state","require distinct active Customers and revalidate both revisions","prove source has active claim for reviewed code","classify target current Account Code as SAME_REVIEWED_CODE, DIFFERENT_CODE, or NONE","insert receipt","supersede source claim","if target has DIFFERENT_CODE supersede that reviewed target claim and insert new reviewed-code claim","if target has NONE insert new reviewed-code claim","if target has SAME_REVIEWED_CODE retain its existing identifier row and do not insert duplicate","increment source Customer revision; increment target Customer revision only when target claim actually changed","audit every changed identifier/Customer plus review binding","commit"],"rules":["does not merge Customers","does not rewrite historical Customer context","does not silently alter any third Customer sharing code","existing same-code target claim is reused rather than duplicated","failure anywhere rolls back every claim/revision/audit change"]},
    {"name":"UpdateReferenceDescriptiveData","targets":["customer_organization"],"input":["target_id","base_revision","allowed descriptive fields","command_id"],"transaction_boundary":"one UnitOfWork after pure validation/normalization","steps":["inside UnitOfWork load active target/revalidate revision","if all governed stored fields/derived keys equal request apply SemanticNoChange","otherwise insert receipt, update allowed fields/increment revision, append descriptive_corrected lifecycle event, audit bounded prior/new evidence, commit"],"rules":["cannot change immutable ID","Account Code uses dedicated commands"]}
  ]
}
