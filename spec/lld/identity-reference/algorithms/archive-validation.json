{
  "schema":"SOMA-LLD-ALGORITHM-V1",
  "lld_id":"LLD-02",
  "name":"ReferenceLifecycleValidation",
  "archive":{
    "input":["ReferenceTarget(type,id)","base_revision","reason_category","command context"],
    "transaction":"one LLD-01 BEGIN IMMEDIATE UnitOfWork",
    "steps":[
      "load exact target by immutable ID and require lifecycle_state active",
      "revalidate base_revision",
      "load deterministic validator list from ReferenceDependencyRegistry for target type",
      "execute every validator against the same UnitOfWork; validators are read-only and query only their owning domain",
      "collect blockers as typed immutable-identity records; do not stop after the first blocker",
      "if any blocker exists return ARCHIVE_BLOCKED with complete bounded blocker set and perform no mutation/receipt",
      "allocate lifecycle event ID and insert command_receipt",
      "set lifecycle_state archived and increment revision",
      "append reference_lifecycle_events archived occurrence referencing the receipt",
      "insert required audit/result references",
      "commit"
    ],
    "rules":[
      "Contact validators include active requester/assignment/unfinished operational dependencies registered by owning domain packets",
      "Dispatch Location validators include active Site-dedicated relationship and unfinished logistics dependencies from LLD-08/LLD-07",
      "Customer Organization validators are registered by every owning domain whose active work would be stranded",
      "archive never cascades deletion/unlinking merely to make validation pass",
      "ordinary Beta 1.0 LLD-02 exposes no hard-delete command for history-bearing reference masters"
    ]
  },
  "reactivate":{
    "transaction":"one LLD-01 BEGIN IMMEDIATE UnitOfWork",
    "steps":["load exact archived target","revalidate base_revision","run registered reactivation validators/current structural constraints","if conflicts exist return REACTIVATION_BLOCKED with no mutation/receipt","allocate lifecycle event ID and insert command_receipt","set active and increment revision","append reactivated lifecycle event referencing receipt","insert audit/result references","commit"],
    "rule":"reactivation never deletes or rewrites the prior archived interval/events"
  },
  "blocker_shape":["validator_id","dependency_type","dependency_id","reason_code","safe_next_action"],
  "ordering":"validators and returned blockers sort deterministically by validator_id, dependency_type, dependency_id so UI/tests receive stable results",
  "failure_atomicity":"stale revision, validator exception, blocker, constraint error, lifecycle-event failure or audit failure rolls back receipt and all attempted lifecycle mutation."
}
