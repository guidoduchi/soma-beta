{
  "schema":"SOMA-LLD-ALGORITHM-V1",
  "lld_id":"LLD-02",
  "name":"ReferenceCandidateMatching",
  "normalization_profile":{
    "id":"UNICODE_MATCH_V1",
    "unicode_version":"17.0.0",
    "nfkc_provider":"unicodedata2==17.0.0",
    "casefold_provider":"repository-versioned full Unicode CaseFolding mapping generated from Unicode 17.0.0 CaseFolding.txt",
    "whitespace_provider":"repository-versioned Unicode 17.0.0 White_Space code-point set",
    "runtime_rule":"Python built-in unicodedata version, str.casefold() and str.isspace() are not authority for matching because supported Python lines may ship different Unicode databases",
    "asset_rule":"generated CaseFolding/White_Space assets are source-controlled implementation assets with recorded Unicode source version/checksum and deterministic regeneration test; changing the profile/version requires an accepted migration/reindex plan for stored match keys",
    "packaging_rule":"LLD-12 must include required third-party attribution/license notice for unicodedata2 without changing SOMA proprietary product ownership"
  },
  "normalizer":{
    "function":"normalize_match_key(raw: str) -> str",
    "steps":[
      "reject non-string input and enforce pre-normalization semantic byte bound",
      "apply Unicode NFKC compatibility normalization through UNICODE_MATCH_V1 NFKC provider",
      "replace each maximal run of code points in the UNICODE_MATCH_V1 White_Space set, including surrounding whitespace, with one ASCII U+0020 space, then remove leading/trailing separator",
      "apply UNICODE_MATCH_V1 full case-fold mapping",
      "do not strip accents/diacritics",
      "do not strip or rewrite punctuation",
      "enforce post-normalization key bound and reject empty normalized result"
    ],
    "determinism":"the same UNICODE_MATCH_V1 vector corpus must produce byte-identical UTF-8 keys on Python 3.13 and 3.14; runtime Unicode database changes cannot alter the result",
    "prohibitions":["no fuzzy distance","no phonetic key","no accent folding","no punctuation removal","no locale-sensitive lowercasing","no substring/contains identity match","no direct Python-runtime Unicode tables as unversioned authority"]
  },
  "customer_org_match":{
    "input":["optional raw Customer Account Code","optional raw Customer Organization label"],
    "active_scope":"active Customer Organizations only",
    "account_code":"normalize complete supplied code; query every active customer_org_identifiers row with identifier_type='customer_account_code' and exact match_key; zero, one, or multiple reviewed canonical claimants may exist",
    "name":"normalize supplied label and query customer_organizations.name_match_key; duplicate names may yield multiple candidates",
    "precedence_and_conflict":[
      "if account code has exactly one active claimant and no supplied name evidence identifies a different Customer, return UNIQUE_CANDIDATE for the account-code claimant with explanation ACCOUNT_CODE_MATCH",
      "if account code has exactly one active claimant and supplied name evidence yields any different Customer candidate, return AMBIGUOUS over the union with explanation ACCOUNT_CODE_NAME_CONFLICT",
      "if account code has multiple active claimants, return AMBIGUOUS over all account-code claimants plus any additional exact name candidates with explanation ACCOUNT_CODE_MULTIPLE_CLAIMS; no claimant silently outranks another",
      "if account code is supplied but has no canonical claimant, name candidates remain proposal evidence only: zero -> UNRESOLVED, one -> UNIQUE_CANDIDATE with explanation ACCOUNT_CODE_UNRESOLVED_NAME_CANDIDATE, many -> AMBIGUOUS",
      "if account code is absent, name-only candidate behavior is zero -> UNRESOLVED, one -> UNIQUE_CANDIDATE, many -> AMBIGUOUS"
    ],
    "source_conflict_rule":"multiply claimed/conflicting account-code observations detected in LLD-03/04 staging never auto-create or overwrite canonical claims; reviewed master commands may resolve the conflict to one claimant or explicitly preserve multiple active claims, in which case future matching remains AMBIGUOUS",
    "identity_rule":"Customer Account Code is preferred stable external matching evidence but never replaces customer_org_id"
  },
  "contact_match":{
    "scope":"exactly one of customer_org_id or UNBOUND",
    "active_only":true,
    "name_key":"contacts.name_match_key",
    "email_key":"active contact_channels where channel_kind='email' using match_key",
    "customer_scope_query":"candidate Contact must have is_current=1 affiliation to the supplied customer_org_id",
    "unbound_scope_query":"candidate Contact must have no is_current=1 affiliation row",
    "candidate_union":"union candidate IDs from supplied governed keys; repeated evidence for the same Contact collapses to one candidate ID; distinct Contacts produced by different exact supplied keys remain AMBIGUOUS rather than allowing one key to silently outrank another",
    "result":"0 candidate IDs -> UNRESOLVED; 1 -> UNIQUE_CANDIDATE; >1 -> AMBIGUOUS",
    "rule":"equal normalized email in different Customer scopes is not a global identity collision; equal keys within one scope may remain ambiguous"
  },
  "stored_key_versioning":{
    "rule":"every persisted match_key column is governed by reference_metadata.matching_profile_id; matching queries/writes refuse an unsupported profile",
    "upgrade":"a future profile change is not an in-place reinterpretation: it requires a governed forward migration that recomputes all affected keys transactionally and proves candidate behavior before publication"
  },
  "write_time_key_rule":"Every accepted write that changes a matched descriptive/channel/external-identifier value computes the match key with this same profile inside application validation before persistence; repositories reject missing/inconsistent derived key input.",
  "review_rule":"A matcher has no write capability. Selection/link/merge/assignment/reassignment is performed only by a separate owning command after operator/reconciliation authority and fresh state revalidation."
}
