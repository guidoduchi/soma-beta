{
  "schema":"SOMA-LLD-FAILURE-INJECTION-V1",
  "lld_id":"LLD-01",
  "matrix":[
    {"area":"migration","points":["before BEGIN","after DDL/DML statement N","before ledger insert","after ledger insert","before COMMIT","COMMIT error","process kill after committed migration before next migration","post-migration verifier failure"],"required_result":"database is absent for failed unpublished clean init or remains at last committed migration; no schema/ledger split"},
    {"area":"authoritative command","points":["after state load","after domain mutation","after lifecycle evidence","after command receipt","during audit payload validation","after audit insert","during audit_event_results","during exact replay-result canonicalization/hash","during command_receipt_results insert","after command_receipt_results insert before COMMIT","COMMIT error"],"required_result":"complete rollback unless COMMIT completed; caller never receives accepted success for rolled-back work; a receipt can never commit without its required exact result snapshot after migration 5"},
    {"area":"command replay result","points":["matching new receipt with missing companion row","response_json hash mismatch","noncanonical or invalid stored JSON","unknown stored response schema/version","matching legacy pre-migration-5 receipt without companion result","replay attempted after target/current projection changed"],"required_result":"new-result corruption/missing companion fails closed as integrity failure; legacy missing exact result returns IDEMPOTENCY_RESULT_UNAVAILABLE; no case calls command prepare, owner/participant mutation, audit insertion or mutable current-state reconstruction"},
    {"area":"accepted no-op","points":["response lost after NO_CHANGE receipt+exact-result commit","underlying target changes before transport retry","retry same command_id/type/hash","retry same command_id/different hash"],"required_result":"same command_id/type/hash returns the exact committed NO_CHANGE result without becoming a mutation under newer state; different hash fails integrity; no lifecycle/audit evidence is fabricated for the original no-op"},
    {"area":"durable job coalescing and claims","points":["same semantic enqueue inside two serialized writers","forced duplicate active dedupe index insert","dedupe_sha256 collision with different packet-derived key","persisted payload changed so registered derived key disagrees with hash candidate","unknown job type/version reaches due ordering","payload/checkpoint malformed/noncanonical/oversized","stale handler from attempt N checkpoints/completes/fails after same run_id has claimed attempt N+1","retry_wait claimed before due time","terminal attempt insert succeeds then durable_jobs terminal update fails","stale prior-run recovery interrupted-attempt insert succeeds then recovery disposition update fails","same stale recovery executed twice"],"required_result":"coalescing is indexed and exact, raw semantic key is not persisted merely for coalescing, ambiguous/collision/corrupt/unknown contracts fail closed, partial technical transitions roll back, retry timing is enforced, complete claim tuple prevents ABA, and every ended running attempt has at most one exact ordinal history row"},
    {"area":"durable job owning-domain claim guard","points":["worker checkpoints successfully to a newer valid checkpoint, then assert_claim_current runs with the original claim value","worker checkpoints successfully, then another transaction cancels the running job before the worker opens an owning-domain command","worker performs external work, then stale-claim recovery/reclaim replaces the exact attempt before the worker opens an owning-domain command","persisted payload/dedupe identity no longer equals the immutable claim identity or current persisted checkpoint is malformed/noncanonical when an owning-domain command begins","owning-domain command invokes assert_claim_current and a later owner guard fails causing outer rollback"],"required_result":"assert_claim_current runs read-only inside the owning command UnitOfWork before that command receipt; it revalidates the exact running job_id/run_id/attempt_ordinal/claim_started_at_utc tuple, exact immutable payload/dedupe identity and the currently persisted registered checkpoint. A legitimate checkpoint(claim, ...) advancement under the same exact claim remains valid even though claim.checkpoint_json is the older claim-time snapshot. Revoked/replaced claims return JOB_CLAIM_CONFLICT, corrupt immutable identity or current checkpoint returns INTEGRITY_FAILURE, no Foundation state is written, and no stale worker receipt/domain mutation/audit/result can commit; later owner failure rolls back normally because the claim guard itself mutated nothing"},
    {"area":"legacy durable job startup reconciliation","points":["post-migration queued legacy row has dedupe_sha256 NULL","post-migration retry_wait legacy row has dedupe_sha256 NULL","post-migration waiting_review legacy row has dedupe_sha256 NULL","post-migration running legacy row has dedupe_sha256 NULL with unrepresented attempt ordinal","legacy running ordinal already represented","failure after interrupted-attempt insert before failed-state update","failure while terminalizing one legacy row among several","startup reconciliation invoked twice","claim attempted before reconciliation completes"],"required_result":"migration never fabricates/backfills a semantic key; before READY every legacy-NULL nonterminal row becomes failed with LEGACY_JOB_DEDUPE_UNAVAILABLE, a running row gets at most one interrupted attempt for its exact ordinal, queued/retry/waiting rows get no invented attempt, per-row failure rolls back only that bounded reconciliation transaction and blocks READY until retry succeeds, repeated reconciliation is idempotent, and no legacy-NULL row can be claimed"},
    {"area":"durable job cancellation","points":["cancel queued/retry_wait/waiting_review job inside caller UnitOfWork","cancel running job after claim before next checkpoint","caller domain guard fails after Foundation cancel participant","stale running handler attempts checkpoint/complete after committed cancellation","cancel races owning-domain completion transaction"],"required_result":"job cancellation and owning-domain cancellation either commit together or both roll back; a committed running-job cancellation records one cancelled attempt/revokes the complete exact claim, stale handler cannot checkpoint/complete/fail or pass assert_claim_current, and the independently revalidated owning-domain transaction determines whether cancellation or completion wins authority"},
    {"area":"durable job restore reconciliation","points":["restored queued job","restored retry_wait job","restored waiting_review job","restored running job with unrepresented current ordinal","restored running job whose ordinal already has attempt history","unknown restored job type/version","failure after interrupted attempt insert before nonterminal invalidation","reconciliation invoked twice before READY"],"required_result":"every restored nonterminal job is invalidated before READY, running work receives at most one interrupted attempt row, terminal rows remain unchanged, unknown contracts never execute, and rollback/retry cannot duplicate history or silently revive historical external work"},
    {"area":"connection safety","points":["foreign_keys cannot be enabled","journal mode unexpected","trusted-schema policy unavailable/misconfigured","busy timeout exceeded","connection used from wrong thread"],"required_result":"unsafe connection is not returned; stable failure category only"},
    {"area":"startup","points":["instance lock contention","security provider unavailable","database invalid","loopback socket bind fails","Uvicorn fails after socket bind","runtime registry atomic replace fails","self-health fails after listen"],"required_result":"host never reports READY; owned resources unwind without altering foreign/stale registry or another process"},
    {"area":"shutdown","points":["active write completes","active write fails","background worker exceeds grace","WAL checkpoint fails","registry changed by another run before removal"],"required_result":"no new mutation begins after quiesce; exact ownership is preserved; unrelated process/registry is untouched"},
    {"area":"strict JSON/audit","points":["duplicate key","nonfinite number","invalid UTF-8","oversize","overdepth","unknown action schema","sensitive canary","duplicate audit identity"],"required_result":"fail closed for authoritative audit/payload validation and roll back associated command; never fall back to raw serialization"}
  ],
  "execution_rules":["failure injection must run on both supported Python lines where behavior is runtime-sensitive","tests use isolated temporary directories/databases and no customer data","every injected failure asserts persisted state, filesystem side effects, host state and returned error category","a failure test that merely observes an exception without verifying rollback/state is insufficient"]
}
