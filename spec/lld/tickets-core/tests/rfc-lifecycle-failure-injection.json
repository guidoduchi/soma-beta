{
  "schema":"SOMA-LLD-FAILURE-INJECTION-V1",
  "lld_id":"LLD-03",
  "suite":"RFC lifecycle transactional failure injection",
  "cases":[
    {"id":"LLD03-FI001","inject":"failure after RFC terminal projection update but before pending cascade proposal insert","expect":"entire acceptance UnitOfWork rolls back; prior projection remains current and no proposal/member rows exist"},
    {"id":"LLD03-FI002","inject":"failure after cascade proposal row insert but before all immutable membership rows","expect":"projection/proposal/partial membership/receipt/audit all roll back"},
    {"id":"LLD03-FI003","inject":"terminal-cascade execution failure at any participant boundary: LLD-05 fails before/after owned mutation; LLD-05 applies then LLD-09 fails before/after owned mutation; or both apply and a failure occurs before proposal transition/audit/exact result/commit","expect":"all Task/Objective and Communication database changes, proposal execution transition, receipt, audit and command result roll back; proposal remains pending; provider WFM source facts and unrelated Local Tasks remain unchanged; an already-consumed deliberate proof is not made reusable"},
    {"id":"LLD03-FI004","inject":"after READY preview/deliberate interaction but before execute writer revalidation, either RFC/WFM scope changes, scope remains identical while LLD-05 Task/Objective or LLD-09 Communication complete impact count/fingerprint changes, or either participant returns INDETERMINATE during complete-set revalidation","expect":"determinate scope/provider mismatch returns RFC_TERMINAL_CASCADE_STALE and INDETERMINATE returns RFC_TERMINAL_CASCADE_PARTICIPANT_FAILED; both fail before proof consumption/receipt/participant mutation and leave proposal pending"},
    {"id":"LLD03-FI005","inject":"deliberate proof has wrong action, proposal target, base revision or reviewed preview fingerprint, or is invalid/expired/session-mismatched/already-consumed","expect":"RFC_TERMINAL_CASCADE_CONFIRMATION_REQUIRED after domain freshness revalidation but before downstream mutation; client timestamp/boolean is ignored"},
    {"id":"LLD03-FI006","inject":"crash during branch archive after some in-memory/member updates but before transaction commit","expect":"all RFC archive states, operation/member/event rows, receipt and audit roll back"},
    {"id":"LLD03-FI007","inject":"member is independently restored/rearchived between archive-operation read and restore commit","expect":"current-UoW latest-event check excludes or stales that member; older operation never overwrites newer archive provenance"},
    {"id":"LLD03-FI008","inject":"new source/history dependency appears after eligible hard-delete preview","expect":"commit recomputation changes fingerprint or returns blocker; RFC_HARD_DELETE_PREVIEW_STALE/BLOCKED and no mutation"},
    {"id":"LLD03-FI009","inject":"unexpected dependent FK causes RFC DELETE to fail after receipt/audit insertion","expect":"same UnitOfWork rolls back RFC delete attempt, command receipt and deletion audit; RFC remains intact"},
    {"id":"LLD03-FI010","inject":"terminal reversal accepted while pending cascade exists","expect":"projection reversal and pending-proposal supersession commit atomically; crash cannot leave nonterminal current state with executable pending proposal from the old epoch"},
    {"id":"LLD03-FI011","inject":"one cross-domain dependency provider returns INDETERMINATE during hard-delete preview/commit","expect":"fail closed without attempting dependent cleanup or RFC deletion"},
    {"id":"LLD03-FI012","inject":"same command_id replay after successful cascade/archive/hard-delete command","expect":"LLD-01 replay returns original committed exact result before owner preparation/current reads; terminal-cascade replay does not validate or consume another deliberate proof and creates no duplicate proposal/event/audit or second destructive effect"}
  ]
}
