{"schema":"SOMA-LLD-TESTS-V1","lld_id":"LLD-03","suite":"RFC archive/restore bounded-authority acceptance","certification_scope":"Certify complete SOMA_RFC_ARCHIVE_SCOPE_V1 freshness, fixed-size ArchiveRfcResultV1 replay, exact RfcArchiveOperationMemberV1 projection, operation-scoped restore and constant-cardinality archive audit JSON/result refs without imposing a branch-member business cap.","scenarios":[{"id":"LLD03-T035","name":"archive_scope_and_audit_are_cardinality_safe","prove":["SOMA_RFC_ARCHIVE_SCOPE_V1 matches both published golden vectors and changes when any member identity, revision or archive_state changes","ArchiveRfcRequestV1 carries only scope_kind and reviewed_scope_fingerprint; it never carries a member revision list or other unbounded branch payload","writer recomputes the complete current scope under the authoritative UnitOfWork and exact-compares reviewed_scope_fingerprint before mutation","a reviewed_branch with more members than Foundation audit max_collection_items remains valid and archives every active reviewed member without truncation or an invented business cap","archive audit JSON remains fixed-shape with scalar counts and has exactly one audit result ref to rfc_archive_operation regardless of changed member cardinality","exact changed RFC identities remain only in immutable rfc_archive_operation_members/rfc_archive_events row-wise authority","ArchiveRfc material and NO_CHANGE responses are fixed-size ArchiveRfcResultV1; NO_CHANGE has null rfc_archive_operation_id and creates no operation/event/audit","same command_id replay returns the original exact ArchiveRfcResultV1 before current scope reads and creates no second operation/event/audit"]},{"id":"LLD03-T036","name":"archive_restore_audit_and_response_are_operation_bounded","prove":["RestoreRfcArchiveOperation considers every immutable origin member but restores only members still archived by that exact operation","a newer independent restore/rearchive wins and an older operation never overwrites later provenance","RfcArchiveOperationMemberV1 has exactly rfc_id, pre_archive_revision, archived_revision, ordinal, current_archive_state, latest_event_type, latest_event_origin_archive_operation_id, latest_event_resulting_rfc_revision and restore_eligible; unknown fields are rejected","latest event authority is greatest resulting_rfc_revision for the RFC and restore_eligible is true exactly for current archived state whose latest archived event belongs to the queried origin operation","restore audit remains fixed-shape with scalar changed_rfc_count and exactly one audit result ref to the origin rfc_archive_operation even when many members restore","restored identities remain row-wise rfc_archive_events for the restore command","RestoreRfcArchiveOperation processes every eligible member before building its response and returns at most 32 RfcArchiveOperationMemberV1 items in the first page with GetRfcArchiveOperation-compatible continuation; this response-page bound never caps restore cardinality","a material restore with more than 32 eligible members succeeds for all eligible members while its exact stored replay response remains within Foundation response collection bounds","same command_id restore replay returns the exact stored bounded RfcArchiveOperationV1 even after later archive state changes and does not reconstruct from mutable current state","zero eligible members is semantic NO_CHANGE with no restored event or audit and stores the exact bounded operation projection for replay"]}]}
