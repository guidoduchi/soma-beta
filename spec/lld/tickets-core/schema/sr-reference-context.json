{"schema":"SOMA-LLD-SCHEMA-V1","lld_id":"LLD-03","purpose":"Own history-preserving Service Request relationships to LLD-02 Customer Organization and Contact identities without turning Advanced Search labels into relational identity or creating a competing Current Handler authority.","tables":[{"name":"sr_customer_relationships","columns":["sr_customer_relationship_id TEXT PRIMARY KEY","service_request_id TEXT NOT NULL REFERENCES service_requests(service_request_id) ON UPDATE RESTRICT ON DELETE RESTRICT","customer_org_id TEXT NOT NULL REFERENCES customer_organizations(customer_org_id) ON UPDATE RESTRICT ON DELETE RESTRICT","relationship_state TEXT NOT NULL CHECK(relationship_state IN ('active','superseded'))","origin_kind TEXT NOT NULL CHECK(origin_kind IN ('manual_review','advanced_search_review','correction'))","reconciliation_proposal_id TEXT REFERENCES reconciliation_proposals(reconciliation_proposal_id) ON UPDATE RESTRICT ON DELETE RESTRICT","opened_at_utc INTEGER NOT NULL CHECK(opened_at_utc>=0)","closed_at_utc INTEGER","opened_command_id TEXT NOT NULL REFERENCES command_receipts(command_id) ON UPDATE RESTRICT ON DELETE RESTRICT","closed_command_id TEXT REFERENCES command_receipts(command_id) ON UPDATE RESTRICT ON DELETE RESTRICT","reason_category TEXT","CHECK((relationship_state='active' AND closed_at_utc IS NULL AND closed_command_id IS NULL) OR (relationship_state='superseded' AND closed_at_utc IS NOT NULL AND closed_command_id IS NOT NULL))","CHECK(origin_kind<>'advanced_search_review' OR reconciliation_proposal_id IS NOT NULL)"],"rules":["at most one active canonical Customer Organization relationship exists per Service Request","Customer labels/account codes are proposal evidence only; accepted relationship always targets immutable LLD-02 customer_org_id","changing Customer supersedes the prior row and inserts a new row atomically; history is never overwritten","Customer change invokes the LLD-06 ServiceRequestCustomerClassificationParticipant in the same UnitOfWork so incompatible current classification is invalidated rather than silently remapped","unresolved or ambiguous source Customer evidence leaves the current relationship unchanged and does not block independent SR source facts"]},{"name":"sr_contact_relationships","columns":["sr_contact_relationship_id TEXT PRIMARY KEY","service_request_id TEXT NOT NULL REFERENCES service_requests(service_request_id) ON UPDATE RESTRICT ON DELETE RESTRICT","reference_role TEXT NOT NULL CHECK(reference_role IN ('customer_contact','current_handler_reference'))","contact_id TEXT NOT NULL REFERENCES contacts(contact_id) ON UPDATE RESTRICT ON DELETE RESTRICT","customer_org_context_id TEXT REFERENCES customer_organizations(customer_org_id) ON UPDATE RESTRICT ON DELETE RESTRICT","relationship_state TEXT NOT NULL CHECK(relationship_state IN ('active','superseded'))","origin_kind TEXT NOT NULL CHECK(origin_kind IN ('manual_review','advanced_search_review','correction'))","reconciliation_proposal_id TEXT REFERENCES reconciliation_proposals(reconciliation_proposal_id) ON UPDATE RESTRICT ON DELETE RESTRICT","supporting_sr_source_field_observation_id TEXT REFERENCES sr_source_field_observations(sr_source_field_observation_id) ON UPDATE RESTRICT ON DELETE RESTRICT","opened_at_utc INTEGER NOT NULL CHECK(opened_at_utc>=0)","closed_at_utc INTEGER","opened_command_id TEXT NOT NULL REFERENCES command_receipts(command_id) ON UPDATE RESTRICT ON DELETE RESTRICT","closed_command_id TEXT REFERENCES command_receipts(command_id) ON UPDATE RESTRICT ON DELETE RESTRICT","reason_category TEXT","CHECK((relationship_state='active' AND closed_at_utc IS NULL AND closed_command_id IS NULL) OR (relationship_state='superseded' AND closed_at_utc IS NOT NULL AND closed_command_id IS NOT NULL))","CHECK(origin_kind<>'advanced_search_review' OR reconciliation_proposal_id IS NOT NULL)","CHECK(reference_role<>'current_handler_reference' OR supporting_sr_source_field_observation_id IS NOT NULL)"],"rules":["at most one active relationship exists per Service Request and reference_role","Contact identity and affiliation remain LLD-02 authority; this table only records ticket relationship/context","customer_org_context_id captures organization-at-use when the relationship opens and is immutable history even if Contact affiliation or SR Customer later changes","a Contact affiliation mismatch is reviewed explicitly and never silently reassigns/merges the Contact","customer_contact may be linked manually or through reviewed source reconciliation; source Customer Contact text remains separate accepted source evidence","current_handler_reference is only an identity resolution of the source-owned Current Handler fact; it never supplies a local handler string or overrides the current source projection","a current_handler_reference is effective for current-handler display only while supporting_sr_source_field_observation_id equals sr_current_source_projection.current_handler_observation_id; when source handler changes or clears, the old relationship remains history but is not current handler authority","static guards verify supporting observation belongs to the same SR and uses field_key customer_contact_label or current_handler_label matching reference_role"]}],"indexes":["CREATE UNIQUE INDEX uq_sr_customer_active ON sr_customer_relationships(service_request_id) WHERE relationship_state='active'","CREATE INDEX idx_sr_customer_sr_fk ON sr_customer_relationships(service_request_id,sr_customer_relationship_id)","CREATE INDEX idx_sr_customer_customer_fk ON sr_customer_relationships(customer_org_id,sr_customer_relationship_id)","CREATE INDEX idx_sr_customer_proposal_fk ON sr_customer_relationships(reconciliation_proposal_id,sr_customer_relationship_id)","CREATE INDEX idx_sr_customer_open_command_fk ON sr_customer_relationships(opened_command_id,sr_customer_relationship_id)","CREATE INDEX idx_sr_customer_close_command_fk ON sr_customer_relationships(closed_command_id,sr_customer_relationship_id)","CREATE UNIQUE INDEX uq_sr_contact_active_role ON sr_contact_relationships(service_request_id,reference_role) WHERE relationship_state='active'","CREATE INDEX idx_sr_contact_sr_fk ON sr_contact_relationships(service_request_id,sr_contact_relationship_id)","CREATE INDEX idx_sr_contact_contact_fk ON sr_contact_relationships(contact_id,sr_contact_relationship_id)","CREATE INDEX idx_sr_contact_customer_context_fk ON sr_contact_relationships(customer_org_context_id,sr_contact_relationship_id)","CREATE INDEX idx_sr_contact_proposal_fk ON sr_contact_relationships(reconciliation_proposal_id,sr_contact_relationship_id)","CREATE INDEX idx_sr_contact_source_obs_fk ON sr_contact_relationships(supporting_sr_source_field_observation_id,sr_contact_relationship_id)","CREATE INDEX idx_sr_contact_open_command_fk ON sr_contact_relationships(opened_command_id,sr_contact_relationship_id)","CREATE INDEX idx_sr_contact_close_command_fk ON sr_contact_relationships(closed_command_id,sr_contact_relationship_id)"],"database_guards":["BEFORE UPDATE permits only governed active-to-superseded closure fields; superseded rows reject further UPDATE","BEFORE DELETE always raises append-only relationship-history protection","BEFORE INSERT current_handler_reference validates supporting source observation belongs to the same SR and field_key='current_handler_label'","BEFORE INSERT advanced_search_review customer_contact validates optional supporting source observation, when supplied, belongs to the same SR and field_key='customer_contact_label'","BEFORE INSERT origin_kind='advanced_search_review' validates reconciliation_proposal_id resolves to a terminal accepted LLD-04 proposal for the same Service Request context; Customer rows require proposal_kind='sr_customer_reconciliation', customer_contact rows require 'sr_contact_reconciliation', and current_handler_reference rows require 'sr_current_handler_reconciliation'","all relationship acceptance revalidates LLD-02 target lifecycle/current affiliation and LLD-03 SR revision/current reference state inside the writer UnitOfWork","reference freshness token binds SR revision, active Customer/contact relationship IDs, target Contact revision and current affiliation relationship identity/revision, target Customer revision, and exact supporting source observation when applicable"] ,"migration_dependency":"LLD-04 import evidence/proposal tables referenced by these cross-packet FKs must exist before this LLD-03 schema extension is created. This schema-order dependency does not invert runtime ownership: LLD-04 submits reviewed proposals through LLD-03 services; it never writes LLD-03 tables directly.","ownership":"LLD-02 owns Customer/Contact identities and affiliation; LLD-03 owns SR-to-reference relationship history; LLD-04 owns immutable import evidence/proposals; LLD-06 owns classification consequences. No packet may bypass these interfaces by writing another owner's tables directly."}