{"schema":"SOMA-LLD-COMMANDS-V1","lld_id":"LLD-03","commands":[{"name":"ApplyAcceptedRfcSourceProjection","visibility":"internal cross-packet command invoked by LLD-04 accepted reconciliation; not a raw workbook route","input":["existing LLD-04 UnitOfWork","accepted LLD-04 RFC reconciliation proposal with exact RFC/base/evidence/review bindings","parent command_context carrying outer command_id/hash and already-inserted parent command_receipt"],"receipt_authority":"parent_shared_uow","steps":["inside the shared UoW require the LLD-04 parent command receipt already exists, then load exact RFC/current projection/hierarchy and revalidate current owner state","validate accepted evidence identities and LLD-04 precedence through RfcSourceEvidenceProvider","determine the exact current-field changes and any terminal-epoch/cascade effect","apply only accepted usable current field changes using the parent command context for provenance; LLD-03 inserts no command receipt","for transition into terminal allocate terminal_epoch_id and atomically capture/insert exact pending cascade proposal and immutable RFC/WFM membership","for terminal reversal require exact fresh high-risk review, clear current terminal epoch and supersede any still-pending proposal for that epoch without undoing executed history","append minimal LLD-03 audit/result references and return RfcLifecycleProjectionV1 to the LLD-04 coordinator without nested commit"],"rules":["LLD-04 AcceptReconciliationProposal exclusively owns replay resolution, command_id/hash authority, the single command receipt and the outer commit for this shared UnitOfWork","source acceptance never overwrites RFC hierarchy, SR links, Device Reference links, Working Notes, archive state or canonical Customer without their governed commands","terminal evidence acceptance never executes Task/Objective/Communication cascade consequences","unchanged current accepted fields produce semantic NO_CHANGE at field level and no duplicate current-history authority"]},{"name":"RefreshRfcTerminalCascadeProposal","input":["pending proposal_id","base proposal revision","command_id"],"response":"RfcTerminalCascadeStateV1 exact persisted state produced inside the owning writer UnitOfWork; NO_CHANGE returns unchanged pending state, material refresh returns replacement pending state","steps":["inside UoW prove proposal is pending at exact base revision and trigger RFC still has same terminal_epoch_id","load current accepted terminal status class/evidence for that epoch; Closed<->Cancelled correction becomes replacement terminal binding, while nonterminal/reversed or different epoch is stale","capture current exact RFC/WFM scope using current accepted terminal class/evidence and compute SOMA_RFC_TERMINAL_CASCADE_SCOPE_V1 replacement fingerprint","if terminal class/evidence and complete scope fingerprint are unchanged return semantic NO_CHANGE with exact unchanged state","insert receipt, supersede old proposal, insert replacement pending proposal/member rows using same terminal epoch/current binding, audit refresh, persist exact replacement state, commit"],"rules":["refresh never executes downstream consequences","executed or superseded proposal cannot be refreshed","refresh never edits proposal identity/evidence/fingerprint/member rows in place","terminal evidence correction within same epoch is a material refresh even when membership unchanged","refresh never invokes PreviewRfcTerminalCascade inside writer UnitOfWork"]},{"name":"ArchiveRfc","input":["rfc_id","scope_kind exact_rfc|reviewed_branch","reviewed_scope_fingerprint SOMA_RFC_ARCHIVE_SCOPE_V1","command_id"],"response":"ArchiveRfcResultV1 fixed-size exact committed result","steps":["exact replay resolves before owner preparation/current-state reads","inside UoW recompute complete current SOMA_RFC_ARCHIVE_SCOPE_V1 and require reviewed_scope_fingerprint exact match; client supplies no member revision list","identify active members and already-archived excluded members","if no active member return semantic NO_CHANGE ArchiveRfcResultV1 with null operation id and no operation/event/audit","allocate archive operation ID; after Foundation receipt insertion create operation row","transition each active reviewed member to archived, increment revision once, insert immutable operation membership and archived event","audit scalar counts/fingerprint with exactly one result ref to the archive operation; never copy member IDs into audit JSON/result refs","persist exact fixed-size ArchiveRfcResultV1 and commit"],"rules":["archive affects RFC local presentation only","pre-existing archived members are never claimed by this operation","reviewed_branch requires explicit complete preview and requested RFC remains parentless at writer revalidation","request/audit/replay bounds never impose branch-member business cap"]},{"name":"RestoreRfcArchiveOperation","input":["rfc_archive_operation_id","command_id"],"response":"RfcArchiveOperationV1 exact bounded operation projection with complete SOMA_RFC_ARCHIVE_OPERATION_PROJECTION_V1; command response first page contains at most 32 RfcArchiveOperationMemberV1 items and continuation binds that exact projection fingerprint","steps":["exact replay resolves before current-state reads","inside UoW load immutable operation membership and latest event per member","select only members still archived by this exact origin operation; later restored/rearchived members are skipped","if no member eligible compute the complete current operation projection fingerprint and return semantic NO_CHANGE with exact bounded RfcArchiveOperationV1 first page of at most 32 members","after Foundation receipt insertion transition every eligible member active/increment revision and append restored events; mutation cardinality is independent of response-page cardinality","audit scalar changed_rfc_count with exactly one result ref to origin archive operation; exact restored identities stay row-wise events","after all mutations recompute/derive the complete current operation member projection, compute SOMA_RFC_ARCHIVE_OPERATION_PROJECTION_V1, build at most 32 first-page members plus continuation bound to that fingerprint, persist exact response and commit"],"rules":["restore never activates a member this operation did not itself archive","source lifecycle/status and downstream domains remain untouched","audit/result-ref/replay-response bounds never impose operation-member business cap","RfcArchiveOperationMemberV1 is the exact member projection type; command response paging is presentation only and does not bound restore work","replayed command response is immutable historical evidence; following its continuation after any later operation-member projection change fails stale/invalid rather than combining historical first-page state with newer continuation rows"]},{"name":"HardDeleteRfc","input":["rfc_id","base_revision","eligibility_fingerprint from an eligible PreviewRfcHardDelete","deliberate_action_proof LLD-12 DeliberateActionProofV1","command_id"],"response":"HardDeleteRfcResultV1 fixed-size exact committed result; replay never reads the deleted RFC","steps":["exact command replay resolves before prepare, RFC reads, provider calls or proof work","inside one BEGIN IMMEDIATE UnitOfWork load exact RFC/rfc_no/revision; missing target returns NOT_FOUND and revision mismatch returns RFC_HARD_DELETE_PREVIEW_STALE","rerun the exact algorithms/rfc-hard-delete.json local guard registry and all four provider classifications, derive LLD-03 provider freshness tokens and recompute SOMA_RFC_HARD_DELETE_ELIGIBILITY_V1","if any local/provider blocker or INDETERMINATE exists return RFC_HARD_DELETE_BLOCKED without proof consumption, receipt, audit or delete","if recomputed fingerprint differs from eligibility_fingerprint return RFC_HARD_DELETE_PREVIEW_STALE without proof consumption","validate_and_consume LLD-12 proof for action tickets.rfc.hard_delete, exact DeliberateActionTargetV1 {target_type:rfc,target_id:rfc_id}, base_revision and eligibility_fingerprint","allocate audit_event_id as hard_delete_evidence_id; insert command receipt then append minimal ticket.rfc.hard_deleted audit while target identity/business number remain available; confirmation_context_id equals proof.challenge_id and proof/session secret is not persisted","delete only the RFC row; no generic cascade; unexpected dependent FK/constraint rolls back receipt/audit/delete while consumed proof remains non-reusable per LLD-12","persist exact HardDeleteRfcResultV1 in command_receipt_results and commit"],"rules":["only a provably untouched manual/provisional local draft is eligible","accepted source adoption/provenance, hierarchy/SR/Device/Note/WFM/Task/Objective/Communication/Inventory/source-projection/archive/cascade/correction/protected-audit history blocks deletion","creation audit alone does not make never-used local draft undeletable; post-creation protected activity does","no default local child rows are silently deleted to manufacture eligibility","ordinary history-bearing RFCs use archive/terminal/correction flows","proof is deliberate-interaction evidence only and is consumed strictly after all current business/freshness guards pass"]}]}